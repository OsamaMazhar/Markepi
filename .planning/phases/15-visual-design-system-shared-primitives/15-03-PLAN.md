---
phase: 15-visual-design-system-shared-primitives
plan: 03
type: execute
wave: 3
depends_on:
  - 15-01
  - 15-02
files_modified:
  - Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - VIS-04
must_haves:
  truths:
    - "All design system primitives render side-by-side in Xcode Previews without building to a device"
    - "The WatermarkCore package compiles cleanly for all 3 targets (App, ShareExtension, PhotoEditExtension)"
    - "Every public type from Plans 01 and 02 is importable by all 3 targets"
  artifacts:
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift"
      provides: "Single-file Xcode Preview rendering all primitives side-by-side"
      exports: ["PreviewCatalog"]
  key_links:
    - from: "PreviewCatalog.body"
      to: "All MarkepiDesignSystem types"
      via: "import WatermarkCore"
      pattern: "markepiGlass|markepiTypography|markepiPrimary|MarkepiPillBar|markepiScrollEdgeProtection"
---

<objective>
Provide the design iteration tooling (PreviewCatalog for Xcode Previews) and verify that all 6 design system primitives from Plans 01–02 compile cleanly across all 3 application targets.

Purpose: D-23 requires a `PreviewCatalog.swift` rendering all primitives side-by-side so the developer can iterate on visual design without building to a device. Additionally, since WatermarkCore is consumed by 3 targets (App, ShareExtension, PhotoEditExtension), every `public` type must be reachable — compilation across all targets is the only reliable proof.

Output: One new file (`PreviewCatalog.swift`) and a passing cross-target `xcodebuild` result. No production code changes.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/15-visual-design-system-shared-primitives/15-CONTEXT.md
@.planning/phases/15-visual-design-system-shared-primitives/15-RESEARCH.md
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/MarkepiUtilities.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/GlassEffect/MarkepiGlassModifier.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/Typography/MarkepiTypography.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ButtonStyles/MarkepiButtonStyle.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/MarkepiPillBar.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ScrollEdge/MarkepiScrollEdgeProtection.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create PreviewCatalog.swift — side-by-side primitive gallery</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift</files>
  <read_first>
    - Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/MarkepiGlassModifier.swift — `.markepiGlass()` modifier API surface and parameter defaults
    - Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/Typography/MarkepiTypography.swift — `MarkepiTypography` enum cases and `.markepiTypography()` modifier
    - Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ButtonStyles/MarkepiButtonStyle.swift — `MarkepiButtonRole` enum, `MarkepiButtonStyle` ButtonStyle, and convenience extensions (`.markepiPrimary()`, `.markepiSecondary()`, `.markepiDestructive()`)
    - Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/MarkepiPillBar.swift — `ControlsSection` enum and `MarkepiPillBar` view signature
    - Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ScrollEdge/MarkepiScrollEdgeProtection.swift — `.markepiScrollEdgeProtection(headerContent:)` modifier
    - .planning/phases/15-visual-design-system-shared-primitives/15-CONTEXT.md:55 — D-23: single `PreviewCatalog.swift` rendering all primitives side-by-side for design iteration without building to device
  </read_first>
  <action>
    Create `PreviewCatalog.swift` at the DesignSystem/ root.

    **Struct: `PreviewCatalog`** (public `View`, not a preview provider itself — the preview provider is a separate `#Preview` block)
    Purpose: Render every Markepi design system primitive in a single scrollable view so the developer can visually verify all components at once.

    Body structure — use a `ScrollView(.vertical)` containing a `VStack(alignment: .leading, spacing: 24)` with labeled sections:

    **Section 1 — Glass Effects:**
    - Header: `Text("Glass Effects").markepiTypography(.sectionHeader)`
    - Row: A `RoundedRectangle(cornerRadius: 12).fill(.clear).frame(height: 60)` with `.markepiGlass(shape: RoundedRectangle(cornerRadius: 12), fallbackMaterial: .regularMaterial)` overlaid with `Text("Regular Glass — Sheet Surface").markepiTypography(.controlLabel)`
    - Row: A `Capsule().fill(.clear).frame(height: 44)` with `.markepiGlass(shape: Capsule(), fallbackMaterial: .ultraThinMaterial)` overlaid with `Text("Thin Glass — Toolbar/Button").markepiTypography(.controlLabel)`

    **Section 2 — Typography:**
    - Header: `Text("Typography").markepiTypography(.sectionHeader)`
    - For each `MarkepiTypography` case (via `CaseIterable`): render `Text(String(describing: style)).markepiTypography(style)` in a `VStack(alignment: .leading, spacing: 4)`

    **Section 3 — Buttons:**
    - Header: `Text("Buttons").markepiTypography(.sectionHeader)`
    - For each `MarkepiButtonRole` case: render a `Button {} label: { Label("\(role) Button", systemImage: "star.fill") }.buttonStyle(MarkepiButtonStyle(role: role))` — use the raw initializer, not the convenience extension
    - Also render one example using a convenience extension: `Button {} label: { Label("Primary (extension)", systemImage: "square.and.arrow.up") }.buttonStyle(.markepiPrimary())`

    **Section 4 — Pill Bar:**
    - Header: `Text("Pill Bar").markepiTypography(.sectionHeader)`
    - Declare a local `@State private var catalogSection: ControlsSection = .watermark`
    - Render `MarkepiPillBar(selection: $catalogSection)` inside a `.frame(height: 60)` container
    - Below it, render `Text("Selected: \(catalogSection.rawValue)").markepiTypography(.metadata)`

    **Section 5 — Scroll Edge Protection:**
    - Header: `Text("Scroll Edge Protection").markepiTypography(.sectionHeader)`
    - Create a small demo scroll area: 20 `Text("Scrolling content line \($0)").markepiTypography(.body)` items wrapped with `.markepiScrollEdgeProtection { Text("Glass Header").markepiTypography(.pillLabel).padding().frame(maxWidth: .infinity).background(.ultraThinMaterial, in: Capsule()) }` inside a `.frame(height: 200).clipShape(RoundedRectangle(cornerRadius: 12))` container

    **Preview provider:**
    At the bottom of the file, include a `#Preview` block:
    ```
    #Preview("Design System Catalog") {
        PreviewCatalog()
    }
    ```

    Do NOT use `@available(iOS 26, *)` on the preview itself — the catalog must render on iOS 18 (showing material fallbacks) and iOS 26 (showing glass effects). The individual modifiers handle availability internally.
  </action>
  <verify>
    <automated>grep -c 'struct PreviewCatalog' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -eq</automated>
    <automated>grep -c '#Preview' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated># Verify all 5 sections rendered:
    grep -c 'Glass Effects' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated>grep -c 'Typography' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 2 -ge</automated>
    <automated>grep -c 'Buttons' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated>grep -c 'Pill Bar' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated>grep -c 'Scroll Edge' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated># Verify all design system types referenced:
    grep -c 'markepiTypography' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated>grep -c 'markepiGlass' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated>grep -c 'MarkepiButtonStyle\|markepiPrimary' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated>grep -c 'MarkepiPillBar\|ControlsSection' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
    <automated>grep -c 'markepiScrollEdgeProtection' Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift | xargs test 1 -ge</automated>
  </verify>
  <acceptance_criteria>
    - `PreviewCatalog.swift` compiles and renders all 5 sections (Glass Effects, Typography, Buttons, Pill Bar, Scroll Edge) in a single scrollable view
    - Each section renders at least one instance of each design system primitive from Plans 01-02
    - `#Preview("Design System Catalog")` macro is present at the bottom of the file
    - No `@available(iOS 26, *)` on the preview — catalog renders on both iOS 18 and iOS 26
  </acceptance_criteria>
  <done>PreviewCatalog.swift ready — all primitives viewable in Xcode Previews without building to a device.</done>
</task>

<task type="auto">
  <name>Task 2: Verify cross-target compilation — all 3 targets build with DesignSystem/ primitives</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/PreviewCatalog.swift</files>
  <read_first>
    - .planning/phases/15-visual-design-system-shared-primitives/15-RESEARCH.md:836-839 — Environment check: Xcode 18 is required for `#available(iOS 26, *)` compilation. If `xcodebuild` fails with "unknown API" errors on the `glassEffect` symbol, fall back to building with a lower deployment target or validating only the non-glass paths.
    - .planning/phases/15-visual-design-system-shared-primitives/15-CONTEXT.md:82-85 — canonical refs: all 3 targets consume WatermarkCore; ShareExtensionRootView.swift and PhotosExtensionRootView.swift host ControlsView
  </read_first>
  <action>
    Run the full build gate script from Phase 9's post-wave step:

    ```bash
    bash scripts/build-gate.sh
    ```

    This script runs `xcodebuild` across all 3 targets (WatermarkApp, ShareExtension, PhotoEditExtension) via the WatermarkApp scheme.

    If the build succeeds (exit 0): DesignSystem/ primitives are confirmed reachable from all targets — done.

    If the build fails (exit non-zero):
    1. Inspect the error output. If errors are about `glassEffect` being an unknown symbol on the current Xcode version (pre-Xcode-18), this is expected — the `#available(iOS 26, *)` guard requires Xcode 18 SDK. Fall back to:
       ```bash
       xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build CODE_SIGNING_ALLOWED=NO 2>&1 | grep -E 'error:|BUILD' | tail -30
       ```
    2. If errors are about missing `public` access on any DesignSystem type, fix the access modifier on the affected type and re-run.
    3. If errors are about a specific design system file not being included in the target, verify the file is under `Sources/WatermarkCore/` (SPM auto-includes per Package.swift line 19).
    4. Any other compile error: document in the SUMMARY.md and flag for the next phase.

    Do NOT modify any existing production code (ControlsView, ContentView, extension roots) — Phase 15 only adds new files to DesignSystem/.

    If the `build-gate.sh` script doesn't exist at project root, fall back to:
    ```bash
    xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build CODE_SIGNING_ALLOWED=NO 2>&1 | tail -30
    ```
  </action>
  <verify>
    <automated>bash scripts/build-gate.sh 2>&1 | grep -q 'BUILD GATE: PASSED' || bash scripts/build-gate.sh 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/build-gate.sh` exits 0 with "BUILD GATE: PASSED" — all 3 targets compile cleanly with the new DesignSystem/ primitives
    - OR (if on pre-Xcode-18): `xcodebuild` completes with errors ONLY on `glassEffect` symbol (expected — requires Xcode 18 SDK), and all other DesignSystem types compile cleanly
    - No existing production code modified
    - No `public` access modifier missing on any DesignSystem type
  </acceptance_criteria>
  <done>Cross-target compilation verified — all 6 DesignSystem/ primitives are reachable from App, ShareExtension, and PhotoEditExtension targets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No trust boundaries intersect this phase. PreviewCatalog is a development-only tool. Cross-target compilation is a build-gate verification step, not a runtime boundary.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-08 | Denial of Service (UX) | PreviewCatalog — crash on launch | accept | PreviewCatalog is a development-only tool rendered in Xcode Previews. A crash during preview has zero user impact. If a primitive fails to render in the catalog, the developer debugs the individual primitive — exactly the catalog's purpose. |
| T-15-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs in this phase. Build gate uses only `xcodebuild` and `bash scripts/build-gate.sh`. |
</threat_model>

<verification>
The build gate script (`bash scripts/build-gate.sh`) is the definitive verification. A passing result means:
1. All 6 DesignSystem files are included in the WatermarkCore target (SPM auto-include confirmed)
2. All `public` access modifiers are correct — types are importable by all 3 targets
3. No compilation errors from availability-gated code paths
</verification>

<success_criteria>
1. `PreviewCatalog.swift` renders all 5 visual sections (Glass Effects, Typography, Buttons, Pill Bar, Scroll Edge) in a single scrollable Xcode Preview
2. `bash scripts/build-gate.sh` exits 0 with "BUILD GATE: PASSED" (or all non-glassEffect code compiles if pre-Xcode-18)
3. No existing source files modified — Phase 15 is purely additive
4. All DesignSystem/ primitives are reachable from App, ShareExtension, and PhotoEditExtension
</success_criteria>

<output>
Create `.planning/phases/15-visual-design-system-shared-primitives/15-03-SUMMARY.md` when done
</output>
