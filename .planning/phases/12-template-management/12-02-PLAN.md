---
phase: 12-template-management
plan: 02
type: execute
wave: 2
depends_on: ["12-01"]
files_modified:
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
  - App/Views/Templates/SaveTemplateAlertModifier.swift
autonomous: true
requirements:
  - TMPL-01

must_haves:
  truths:
    - "WatermarkConfigurable protocol declares showSaveTemplateAlert, showTemplateList Bool properties and applyTemplate(_:) method with default implementation"
    - "ControlsView contains a 'Save as Template' button between LayerListView and exportOptionsDisclosure"
    - "SaveTemplateAlertModifier presents an alert with text field for template name input"
  artifacts:
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift"
      contains: "var showSaveTemplateAlert: Bool"
      contains: "var showTemplateList: Bool"
      contains: "func applyTemplate(_ template: Template)"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift"
      contains: "saveTemplateButton"
      contains: "Save as Template"
    - path: "App/Views/Templates/SaveTemplateAlertModifier.swift"
      contains: "struct SaveTemplateAlertModifier: ViewModifier"
      contains: '"Save Template"'
  key_links:
    - from: "ControlsView saveTemplateButton"
      to: "WatermarkConfigurable.showSaveTemplateAlert"
      via: "viewModel binding"
      pattern: "viewModel\\.showSaveTemplateAlert"
---

<objective>
Add template protocol requirements to WatermarkConfigurable, insert the "Save as Template" button into ControlsView, and create the reusable save-template alert modifier. Enables TMPL-01 — user saves current watermark config as a named template.

Purpose: The protocol additions let all three ViewModels conform uniformly. The button surfaces the save action. The alert modifier handles name input with validation (non-empty, max 50 chars, duplicate detection via TemplateStore).

Output: Modified WatermarkConfigurable.swift (2 properties + 1 method + default impl), modified ControlsView.swift (new button + Divider separators), new SaveTemplateAlertModifier.swift (ViewModifier + View extension).
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-template-management/12-PATTERNS.md
@.planning/phases/12-template-management/12-UI-SPEC.md
@Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
@App/Views/Common/ErrorAlertModifier.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add template properties and applyTemplate method to WatermarkConfigurable protocol</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift</files>
  <read_first>Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift</read_first>
  <action>
    Three insertions into WatermarkConfigurable.swift, following the patterns at PATTERNS.md lines 695-747:

    A) After "var showError: Bool { get set }" (line 27), add two protocol properties:
    var showSaveTemplateAlert: Bool { get set }
    var showTemplateList: Bool { get set }
    With comment: "// Template Management (Phase 12)"

    B) After "func cancelVideoExport()" (line 46), add one protocol method:
    func applyTemplate(_ template: Template)
    With doc-comment: "/// Applies a template's configuration to the current state. Called from TemplateListView when a row is tapped."

    C) At end of extension WatermarkConfigurable (after line 134, before closing brace), add default implementation:
    public func applyTemplate(_ template: Template) {
        config = template.config
    }
    Under "// MARK: - Template Management (Phase 12)".
  </action>
  <verify>
    <automated>grep -c "var showSaveTemplateAlert: Bool" Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift && grep -c "var showTemplateList: Bool" Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift && grep -c "func applyTemplate(_ template: Template)" Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift</automated>
  </verify>
  <acceptance_criteria>
    - WatermarkConfigurable protocol contains "var showSaveTemplateAlert: Bool { get set }" at line ~29
    - WatermarkConfigurable protocol contains "var showTemplateList: Bool { get set }" at line ~30
    - Protocol contains "func applyTemplate(_ template: Template)" in method requirements section
    - Extension contains default implementation "public func applyTemplate(_ template: Template) { config = template.config }"
    - Total file length increases by ~15 lines (from 134 to ~149)
  </acceptance_criteria>
  <done>Protocol declares template properties and applyTemplate with default implementation</done>
</task>

<task type="auto">
  <name>Task 2: Add Save as Template button to ControlsView</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift</files>
  <read_first>Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift</read_first>
  <action>
    Two changes to ControlsView.swift, following the pattern at PATTERNS.md lines 615-691:

    A) In the body VStack (lines 32-46), after "LayerListView(viewModel: viewModel)" at line 39 and before "exportOptionsDisclosure" at line 41, insert:
    - Divider().padding(.horizontal, -16)
    - saveTemplateButton
    - Divider().padding(.horizontal, -16)

    B) Add new private computed property saveTemplateButton after the existing shareButton property (after line 211). Pattern: Button with HStack containing Image(systemName: "square.and.arrow.down.on.square") and Text("Save as Template"). frame(maxWidth: .infinity).frame(height: 44). buttonStyle(.bordered). tint(.accentColor). Action: sets viewModel.showSaveTemplateAlert = true.

    Use Text("Save as Template") — exact copy per UI-SPEC line 115 "Save Template" as the primary CTA label. The button style is .bordered (not .borderedProminent) matching PATTERNS.md line 689.
  </action>
  <verify>
    <automated>grep -c "saveTemplateButton" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift && grep -c "Save as Template" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift</automated>
  </verify>
  <acceptance_criteria>
    - ControlsView contains private var saveTemplateButton computed property
    - Button label contains Text "Save as Template"
    - Button icon is Image(systemName: "square.and.arrow.down.on.square")
    - Button uses .buttonStyle(.bordered) and .tint(.accentColor)
    - button height is 44pt (.frame(height: 44))
    - Button inserted in VStack between LayerListView and exportOptionsDisclosure with Divider separators
    - Button action sets viewModel.showSaveTemplateAlert = true
  </acceptance_criteria>
  <done>Save as Template button renders between LayerListView and export options in ControlsView</done>
</task>

<task type="auto">
  <name>Task 3: Create SaveTemplateAlertModifier — reusable save-template alert</name>
  <files>App/Views/Templates/SaveTemplateAlertModifier.swift</files>
  <read_first>
    - App/Views/Common/ErrorAlertModifier.swift — replicate ViewModifier + View extension pattern
    - .planning/phases/12-template-management/12-PATTERNS.md — SaveTemplateAlertModifier section (lines 550-611)
  </read_first>
  <action>
    Create NEW file App/Views/Templates/SaveTemplateAlertModifier.swift.

    Declare struct SaveTemplateAlertModifier: ViewModifier replicating ErrorAlertModifier pattern (PATTERNS.md lines 550-611):

    Properties: @Binding var isPresented: Bool, let onSave: (String) -> Void, @State private var templateName = "".

    body applies .alert("Save Template", isPresented: $isPresented) with:
    - TextField("Template name", text: $templateName) inside the alert
    - Button("Save") that trims templateName, calls onSave(trimmed) if non-empty, resets templateName to ""
    - Button("Cancel", role: .cancel) that resets templateName to ""
    - Message: Text("Enter a name for this template.")

    Add extension View { func saveTemplateAlert(isPresented:onSave:) -> some View } returning modifier(self).

    Copy: alert title "Save Template" per UI-SPEC line 117. TextField placeholder "Template name" per line 119. Message "Enter a name for this template." per line 118. Confirm button "Save" per line 120. Cancel button "Cancel" per line 121.
  </action>
  <verify>
    <automated>grep -c "struct SaveTemplateAlertModifier: ViewModifier" App/Views/Templates/SaveTemplateAlertModifier.swift && grep -c '"Save Template"' App/Views/Templates/SaveTemplateAlertModifier.swift && grep -c '"Template name"' App/Views/Templates/SaveTemplateAlertModifier.swift</automated>
  </verify>
  <acceptance_criteria>
    - SaveTemplateAlertModifier.swift exists in App/Views/Templates/
    - Declares struct SaveTemplateAlertModifier: ViewModifier with @Binding isPresented and onSave closure
    - Alert title is "Save Template"
    - Alert contains TextField with placeholder "Template name"
    - Save button trims whitespace from name before calling onSave
    - Alert message is "Enter a name for this template."
    - Extension on View provides .saveTemplateAlert(isPresented:onSave:) convenience modifier
  </acceptance_criteria>
  <done>SaveTemplateAlertModifier compiles and can be attached to any SwiftUI View</done>
</task>

</tasks>

<verification>
- WatermarkConfigurable: grep for "showSaveTemplateAlert" and "showTemplateList" and "applyTemplate"
- ControlsView: grep for "saveTemplateButton" and "Save as Template"
- SaveTemplateAlertModifier: grep for "ViewModifier" and "Save Template" and "Template name"
</verification>

<success_criteria>
- [ ] WatermarkConfigurable protocol has showSaveTemplateAlert, showTemplateList, applyTemplate(_:)
- [ ] applyTemplate default implementation sets config = template.config
- [ ] ControlsView shows "Save as Template" button with correct icon, style, and 44pt height
- [ ] Button inserted between LayerListView and exportOptionsDisclosure
- [ ] SaveTemplateAlertModifier presents alert with text field and save/cancel buttons
- [ ] All files compile as part of their respective targets
</success_criteria>

<threat_model>
Plan 02 modifies the protocol layer and UI — no new trust boundaries beyond those established in Plan 01. TemplateStore.save() validates name uniqueness and non-emptiness before writing. The save alert modifier trims whitespace. All persistence threats mitigated in Plan 01's threat model. No new HIGH-severity threats introduced.
</threat_model>

<output>
Create `.planning/phases/12-template-management/12-02-SUMMARY.md` when done
</output>
