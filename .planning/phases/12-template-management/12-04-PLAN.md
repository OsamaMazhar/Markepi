---
phase: 12-template-management
plan: 04
type: execute
wave: 3
depends_on: ["12-01", "12-02", "12-03"]
files_modified:
  - App/ViewModels/WatermarkViewModel.swift
  - App/Views/ContentView.swift
autonomous: true
requirements:
  - TMPL-01
  - TMPL-02
  - TMPL-04

must_haves:
  truths:
    - "WatermarkViewModel has showSaveTemplateAlert, showTemplateList properties and auto-applies default template on media import"
    - "ContentView presents TemplateListView as a sheet and save-template alert when bindings are true"
  artifacts:
    - path: "App/ViewModels/WatermarkViewModel.swift"
      contains: "var showSaveTemplateAlert: Bool = false"
      contains: "var showTemplateList: Bool = false"
      contains: "TemplateStore.shared.defaultTemplate"
    - path: "App/Views/ContentView.swift"
      contains: ".sheet(isPresented: $viewModel.showTemplateList)"
      contains: "TemplateListView(viewModel: viewModel)"
      contains: ".saveTemplateAlert(isPresented:"
  key_links:
    - from: "WatermarkViewModel handleSelection"
      to: "TemplateStore.defaultTemplate"
      via: "auto-apply on import"
      pattern: "defaultTemplate\\.config"
    - from: "ContentView sheet"
      to: "TemplateListView"
      via: "viewModel.showTemplateList binding"
    - from: "ContentView saveTemplateAlert"
      to: "TemplateStore.shared.save"
      via: "onSave closure"
      pattern: "TemplateStore\\.shared\\.save"
---

<objective>
Integrate template management into the main app: add template properties to WatermarkViewModel with default template auto-apply on media import, and wire ContentView with the template list sheet and save-template alert. Enables TMPL-01 (save template via ContentView alert), TMPL-02 (browse/apply via sheet), TMPL-04 (default auto-apply on main app import).

Purpose: WatermarkViewModel needs showSaveTemplateAlert and showTemplateList to conform to the expanded WatermarkConfigurable protocol. The auto-apply method checks for a default template after each media import path (handleSelection, handleIncomingFile, fetchMostRecentPhoto, loadFromClipboard, checkPendingIntent). ContentView presents TemplateListView as a sheet and SaveTemplateAlertModifier as an alert, both bound to ViewModel state.

Output: Modified WatermarkViewModel.swift (2 properties + 5 auto-apply insertion points), modified ContentView.swift (sheet + alert modifiers).
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-template-management/12-PATTERNS.md
@App/ViewModels/WatermarkViewModel.swift
@App/Views/ContentView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Storage/TemplateStore.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add template properties and auto-apply to WatermarkViewModel</name>
  <files>App/ViewModels/WatermarkViewModel.swift</files>
  <read_first>App/ViewModels/WatermarkViewModel.swift</read_first>
  <action>
    Five modifications to WatermarkViewModel.swift:

    A) After "var showError: Bool = false" (line 41), add two new properties:
    var showSaveTemplateAlert: Bool = false
    var showTemplateList: Bool = false
    With comment "// Phase 12: Template Management" per PATTERNS.md lines 818-823.

    B) After handleSelection's final line "showPicker = false" (line 196), before the closing brace of the Task, insert auto-apply:
    if let defaultTemplate = TemplateStore.shared.defaultTemplate { config = defaultTemplate.config }
    per PATTERNS.md lines 852-857.

    C) After handleIncomingFile's "Task { await loadSourceForComparison() }" (line 552), insert the same auto-apply:
    if let defaultTemplate = TemplateStore.shared.defaultTemplate { config = defaultTemplate.config }

    D) After fetchMostRecentPhoto's "Task { await loadSourceForComparison() }" (line 633), insert auto-apply.

    E) After loadFromClipboard's "Task { await loadSourceForComparison() }" (line 665), insert auto-apply.

    F) After checkPendingIntent's media load completes (around line 693, after setting photos and currentIndex), insert auto-apply.

    The auto-apply pattern is identical at every insertion point:
    if let defaultTemplate = TemplateStore.shared.defaultTemplate { config = defaultTemplate.config }
    config.didSet triggers AppGroupConfigSync.save(config) automatically — no extra sync code needed.
  </action>
  <verify>
    <automated>grep -c "var showSaveTemplateAlert: Bool = false" App/ViewModels/WatermarkViewModel.swift && grep -c "var showTemplateList: Bool = false" App/ViewModels/WatermarkViewModel.swift && grep -c "TemplateStore.shared.defaultTemplate" App/ViewModels/WatermarkViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - WatermarkViewModel has showSaveTemplateAlert: Bool = false property
    - WatermarkViewModel has showTemplateList: Bool = false property
    - auto-apply code present in handleSelection (after showPicker = false)
    - auto-apply code present in handleIncomingFile
    - auto-apply code present in fetchMostRecentPhoto
    - auto-apply code present in loadFromClipboard
    - auto-apply code present in checkPendingIntent
    - Each auto-apply uses pattern "if let defaultTemplate = TemplateStore.shared.defaultTemplate { config = defaultTemplate.config }"
    - TemplateStore.shared.defaultTemplate appears exactly 5 times (one per import path)
  </acceptance_criteria>
  <done>WatermarkViewModel conforms to expanded WatermarkConfigurable and auto-applies default template on all import paths</done>
</task>

<task type="auto">
  <name>Task 2: Wire ContentView with template list sheet and save-template alert</name>
  <files>App/Views/ContentView.swift</files>
  <read_first>
    App/Views/ContentView.swift
    App/Views/Templates/TemplateListView.swift
    App/Views/Templates/SaveTemplateAlertModifier.swift
    .planning/phases/12-template-management/12-PATTERNS.md (ContentView section lines 749-799)
  </read_first>
  <action>
    Two insertions into ContentView.swift body, matching PATTERNS.md lines 775-798:

    A) After the existing .sheet for share sheet (line 85 closing brace), add template list sheet:
    .sheet(isPresented: $viewModel.showTemplateList) {
        NavigationStack {
            TemplateListView(viewModel: viewModel)
                .navigationTitle("Templates")
                .navigationBarTitleDisplayMode(.inline)
        }
    }

    B) After the template list .sheet, add save-template alert:
    .saveTemplateAlert(isPresented: $viewModel.showSaveTemplateAlert) { name in
        do {
            let template = Template(name: name, config: viewModel.config, isDefault: false)
            try TemplateStore.shared.save(template)
        } catch {
            viewModel.errorMessage = error.localizedDescription
            viewModel.showError = true
        }
    }

    Both modifiers chain onto the existing GeometryReader→VStack→modifier chain. Insert between the existing .sheet(isPresented: $viewModel.showShareSheet) closing brace and the .task modifier.

    Import WatermarkCore is already present (line 4). Template type and TemplateStore are accessible from WatermarkCore. SaveTemplateAlertModifier's View extension provides .saveTemplateAlert.
  </action>
  <verify>
    <automated>grep -c ".sheet(isPresented: \$viewModel.showTemplateList)" App/Views/ContentView.swift && grep -c "TemplateListView(viewModel: viewModel)" App/Views/ContentView.swift && grep -c ".saveTemplateAlert(isPresented:" App/Views/ContentView.swift && grep -c "TemplateStore.shared.save(template)" App/Views/ContentView.swift</automated>
  </verify>
  <acceptance_criteria>
    - ContentView has .sheet(isPresented: $viewModel.showTemplateList) modifier
    - Sheet presents NavigationStack containing TemplateListView(viewModel: viewModel) with title "Templates"
    - ContentView has .saveTemplateAlert(isPresented: $viewModel.showSaveTemplateAlert) modifier
    - saveTemplateAlert onSave closure creates Template(name:config:isDefault:) and calls TemplateStore.shared.save(template)
    - On save error, sets viewModel.errorMessage and viewModel.showError = true
    - Both modifiers are placed between the share sheet and .task modifiers
  </acceptance_criteria>
  <done>ContentView presents template list sheet and save-template alert when ViewModel bindings are true</done>
</task>

</tasks>

<verification>
- WatermarkViewModel: grep for "showSaveTemplateAlert", "showTemplateList", and "TemplateStore.shared.defaultTemplate"
- ContentView: grep for ".sheet(isPresented: $viewModel.showTemplateList)" and ".saveTemplateAlert"
- Verify 5 auto-apply insertion points with "grep -c 'TemplateStore.shared.defaultTemplate'"
</verification>

<success_criteria>
- [ ] WatermarkViewModel has both showSaveTemplateAlert and showTemplateList Bool properties initialized to false
- [ ] Default template auto-applies on all 5 media import paths in WatermarkViewModel
- [ ] ContentView presents TemplateListView as a sheet with NavigationStack wrapper
- [ ] ContentView's saveTemplateAlert creates and saves a Template via TemplateStore
- [ ] Template save errors propagate to the existing error alert (errorMessage/showError)
- [ ] Both files compile in the App target
</success_criteria>

<threat_model>
Plan 04 integrates templates into the main app. The auto-apply reads from TemplateStore.defaultTemplate which only returns templates explicitly marked as default by the user. The save-template alert closure creates Template with isDefault: false — default status can only be set via explicit user action (context menu). ContentView sheet binding only presents TemplateListView — no new attack surface beyond Plan 03. No new HIGH-severity threats introduced.
</threat_model>

<output>
Create `.planning/phases/12-template-management/12-04-SUMMARY.md` when done
</output>
