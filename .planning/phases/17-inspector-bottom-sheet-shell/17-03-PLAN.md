---
phase: 17-inspector-bottom-sheet-shell
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
  - 17-02
files_modified:
  - App/Views/ContentView.swift
  - App/Views/PreviewArea/PreviewView.swift
autonomous: true
requirements:
  - LYT-01
  - LYT-04

must_haves:
  truths:
    - "D-02: Full-bleed preview stays edge-to-edge behind glass sheet, no push/resize"
    - "D-06: Pinned action bar is Liquid Glass floating pill centered at bottom of preview, above sheet"
    - "D-07: Pinned bar contains Share button only — no batch controls, no import button"
    - "D-12: Preview gets no visual treatment (no dimming/blur/scale) as sheet expands"
    - "D-13: Shell renders correctly in light and dark — all colors system-adaptive, no hardcoded hex"
    - "D-16: Batch Cancel/Reset All stay in NavigationStack toolbar, not in sheet or pinned bar"
    - "D-17: Batch overlays (ThumbnailStripView, BatchProgressOverlay) at z=1 between preview (z=0) and sheet (z=2)"
    - "The photo/video preview fills the entire screen edge-to-edge as a full-bleed hero (LYT-01)"
    - "Controls live in a resizable glass bottom sheet with peek and expanded detents (LYT-02)"
    - "The Share/Watermark All button is always visible in a Liquid Glass floating pill above the sheet, regardless of detent (LYT-03)"
    - "The entire shell renders correctly in both system light and dark appearance — all colors are system-adaptive (LYT-04)"
    - "Batch overlays (ThumbnailStripView, BatchProgressOverlay) float between preview and sheet layers"
    - "Existing share/rendering behavior, toolbar items, modal sheets, and modifier groups work identically"
  artifacts:
    - path: "App/Views/ContentView.swift"
      provides: "Restructured main app shell with ZStack layout: full-bleed preview + batch overlays + glass sheet + pinned Share bar"
    - path: "App/Views/PreviewArea/PreviewView.swift"
      provides: "Full-bleed preview extending to all edges including home indicator area"
  key_links:
    - from: "ContentView pinnedShareBar"
      to: "ShareActionButton(viewModel: viewModel)"
      via: "Direct instantiation in Glass Capsule background"
      pattern: "ShareActionButton\\(viewModel:"
    - from: "ContentView inspectorSheet"
      to: "InspectorSheetView(detent:expandedHeight:peekHeight:viewModel:)"
      via: "ZStack layer at zIndex 2"
      pattern: "InspectorSheetView\\("
    - from: "ContentView batchOverlays"
      to: "ThumbnailStripView, BatchProgressOverlay"
      via: "ZStack layer at zIndex 1, conditional on viewModel state"
      pattern: "batchOverlays"
    - from: "PreviewView.swift"
      to: ".ignoresSafeArea()"
      via: "Replaces .edgesIgnoringSafeArea(.top) on line 94"
      pattern: "\\.ignoresSafeArea\\(\\)"
---

<objective>
Restructure ContentView's `mainLayout` from the hardcoded 60/40 VStack split into a ZStack layout with full-bleed preview (z=0), batch overlays (z=1), glass inspector sheet (z=2), and pinned Share action bar (z=3). Update PreviewView to full-bleed edge-to-edge via `.ignoresSafeArea()`.

Purpose: Per D-02/D-06/D-07/D-12/D-13/D-16/D-17, replace the split layout with a professional photo-editor shell: full-bleed hero preview, overlaying glass sheet with ControlsView, always-visible pinned Share button, and batch overlays at the correct Z layer. All existing functionality (toolbar, modal sheets, modifier groups, rendering pipeline) is preserved unchanged.

Output: Modified ContentView.swift (major restructure) + modified PreviewView.swift (single-line safe-area change).
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-inspector-bottom-sheet-shell/17-CONTEXT.md — D-02, D-06, D-07, D-12, D-13, D-16, D-17, canonical refs for ContentView
@.planning/phases/17-inspector-bottom-sheet-shell/17-RESEARCH.md — Pattern 1: Custom ZStack bottom sheet in ContentView, Pattern 4: Z-order layering for batch overlays, Pitfall 1 (ZStack clipping), Pitfall 5 (notch/home indicator)
@.planning/phases/17-inspector-bottom-sheet-shell/17-PATTERNS.md — Pattern 3: ContentView restructured layout, existing toolbar pattern, existing modifier groups
@.planning/phases/17-inspector-bottom-sheet-shell/17-UI-SPEC.md — Z-Stack Layer Map, Detent States table, Sheet Interaction Contract, Pinned Action Bar Contract
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ShareActionButton.swift — created in 17-01
@Packages/WatermarkCore/Sources/WatermarkCore/UI/InspectorSheetView.swift — created in 17-02
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Restructure ContentView mainLayout from 60/40 VStack to ZStack-based inspector shell</name>
  <files>App/Views/ContentView.swift</files>

  <read_first>
Read the full ContentView.swift (332 lines) before any edits. Read the newly created InspectorSheetView.swift for init signature. Read the newly created ShareActionButton.swift for init signature. Read 17-PATTERNS.md Pattern 3 for the complete restructured layout template. Read 17-UI-SPEC.md for exact spacing values (peekDetentHeight: 60pt, expandedDetentRatio: 0.55, pinned bar padding).
  </read_first>

  <behavior>
    - Test 1: ContentView renders with full-bleed preview at z=0, no 60% height constraint
    - Test 2: ContentView renders InspectorSheetView at z=2 with peek detent showing pill bar only
    - Test 3: ContentView renders pinned Share bar at z=3 above the sheet, always visible
    - Test 4: Batch overlays (ThumbnailStripView, BatchProgressOverlay) render at z=1 between preview and sheet
    - Test 5: NavigationStack toolbar with Cancel/Reset All/Import remains functional
    - Test 6: Existing modal sheets (ShareSheetView, TemplateListView, BatchItemDetailSheet) still work
    - Test 7: System light and dark appearance both render correctly with adaptive colors
  </behavior>

  <action>
**This is the core restructure of Phase 17.** Replace the hardcoded 60/40 VStack in `mainLayout(_:)` (lines 69-93) with a ZStack-based inspector layout.

**Step 1 — Add new @State properties (insert after line 20, before `var body`):**
```
    // Phase 17: Inspector bottom-sheet shell state
    @State private var detent: SheetDetent = .peek
    @State private var sheetDragOffset: CGFloat = 0

    // Peek detent height — pill bar intrinsic + drag indicator
    private let peekDetentHeight: CGFloat = 60
```
Import `SheetDetent` comes from `InspectorSheetView.swift` in the WatermarkCore module — no explicit import needed since WatermarkCore is already imported on line 4.

**Step 2 — Replace `mainLayout(_:)` (lines 69-93):**
Remove the entire `VStack(spacing: 0) { previewArea .frame(60%) + separator + controlsArea .frame(40%) }` block. Replace with:

```
    // MARK: - Main Layout (Phase 17: ZStack inspector shell)

    private func mainLayout(_ geometry: GeometryProxy) -> some View {
        let expandedHeight = geometry.size.height * 0.55

        ZStack(alignment: .bottom) {
            // z=0: Full-bleed preview (LYT-01)
            previewArea
                .ignoresSafeArea()

            // z=1: Batch overlays (D-17)
            batchOverlays
                .zIndex(1)

            // z=2: Glass bottom sheet (LYT-02)
            inspectorSheet(expandedHeight: expandedHeight)
                .zIndex(2)

            // z=3: Pinned Share action bar (LYT-03)
            pinnedShareBar
                .zIndex(3)
        }
        .task(id: viewModel.previewIdentifier) {
            guard viewModel.currentPhoto != nil else { return }
            await viewModel.generatePreview()
        }
        .onChange(of: viewModel.currentIndex) {
            viewModel.fullResResult = nil
            viewModel.renderingState = .idle
        }
        .onChange(of: viewModel.renderingState) { _, newState in
            if case .done = newState, viewModel.batchResults != nil {
                showBatchResultAlert = true
            }
        }
    }
```
The `.task`, `.onChange(of: currentIndex)`, and `.onChange(of: renderingState)` modifiers are MOVED from the old VStack to the new ZStack — they are NOT removed.

**Step 3 — Rewrite `previewArea` (lines 127-171):**
The previewArea now contains ONLY the PreviewView and the plus-button overlay. Batch overlays (ThumbnailStripView, BatchProgressOverlay) are REMOVED from here and moved to the root ZStack. The structure becomes:

```
    // MARK: - Preview Area (z=0)

    private var previewArea: some View {
        ZStack(alignment: .bottom) {
            PreviewView(viewModel: viewModel)

            // Plus button (top-right, existing)
            if viewModel.currentPhoto != nil {
                Button {
                    viewModel.showPicker = true
                } label: {
                    Image(systemName: "plus.rectangle.on.rectangle")
                        .font(.system(size: 22, weight: .regular))
                }
                .frame(width: 44, height: 44)
                .background(.ultraThinMaterial, in: RoundedRectangle(cornerRadius: 14))
                .frame(maxWidth: .infinity, maxHeight: .infinity, alignment: .topTrailing)
                .padding(12)
            }
        }
        .animation(.easeInOut(duration: 0.2), value: viewModel.renderingState)
    }
```
The `.ignoresSafeArea()` is applied at the ZStack level in mainLayout (Step 2), not here on previewArea itself — this allows the ZStack to extend edge-to-edge while the previewArea child fills that space.

**Step 4 — Create `batchOverlays` computed property (new):**
Extract the ThumbnailStripView and BatchProgressOverlay from the old previewArea (lines 131-155) into a new `@ViewBuilder` computed property. These go at zIndex 1 in the root ZStack.

```
    // MARK: - Batch Overlays (z=1, D-17)

    @ViewBuilder
    private var batchOverlays: some View {
        if viewModel.hasMultiplePhotos {
            ThumbnailStripView(
                photos: viewModel.photos,
                currentIndex: $viewModel.currentIndex,
                perItemOverrides: viewModel.perItemOverrides,
                onItemTapped: { index in
                    selectedItemForOverride = IdentifiableIndex(value: index)
                },
                onReorder: { reordered in
                    viewModel.photos = reordered
                }
            )
            .padding(.bottom, peekDetentHeight + 8)
        }

        if case .batchProcessing(let current, let total, let eta) = viewModel.renderingState {
            BatchProgressOverlay(
                current: current,
                total: total,
                eta: eta,
                onCancel: {
                    showBatchCancelConfirmation = true
                }
            )
            .transition(.opacity)
        }
    }
```
Key positioning: `.padding(.bottom, peekDetentHeight + 8)` on ThumbnailStripView offsets it above the sheet at peek detent — prevents overlap while keeping it visible through the glass.

**Step 5 — Create `inspectorSheet(expandedHeight:)` computed property (new):**
Instantiate the InspectorSheetView created in Plan 17-02 with the binding and bindings.

```
    // MARK: - Inspector Sheet (z=2, LYT-02)

    private func inspectorSheet(expandedHeight: CGFloat) -> some View {
        InspectorSheetView(
            detent: $detent,
            peekHeight: peekDetentHeight,
            expandedHeight: expandedHeight,
            viewModel: viewModel
        )
    }
```
The InspectorSheetView's init signature must match: `init(detent: Binding<SheetDetent>, peekHeight: CGFloat, expandedHeight: CGFloat, viewModel: ViewModel)`.

**Step 6 — Create `pinnedShareBar` computed property (new):**
The always-visible Share button in a Liquid Glass floating pill (D-06, D-07). This uses the ShareActionButton created in Plan 17-01.

```
    // MARK: - Pinned Share Bar (z=3, LYT-03)

    private var pinnedShareBar: some View {
        VStack {
            Spacer()
            ShareActionButton(viewModel: viewModel)
                .padding(.horizontal, 40)
                .padding(.bottom, peekDetentHeight + 16)
        }
        .background(alignment: .bottom) {
            Capsule()
                .fill(.clear)
                .markepiGlass(
                    shape: Capsule(),
                    isEnabled: !reduceTransparency
                )
                .frame(height: 52)
                .padding(.horizontal, 24)
                .padding(.bottom, peekDetentHeight + 8)
        }
    }
```
Note: `reduceTransparency` needs to be added as an `@Environment` property since ContentView currently doesn't have it. Add alongside the new state properties:
```
@Environment(\.accessibilityReduceTransparency) private var reduceTransparency
```
Also add the corresponding import if not already present (it is — SwiftUI is imported on line 2).

**Step 7 — Remove `controlsArea` computed property (lines 175-177):**
Delete the entire `private var controlsArea: some View { ControlsView(viewModel: viewModel) }` block. ControlsView now lives inside InspectorSheetView — not as a standalone computed property in ContentView.

**Step 8 — ZStack clipping prevention (Pitfall 1):**
Per RESEARCH.md Pitfall 1, ensure the pinned bar is a SIBLING of the sheet in the ZStack, NOT a child of the sheet. Both are children of the root ZStack. The `zIndex(3) > zIndex(2)` ensures the pinned bar renders on top. Verified by the structure: both `inspectorSheet(...).zIndex(2)` and `pinnedShareBar.zIndex(3)` are direct children of the ZStack in mainLayout.

**Do NOT modify:**
- The `body` property (lines 22-65) — NavigationStack wrapper, GeometryReader, toolbar, modifier groups all stay
- The `toolbarContent` (lines 97-123) — Cancel, Reset All, Import all stay in NavigationStack toolbar (D-16)
- The `AlertModifiers`, `BatchAlertModifiers`, `SheetModifiers` structs (lines 180-332) — all modal sheets (ShareSheetView, TemplateListView, BatchItemDetailSheet) preserved
- The `IdentifiableIndex` struct (lines 7-10)
- Any `@State` properties from lines 16-20 — only ADD new ones, never remove existing
- The `.photosPicker`, `.fileImporter`, `.onAppear` modifiers
  </action>

  <verify>
    <automated>MISSING — Wave 0 must create AppTests/ContentViewLayoutTests.swift with tests: ZStack has at least 4 children, previewArea uses ignoresSafeArea, InspectorSheetView instantiated with correct params, ShareActionButton used in pinned bar, batchOverlays present, controlsArea removed, toolbar unchanged</automated>
  </verify>

  <acceptance_criteria>
    - ContentView.swift `mainLayout(_:)` uses ZStack instead of VStack — grep confirms no `.frame(height: geometry.size.height * 0.60)` or `* 0.40`
    - `@State private var detent: SheetDetent = .peek` property exists in ContentView
    - `peekDetentHeight: CGFloat = 60` constant exists
    - `@Environment(\.accessibilityReduceTransparency)` declared for pinned bar glass gating
    - `previewArea` computed property contains PreviewView + plus button only (no ThumbnailStripView, no BatchProgressOverlay)
    - `batchOverlays` @ViewBuilder property exists with ThumbnailStripView and BatchProgressOverlay
    - `inspectorSheet(expandedHeight:)` function exists, instantiates InspectorSheetView with correct params
    - `pinnedShareBar` computed property exists with ShareActionButton in Capsule glass background
    - `controlsArea` computed property is REMOVED (grep returns no match)
    - All existing toolbar items (Cancel, Reset All, Import) are unchanged
    - All existing .sheet/.alert/.confirmationDialog modifiers are unchanged
    - ContentView compiles with all targets: `xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp build` exits 0
    - D-13: No hardcoded colors anywhere in new code — only .primary, .secondary, .accentColor, .secondary.opacity(0.4), system materials
  </acceptance_criteria>
  <done>ContentView mainLayout replaced with ZStack inspector shell. previewArea stripped of batch overlays. batchOverlays, inspectorSheet, pinnedShareBar computed properties created. controlsArea removed. All existing toolbar/modals/pipeline preserved. App scheme builds cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Update PreviewView to full-bleed .ignoresSafeArea() for true edge-to-edge hero</name>
  <files>App/Views/PreviewArea/PreviewView.swift</files>

  <read_first>
Read PreviewView.swift in full (143 lines). Focus on line 94 — the current `.edgesIgnoringSafeArea(.top)` modifier. Read RESEARCH.md Pitfall 5 for notch/home indicator considerations.
  </read_first>

  <action>
Single-line change to PreviewView.swift line 94:

**Change:**
```
.edgesIgnoringSafeArea(.top)
```
**To:**
```
.ignoresSafeArea()
```

`.ignoresSafeArea()` with no arguments applies to all edges (.all) — this is the modern SwiftUI API (`.edgesIgnoringSafeArea` is soft-deprecated). The preview view's frame `.frame(maxWidth: .infinity, maxHeight: .infinity)` on line 93 already fills available space; the safe area ignorance extends it behind the notch, Dynamic Island, and home indicator.

**Do NOT:**
- Change any other line in PreviewView.swift — the gesture handling (magnify, comparison, combined), zoom accessibility, picker button, and all image rendering logic stays identical
- Wrap in any conditional or availability check
- Adjust the image content mode or aspect ratio — `.aspectRatio(contentMode: .fit)` on the internal Image handles actual positioning; the view frame just extends edge-to-edge
  </action>

  <verify>
    <automated>xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp build 2>&1 | tee /dev/stderr | grep -c "BUILD SUCCEEDED"</automated>
  </verify>

  <acceptance_criteria>
    - PreviewView.swift line 94 reads `.ignoresSafeArea()` (not `.edgesIgnoringSafeArea(.top)` or `.edgesIgnoringSafeArea`)
    - App scheme builds successfully with zero errors
    - Preview extends to all screen edges — no black bar at bottom where the old 40% controls area was
    - Image aspect ratio is preserved — preview content uses `aspectRatio(contentMode: .fit)`, only the view frame extends
    - No other lines in PreviewView.swift changed
  </acceptance_criteria>
  <done>PreviewView uses .ignoresSafeArea() on all edges. Full-bleed hero extends behind home indicator and notch. App scheme builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries. The ContentView restructure reorders SwiftUI views within the same view hierarchy. No new data paths, no network, no external input surfaces.

| Boundary | Description |
|----------|-------------|
| _(none)_ | Phase 17 is presentation-layer-only. All user interaction flows through existing, tested ViewModel and WatermarkConfigurable protocol paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-04 | Denial of Service | ContentView ZStack layout | accept | ZStack with 4 children is a standard SwiftUI pattern with no known performance or memory implications. Each child is conditional (batch overlays) or lightweight (preview, sheet, button). No recursive rendering, no infinite layout loops. |
| T-17-05 | Information Disclosure | Full-bleed preview (LYT-01) | accept | `.ignoresSafeArea()` extends preview behind notch/indicator — this is standard iOS behavior for media apps (Photos, Lightroom). iOS handles home indicator visibility and notch-aware safe areas automatically. No sensitive information is exposed — the preview shows user's own photos, no PII or credentials. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No external packages installed. Apple system frameworks + WatermarkCore internal package only. |

## Note on Pre-Existing Threats

The ContentView already includes user-facing file import (`fileImporter`), image/video picker (`photosPicker`), and rendering pipeline. These existing input paths have their own threat models from prior phases (v1.0, v2.0). Phase 17 does not modify these paths — only the view layout changes. No new mitigations are needed.
</threat_model>

<verification>
- [ ] ContentView mainLayout uses ZStack, not VStack(60%/40%)
- [ ] SheetDetent @State, peekDetentHeight constant, reduceTransparency @Environment declared
- [ ] previewArea contains PreviewView + plus button only (no batch overlays)
- [ ] batchOverlays @ViewBuilder with ThumbnailStripView + BatchProgressOverlay at z=1
- [ ] inspectorSheet function instantiates InspectorSheetView at z=2
- [ ] pinnedShareBar instantiates ShareActionButton in Capsule glass at z=3
- [ ] controlsArea computed property removed entirely
- [ ] All existing toolbar items (Cancel, Reset All, Import) unchanged
- [ ] All existing sheets/alerts/confirmationDialogs unchanged
- [ ] PreviewView.swift uses .ignoresSafeArea() (all edges)
- [ ] App scheme builds: `xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp build` exits 0
- [ ] No hardcoded colors in any new code — system-adaptive semantic colors only (D-13/LYT-04)
- [ ] D-12: No dimming, blur, or scale treatment on preview
- [ ] D-16: Batch Cancel/Reset All in NavigationStack toolbar only
- [ ] D-17: Batch overlays at z=1 between preview and sheet
</verification>

<success_criteria>
ContentView renders as a professional photo-editing shell: full-bleed preview (z=0), batch overlays when applicable (z=1), glass inspector sheet with ControlsView (z=2), and always-visible pinned Share button (z=3). All existing functionality — toolbar items, modal sheets, rendering pipeline, modifier groups — is preserved without behavioral change. System light/dark appearance renders correctly with adaptive colors. PreviewView extends to all screen edges including notch/home indicator areas.
</success_criteria>

<output>
Create `.planning/phases/17-inspector-bottom-sheet-shell/17-03-SUMMARY.md` when done
</output>
