---
phase: 17-inspector-bottom-sheet-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ShareActionButton.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
autonomous: true
requirements:
  - LYT-03

must_haves:
  truths:
    - "D-05: ShareActionButton is a standalone component in WatermarkCore/DesignSystem/, the single source of truth for all 3 placements"
    - "D-08: ShareActionButton preserves the full rendering state machine: idle, rendering, renderingVideo, batchProcessing, done, error"
    - "ShareActionButton renders correctly for all 6 rendering states: idle, rendering, renderingVideo, batchProcessing, done, error"
    - "ControlsView uses ShareActionButton identically to its previous shareButton computed property"
    - "The Share/Watermark All button calls renderAndPrepareShare()/presentShareSheet()/cancelProcessing() exactly as before"
  artifacts:
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ShareActionButton.swift"
      provides: "Extracted standalone Share button component, placement-agnostic"
      exports: ["ShareActionButton"]
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift"
      provides: "ControlsView wired to ShareActionButton, shareButton computed property removed"
  key_links:
    - from: "ControlsView.swift outputSectionContent"
      to: "ShareActionButton(viewModel: viewModel)"
      via: "Direct instantiation replacing shareButton reference"
      pattern: "ShareActionButton\\(viewModel:"
    - from: "ShareActionButton.swift body switch"
      to: "WatermarkConfigurable protocol"
      via: "viewModel.renderAndPrepareShare(), viewModel.presentShareSheet(), viewModel.cancelProcessing()"
      pattern: "viewModel\\.(renderAndPrepareShare|presentShareSheet|cancelProcessing)"
---

<objective>
Extract the Share button state machine from ControlsView.swift:239-358 into a standalone reusable `ShareActionButton` component in the DesignSystem package, then wire it into ControlsView as a drop-in replacement for the existing `shareButton` computed property.

Purpose: Per D-05/D-08, the Share button must become a single source of truth consumed by three placements — the main app's pinned action bar (Phase 17-03), ControlsView's Output section (this plan), and extension shells via ControlsView. Extraction preserves all 6 rendering states, the protocol surface, and the animation behavior.

Output: `ShareActionButton.swift` (new, in WatermarkCore/DesignSystem/) + modified `ControlsView.swift` (shareButton property removed, replaced with ShareActionButton instantiation).
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-inspector-bottom-sheet-shell/17-CONTEXT.md
@.planning/phases/17-inspector-bottom-sheet-shell/17-RESEARCH.md — Pattern 3: ShareActionButton, Pitfall 4
@.planning/phases/17-inspector-bottom-sheet-shell/17-PATTERNS.md — Pattern 1: full extraction source + file structure
@.planning/phases/17-inspector-bottom-sheet-shell/17-UI-SPEC.md — ShareActionButton state machine interaction table, copy contract
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract ShareActionButton from ControlsView into standalone DesignSystem component</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ShareActionButton.swift</files>

  <read_first>
Read the full ControlsView.swift first (lines 1-15 for imports/struct declaration pattern, lines 239-358 for the shareButton computed property being extracted). Read WatermarkConfigurable.swift for the protocol surface (renderAndPrepareShare, presentShareSheet, cancelProcessing, renderingState, hasMultiplePhotos). Read ProcessingResult.swift for the RenderingState enum cases.
  </read_first>

  <behavior>
    - Test 1: ShareActionButton renders .idle state with "Share" label and markepiPrimary button style for single photo
    - Test 2: ShareActionButton renders .idle state with "Watermark All" label and multi-photo icon for batch mode
    - Test 3: ShareActionButton renders .rendering state with ProgressView spinner, "Rendering..." text, glass capsule background, disabled
    - Test 4: ShareActionButton renders .renderingVideo(0.5, 30) with 50% progress, "~30s remaining", Cancel button calling cancelProcessing()
    - Test 5: ShareActionButton renders .batchProcessing(3, 10, nil) with "3/10" progress, "--" ETA, "Stop Processing" button
    - Test 6: ShareActionButton renders .done state with "Ready to Share" and checkmark icon, calls presentShareSheet() on tap
    - Test 7: ShareActionButton renders .error state with "Retry" label, arrow.clockwise icon, markepiSecondary style, calls renderAndPrepareShare() on tap
  </behavior>

  <action>
Create the new file `Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ShareActionButton.swift`.

**Struct signature** — follow the ControlsView pattern (lines 1-15):
- `import SwiftUI` only (no other imports needed — ViewModel is generic, MarkepiButtonStyle/MarkepiGlassModifier are in same WatermarkCore module)
- Doc comment: "A standalone Share/Render button that drives off the ViewModel's renderingState. Generic over any WatermarkConfigurable & Observable ViewModel. Placement-agnostic — does not assume it's in ControlsView, a toolbar, or a floating pill."
- `public struct ShareActionButton<ViewModel: WatermarkConfigurable & Observable>: View`
- `@State var viewModel: ViewModel`
- `@Environment(\.accessibilityReduceMotion) private var reduceMotion`
- `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency`
- `public init(viewModel: ViewModel) { self.viewModel = viewModel }`

**Body** — extract the ENTIRE `shareButton` computed property (ControlsView.swift lines 239-358) into `public var body: some View`. The extraction is a verbatim code move with these adjustments:
- Replace `isBatchMode` references with `viewModel.hasMultiplePhotos` (ControlsView has a local `isBatchMode` computed property; ShareActionButton uses the protocol property directly)
- The `Group { switch viewModel.renderingState { ... } }` wrapper stays
- The final `.animation(reduceMotion ? nil : .easeInOut(duration: 0.2), value: viewModel.renderingState)` modifier stays

**Video rendering helper** — extract ControlsView.swift lines 268-298 (the `.renderingVideo` case body) into a `private func videoRenderingView(progress: Double, eta: TimeInterval?) -> some View` method. Keep all subviews, padding, and button style identical. The Cancel button calls `viewModel.cancelProcessing()`.

**Batch processing helper** — extract ControlsView.swift lines 300-330 (the `.batchProcessing` case body) into a `private func batchProcessingView(current: Int, total: Int, eta: TimeInterval?) -> some View` method. Keep all subviews identical. The "Stop Processing" button calls `viewModel.cancelProcessing()`.

**Do NOT:**
- Rewrite the switch cases — this is a copy-paste extraction, not a redesign
- Change button labels, icons, or copy — preserve exactly: "Share", "Watermark All", "Rendering...", "Cancel", "Stop Processing", "Ready to Share", "Ready to Share All", "Retry"
- Change the animation timing or spring parameters
- Add any new states or behaviors
  </action>

  <verify>
    <automated>MISSING — Wave 0 must create Packages/WatermarkCore/Tests/WatermarkCoreTests/ShareActionButtonTests.swift first with tests for all 6 rendering state assertions via a mock ViewModel</automated>
  </verify>

  <acceptance_criteria>
    - ShareActionButton.swift exists at the correct path within the WatermarkCore package
    - The struct is `public` with generic constraint `<ViewModel: WatermarkConfigurable & Observable>`
    - Body contains a `Group { switch viewModel.renderingState { ... } }` with all 6 cases: .idle, .rendering, .renderingVideo, .batchProcessing, .done, .error
    - All button copy matches CONTEXT.md D-08 and 17-UI-SPEC.md copy contract verbatim
    - `videoRenderingView(progress:eta:)` and `batchProcessingView(current:total:eta:)` exist as private methods
    - `.animation(reduceMotion ? nil : .easeInOut(duration: 0.2), value: viewModel.renderingState)` modifier is present
    - No `isBatchMode` references remain — all replaced with `viewModel.hasMultiplePhotos`
    - File compiles: `xcodebuild -project Watermark.xcodeproj -scheme WatermarkCore build` exits 0
  </acceptance_criteria>
  <done>ShareActionButton.swift created in WatermarkCore/DesignSystem/ with all 6 rendering states extracted verbatim from ControlsView. WatermarkCore scheme builds without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Wire ShareActionButton into ControlsView, remove shareButton computed property</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift</files>

  <read_first>
Read ControlsView.swift lines 145-160 (the outputSectionContent where shareButton appears), lines 239-358 (the shareButton property being removed). Read the newly created ShareActionButton.swift for the correct init signature.
  </read_first>

  <action>
Two changes to ControlsView.swift:

**Change 1 — Replace shareButton usage (line 152):**
Replace:
```
shareButton
    .padding(.horizontal, 16)
```
With:
```
ShareActionButton(viewModel: viewModel)
    .padding(.horizontal, 16)
```

**Change 2 — Remove the `shareButton` computed property:**
Delete the entire `private var shareButton: some View { ... }` computed property (lines 239-358). All 120 lines including the Group wrapper, switch statement, all 6 cases, the `.animation` modifier, and the closing brace.

**Do NOT:**
- Change any other line in ControlsView.swift — D-14's shell-agnostic mandate is absolute
- Touch the struct declaration, section content, `isBatchMode`, ControlSection, or any other computed property
- Add new imports (ShareActionButton is in DesignSystem within the same WatermarkCore module)
- Change the padding or layout around the new ShareActionButton instantiation

**Note:** ShareActionButton.swift is in `WatermarkCore/Sources/WatermarkCore/DesignSystem/` — same module as ControlsView (`WatermarkCore/Sources/WatermarkCore/UI/`). No import statement needed since both files share the WatermarkCore module. If the compiler requires an explicit import for intra-module visibility within a Swift Package, add `import WatermarkCore` at the top (matching the existing import on line 2).
  </action>

  <verify>
    <automated>xcodebuild -project Watermark.xcodeproj -scheme WatermarkCore build 2>&1 | tee /dev/stderr | grep -c "BUILD SUCCEEDED"</automated>
  </verify>

  <acceptance_criteria>
    - ControlsView.swift no longer contains a `shareButton` computed property (grep for `shareButton` in ControlsView.swift returns only the instantiation, not a var declaration)
    - Line ~152 reads `ShareActionButton(viewModel: viewModel)` with `.padding(.horizontal, 16)`
    - WatermarkCore scheme builds successfully with zero errors and zero warnings related to this change
    - ControlsView body compiles — all other sections (pill bar, watermark controls, style controls, output section) remain functional
    - The ControlsView struct declaration, generic constraint, @Environment properties, and body structure are unchanged
  </acceptance_criteria>
  <done>ControlsView uses ShareActionButton as drop-in replacement. shareButton computed property removed. WatermarkCore builds cleanly.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new trust boundaries are introduced in this phase. The Code Extraction is a pure refactor within the WatermarkCore module and the App target's ContentView.

| Boundary | Description |
|----------|-------------|
| _(none)_ | Phase 17 is presentation-layer-only. No new input paths, no network boundaries, no persistent storage changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-17-01 | Tampering | ShareActionButton.swift (extracted from ControlsView) | accept | Pure extraction — no behavioral change. All protocol calls (renderAndPrepareShare, presentShareSheet, cancelProcessing) go through the frozen WatermarkConfigurable protocol. The rendering state machine is unchanged from shipped v2.0 code. No new attack surface. |
| T-17-SC | Tampering | npm/pip/cargo installs | accept | No external packages are installed in this phase. The implementation uses only Apple's SwiftUI framework and the project's own WatermarkCore Swift Package (already part of the repository). |
</threat_model>

<verification>
- [ ] ShareActionButton.swift exists at `Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ShareActionButton.swift`
- [ ] ControlsView.swift no longer contains `shareButton` as a computed property
- [ ] ControlsView.swift uses `ShareActionButton(viewModel: viewModel)` in the Output section
- [ ] WatermarkCore scheme builds without errors: `xcodebuild -project Watermark.xcodeproj -scheme WatermarkCore build`
- [ ] ShareActionButton body contains all 6 rendering state cases matching the extracted source
- [ ] D-05 satisfied: Share button is a standalone component in WatermarkCore/DesignSystem/
- [ ] D-08 satisfied: Full rendering state machine preserved (idle/rendering/renderingVideo/batchProcessing/done/error)
</verification>

<success_criteria>
The Share button is extracted from ControlsView into a standalone `ShareActionButton` component living in WatermarkCore/DesignSystem/. ControlsView uses it as a drop-in replacement with zero behavioral change. All existing rendering-state transitions, button labels, icons, and animations are preserved exactly.
</success_criteria>

<output>
Create `.planning/phases/17-inspector-bottom-sheet-shell/17-01-SUMMARY.md` when done
</output>
