---
phase: 04-photos-edit-extension-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Watermark.xcodeproj/project.pbxproj
  - PhotoEditExtension/Info.plist
  - PhotoEditExtension/PhotoEditExtension.entitlements
  - PhotoEditExtension/PhotoEditingViewController.swift
  - PhotoEditExtension/PhotosExtensionViewModel.swift
  - PhotoEditExtension/PhotosExtensionRootView.swift
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/PhotosExtensionTests.swift
autonomous: true
requirements: [MEDI-03]

must_haves:
  truths:
    - "Photo Editing Extension target exists in Xcode project alongside the missing ShareExtension target"
    - "User can open a photo in Photos app, tap Edit, and select Watermark from the extensions menu"
    - "Watermark extension shows full watermarking UI with real-time preview when editing a photo"
    - "Tapping Done renders the watermarked photo at full resolution and commits to Photos"
    - "Committing an edit creates an undoable entry in Photos edit history via PHAdjustmentData"
  artifacts:
    - path: "PhotoEditExtension/Info.plist"
      provides: "Extension manifest with NSExtensionPointIdentifier=com.apple.photo-editing"
      min_lines: 25
    - path: "PhotoEditExtension/PhotoEditExtension.entitlements"
      provides: "App Group entitlement for config sync"
      contains: "group.com.watermark.app"
    - path: "PhotoEditExtension/PhotoEditingViewController.swift"
      provides: "PHContentEditingController implementation hosting SwiftUI"
      min_lines: 80
    - path: "PhotoEditExtension/PhotosExtensionViewModel.swift"
      provides: "@Observable ViewModel with WatermarkConfigurable conformance, photo processing, PHAdjustmentData encode/decode"
      min_lines: 300
    - path: "PhotoEditExtension/PhotosExtensionRootView.swift"
      provides: "SwiftUI root view with preview area and ControlsView, Done toolbar button"
      min_lines: 150
    - path: "Watermark.xcodeproj/project.pbxproj"
      provides: "Xcode project with PhotoEditExtension + ShareExtension targets, embed build phase"
  key_links:
    - from: "PhotoEditExtension/PhotoEditingViewController.swift"
      to: "PhotoEditExtension/PhotosExtensionViewModel.swift"
      via: "viewModel property + PHContentEditingController delegation"
      pattern: "viewModel\.(canHandle|startEditing|finishEditing|cancelEditing|hasUnsavedChanges)"
    - from: "PhotoEditExtension/PhotosExtensionRootView.swift"
      to: "Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift"
      via: "ControlsView(viewModel:)"
      pattern: "ControlsView\(viewModel:"
    - from: "PhotoEditExtension/PhotosExtensionViewModel.swift"
      to: "Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift"
      via: "WatermarkEngine.process(sourceURL:config:)"
      pattern: "engine\.process\(sourceURL:"
    - from: "project.pbxproj PhotoEditExtension target"
      to: "project.pbxproj WatermarkApp target"
      via: "PBXCopyFilesBuildPhase (Embed App Extensions)"
---

<objective>
Deliver the complete Photo Editing Extension vertical slice: users can open any photo in the Photos app, tap Edit, select "Watermark" from the extensions menu, configure a text watermark with the same UI as the main app, and tap Done to commit a non-destructive edit with full undo support via PHAdjustmentData. Photo processing reuses the existing WatermarkEngine.process() pipeline with source format preservation.

This plan also fixes the Phase 3 gap: the ShareExtension files exist on disk but have no Xcode target in project.pbxproj. Both extension targets (ShareExtension + PhotoEditExtension) are added to the project in a single pass since they share the same structural pattern (UIViewController + UIHostingController + WatermarkCore linkage).

Purpose: MEDI-03 — "User can receive photos and videos via Photos app native edit extension." This plan delivers the photo path of the vertical slice.
Output: Buildable Xcode project with PhotoEditExtension target, working photo editing extension, automated tests covering pipeline correctness, HDR preservation, and PHAdjustmentData round-trip.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 04 source-of-truth files (design decisions and patterns)
@.planning/phases/04-photos-edit-extension-polish/04-CONTEXT.md
@.planning/phases/04-photos-edit-extension-polish/04-RESEARCH.md
@.planning/phases/04-photos-edit-extension-polish/04-PATTERNS.md

# Pattern reference — exact code to replicate
@ShareExtension/ShareViewController.swift
@ShareExtension/ShareExtensionRootView.swift
@ShareExtension/ShareExtensionViewModel.swift
@ShareExtension/Info.plist
@ShareExtension/ShareExtension.entitlements

# Engine API — called by the new ViewModel
@Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/PipelineError.swift

# Project structure for pbxproj additions
@Watermark.xcodeproj/project.pbxproj
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create failing tests for Photos Editing Extension photo path</name>
  <files>Packages/WatermarkCore/Tests/WatermarkCoreTests/PhotosExtensionTests.swift</files>
  <read_first>
    @Packages/WatermarkCore/Tests/WatermarkCoreTests/WatermarkEngineTests.swift (test patterns: @Suite, @Test, #expect, Issue.record, temp file helpers)
    @Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift (process() signature, mediaType() API)
    @Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift (Codable WatermarkConfiguration struct)
    @Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift (ProcessingResult: url, data, outputUTI)
  </read_first>
  <action>
    Create PhotosExtensionTests.swift in WatermarkCoreTests with Swift Testing framework (@Suite, @Test, #expect, Issue.record).
    Write 7 failing tests using test resource images (create minimal 1×1 JPEG test assets programmatically via CGImageDestination):

    1. `@Test("WatermarkEngine.process() produces valid output for photo via fullSizeImageURL pipeline")` — creates temp JPEG, calls engine.process(sourceURL:config:), asserts ProcessingResult.url is non-nil, asserts output file exists at URL, asserts Data(contentsOf:) succeeds
    2. `@Test("WatermarkEngine.mediaType(for:) returns .photo for JPEG URL")` — writes temp JPEG, calls WatermarkEngine.mediaType(for:), asserts equals .photo
    3. `@Test("PHAdjustmentData JSON encode/decode round-trip for text-only WatermarkConfiguration")` — creates config with .bottomRight text watermark ("Test", fontSize=48, white color), encodes to JSON via JSONEncoder, decodes back, asserts decoded.config.watermarks[0] position is .bottomRight, text is "Test"
    4. `@Test("PHAdjustmentData formatIdentifier and formatVersion match expected constants")` — encodes config, creates PHAdjustmentData(formatIdentifier:"com.watermark.app.adjustment", formatVersion:"1.0", data:encodedData), asserts adjustmentData.formatIdentifier=="com.watermark.app.adjustment", formatVersion=="1.0"
    5. `@Test("HDR gain map auxiliary data survives engine.process() from HEIC source")` — requires test HEIC with gain map in test bundle, calls engine.process(), inspects output via CGImageSource to verify kCGImagePropertyAuxiliaryDataInfoTopLevel contains HDRGainMap entry (skip if no HEIC test asset — log via Issue.record with "SKIP: No HDR HEIC test asset")
    6. `@Test("EXIF metadata fields survive engine.process() round-trip")` — creates temp JPEG with dummy EXIF via CGImageDestinationAddMetadata, calls engine.process(), reads output via CGImageSource, verifies at least 3 EXIF keys present in output metadata
    7. `@Test("PHAdjustmentData decode rejects unknown formatIdentifier")` — creates adjustment data with formatIdentifier="com.other.app", asserts decode returns false/nil

    Use helper pattern from WatermarkEngineTests: createTempInputFile(data:name:) for test asset creation, cleanup(_:) for temp file removal after each test.
    All tests MUST fail (RED phase) because PhotosEditingViewController, PhotosExtensionViewModel, and AdjustmentDataHelper do not exist yet.
  </action>
  <behavior>
    - Test 1: engine.process() on temp JPEG returns ProcessingResult with valid URL and writable output file
    - Test 2: mediaType(for:) correctly identifies JPEG URL as .photo
    - Test 3: WatermarkConfiguration JSON encode→decode round-trip preserves watermark type, text, position, scale
    - Test 4: PHAdjustmentData constructed with correct formatIdentifier "com.watermark.app.adjustment" and formatVersion "1.0"
    - Test 5: engine.process() preserves HDR gain map in HEIC output (skip if no test asset)
    - Test 6: engine.process() preserves EXIF metadata across round-trip (≥3 keys survive)
    - Test 7: PHAdjustmentData with unknown formatIdentifier is rejected
  </behavior>
  <verify>
    <automated>swift test --filter "PhotosExtensionTests" 2>&amp;1 | grep -c "FAIL" | test $(cat) -ge 7</automated>
  </verify>
  <done>
    PhotosExtensionTests.swift exists with 7 @Test functions. Running swift test --filter "PhotosExtensionTests" shows 7 failures (RED) — either because code under test doesn't exist or test assertions fail.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add both extension targets to Xcode project + scaffold Photo Edit Extension files</name>
  <files>
    Watermark.xcodeproj/project.pbxproj
    PhotoEditExtension/Info.plist
    PhotoEditExtension/PhotoEditExtension.entitlements
    PhotoEditExtension/PhotoEditingViewController.swift
    PhotoEditExtension/PhotosExtensionViewModel.swift
    PhotoEditExtension/PhotosExtensionRootView.swift
  </files>
  <read_first>
    @Watermark.xcodeproj/project.pbxproj (full file — understand ID convention: 3-digit hex refs, objectVersion=77, existing target structure WatermarkApp PBXNativeTarget, productRef pattern)
    @ShareExtension/Info.plist (template for NSExtension section structure, CFBundleDisplayName)
    @ShareExtension/ShareExtension.entitlements (copy verbatim: group.com.watermark.app)
    @ShareExtension/ShareViewController.swift (full file — UIViewController + UIHostingController pattern, property declaration pattern line 18-19, hosting controller setup lines 32-45)
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift (full file — protocol requirements the ViewModel must satisfy)
    @Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift (AppGroupConfigSync.save() and .load() API)
    @Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift (process() signature, mediaType() static method)
    @.planning/phases/04-photos-edit-extension-polish/04-PATTERNS.md (Sections: PhotoEditingViewController, PhotosExtensionRootView, PhotosExtensionViewModel, Info.plist, entitlements, project.pbxproj)
  </read_first>
  <action>
    **PART A: project.pbxproj — Add BOTH extension targets**

    Read the full project.pbxproj. Add these targets using the existing ID convention (next available IDs in each series):

    **ShareExtension target** (productType: "com.apple.product-type.app-extension"):
    - Create PBXNativeTarget section with name=ShareExtension, productName=ShareExtension
    - Sources build phase with ShareViewController.swift, ShareExtensionViewModel.swift, ShareExtensionRootView.swift, ShareSheetView.swift
    - Frameworks build phase (empty, system frameworks auto-linked)
    - Resources build phase with Info.plist
    - packageProductDependencies referencing WatermarkCore (reuse existing 30E ref from WatermarkApp target)
    - Debug/Release build configurations: INFOPLIST_FILE=ShareExtension/Info.plist, PRODUCT_BUNDLE_IDENTIFIER=com.watermark.app.share, CODE_SIGN_ENTITLEMENTS=ShareExtension/ShareExtension.entitlements, LD_RUNPATH_SEARCH_PATHS="$(inherited) @executable_path/Frameworks @executable_path/../../Frameworks", SWIFT_VERSION=5.0, TARGETED_DEVICE_FAMILY=1, IPHONEOS_DEPLOYMENT_TARGET=18.0
    - productReference: ShareExtension.appex in Products group (path: ShareExtension.appex, explicitFileType: wrapper.app-extension)

    **PhotoEditExtension target** (productType: "com.apple.product-type.app-extension"):
    - Create PBXNativeTarget section with name=PhotoEditExtension, productName=PhotoEditExtension
    - Sources build phase with PhotoEditingViewController.swift, PhotosExtensionViewModel.swift, PhotosExtensionRootView.swift
    - Frameworks build phase (empty)
    - Resources build phase with Info.plist
    - packageProductDependencies referencing WatermarkCore
    - Build configurations: INFOPLIST_FILE=PhotoEditExtension/Info.plist, PRODUCT_BUNDLE_IDENTIFIER=com.watermark.app.photoedit, CODE_SIGN_ENTITLEMENTS=PhotoEditExtension/PhotoEditExtension.entitlements, LD_RUNPATH_SEARCH_PATHS="$(inherited) @executable_path/Frameworks @executable_path/../../Frameworks", SWIFT_VERSION=5.0, TARGETED_DEVICE_FAMILY=1, IPHONEOS_DEPLOYMENT_TARGET=18.0
    - productReference: PhotoEditExtension.appex in Products group (path: PhotoEditExtension.appex)

    **Embed App Extensions build phase** on WatermarkApp target:
    - Add PBXCopyFilesBuildPhase with dstPath="", dstSubfolderSpec=13 (PlugIns), name="Embed App Extensions"
    - files: both ShareExtension.appex and PhotoEditExtension.appex

    **Project-level additions:**
    - `targets` array: append both target refs
    - `attributes.TargetAttributes`: add entries for both targets with CreatedOnToolsVersion, DevelopmentTeam, ProvisioningStyle=Automatic
    - File groups: create "ShareExtension/" and "PhotoEditExtension/" groups under project group
    - Product references: add to "Products" group
    - Add PBXFileReference entries for all source files, plists, and entitlements
    - Add PBXBuildFile entries for all source files in their respective build phases

    Use exact file paths from the ShareExtension/ directory on disk for existing files. Use the new paths under PhotoEditExtension/ for new files.

    **PART B: Create PhotoEditExtension files**

    **PhotoEditExtension/Info.plist** — copy ShareExtension/Info.plist structure, replace:
    - NSExtensionAttributes: remove NSExtensionActivationRule, add PHSupportedMediaTypes array: ["Image", "Video"]
    - NSExtensionPointIdentifier: "com.apple.photo-editing"
    - NSExtensionPrincipalClass: "$(PRODUCT_MODULE_NAME).PhotoEditingViewController"
    - CFBundleDisplayName: "Watermark" (keep same)

    **PhotoEditExtension/PhotoEditExtension.entitlements** — copy ShareExtension/ShareExtension.entitlements VERBATIM (App Group: group.com.watermark.app)

    **PhotoEditExtension/PhotoEditingViewController.swift** — UIViewController subclass conforming to PHContentEditingController (D-01, D-02):
    - imports: UIKit, SwiftUI, PhotosUI, WatermarkCore
    - Property: `private let viewModel = PhotosExtensionViewModel()`
    - Property: `private var hostingController: UIHostingController<PhotosExtensionRootView>?`
    - canHandle(_:): delegate to viewModel.canHandle(adjustmentData) [D-09]
    - startContentEditing(with:placeholderImage:): calls viewModel.startEditing(with:placeholderImage:), then setupHostingController() [D-02]
    - finishContentEditing(completionHandler:): calls viewModel.finishEditing(completionHandler:) [D-02]
    - cancelContentEditing(): calls viewModel.cancelEditing()
    - shouldShowCancelConfirmation: returns viewModel.hasUnsavedChanges [Pitfall 5]
    - setupHostingController(): EXACT copy of ShareViewController.setupHostingController() pattern (lines 32-45) but with PhotosExtensionRootView and NSLayoutConstraint.activate same 4 constraints

    **PhotoEditExtension/PhotosExtensionViewModel.swift** — @Observable @MainActor ViewModel conforming to WatermarkConfigurable (D-03):
    - imports: CoreImage, Foundation, Observation, PhotosUI, SwiftUI, UIKit, UniformTypeIdentifiers, WatermarkCore
    - Declaration: `@Observable @MainActor final class PhotosExtensionViewModel: WatermarkConfigurable`
    - Properties (subset from ShareExtensionViewModel, minus multi-item/share-specific):
      - config: WatermarkConfiguration { didSet { AppGroupConfigSync.save(config) } }
      - activeLayerIndex: Int = 0
      - renderingState: RenderingState = .idle
      - whiteFrameEnabled: Bool (computed — checks if any layer is white frame)
      - sourceURL: URL?
      - isVideo: Bool = false
      - isLoadingMedia: Bool = true
      - previewImage: UIImage?
      - isGeneratingPreview: Bool = false
      - showLogoPicker: Bool = false
      - showHDRWarning: Bool = false
      - hdrWarningMessage: String?
      - hasUnsavedChanges: Bool = false [Pitfall 5]
      - errorMessage: String?
      - showError: Bool = false
    - Private: `private let engine = WatermarkEngine.shared`, `private var input: PHContentEditingInput?`, `private var finishHandler: ((PHContentEditingOutput?) -> Void)?`

    - init(): load config from AppGroupConfigSync.load() ?? default text watermark config (same default as ShareExtensionViewModel: "Sample", fontSize=48, white, bottomRight, scale=0.15)

    - WatermarkConfigurable protocol methods (copy body from ShareExtensionViewModel):
      - addLogoLayer(pngData:), removeLayer(at:), updateLayerPosition(at:position:), updateLayerScale(at:scale:), toggleWhiteFrame() — copy exact implementation
      - renderAndPrepareShare() — becomes `finishEditing(completionHandler:)` wrapper (will be Task 3)
      - presentShareSheet() — no-op (empty body, Photos extension has no share sheet)

    - NEW methods for PHContentEditingController lifecycle:
      - `canHandle(_ adjustmentData: PHAdjustmentData) -> Bool`: returns adjustmentData.formatIdentifier=="com.watermark.app.adjustment" && formatVersion=="1.0" [D-09]
      - `startEditing(with input: PHContentEditingInput, placeholderImage: UIImage)`: sets self.input=input, sets previewImage=placeholderImage, reads sourceURL from input.fullSizeImageURL [D-06], sets isVideo=false, sets isLoadingMedia=false, if input.adjustmentData exists and canHandle→decode config→set self.config [D-05], fires Task { await generatePreview() }
      - `cancelEditing()`: calls finishHandler?(nil), resets state
      - `finishEditing(completionHandler:)`: stores finishHandler, fires Task { await renderAndCommit() } — renderAndCommit stub for now (returns nil — full implementation in Task 3)

    - Preview generation: copy generatePreview() pattern from ShareExtensionViewModel lines 415-438 (debounce 350ms, engine.process(), UIImage(data:) from result)

    - previewIdentifier computed property: copy from ShareExtensionViewModel (hash of config + sourceURL for .task(id:) trigger)

    **PhotoEditExtension/PhotosExtensionRootView.swift** — SwiftUI root view (D-02):
    - imports: SwiftUI, WatermarkCore
    - `@State var viewModel: PhotosExtensionViewModel`
    - Layout: same 60/40 split as ShareExtensionRootView (GeometryReader → VStack → previewArea 60% + separator + controlsArea 40%)
    - previewArea: copy pattern from ShareExtensionRootView lines 96-141 with states: isLoadingMedia (ProgressView+"Loading from Photos…"), previewImage (Image(uiImage:) scaledToFit), errorMessage (exclamationmark.triangle + text), else (photo system image + "Preparing photo…")
    - controlsArea: `ControlsView(viewModel: viewModel)` — identical, generic over WatermarkConfigurable [D-03]
    - Toolbar: add `.toolbar { ToolbarItem(placement: .confirmationAction) { Button("Done") { viewModel.finishEditing(completionHandler: { _ in }) } } }` — Done button [D-02]
    - Refresh: `.task(id: viewModel.previewIdentifier) { guard viewModel.sourceURL != nil else { return }; await viewModel.generatePreview() }`
    - Error alert: `.alert("Error", isPresented: $viewModel.showError) { Button("OK") {} } message: { Text(viewModel.errorMessage ?? "") }`
    - HDR warning: show banner when viewModel.showHDRWarning && renderingState==.done (copy hdrWarningBanner from ShareExtensionRootView)
    - Video indicator overlay: "▶ Video" badge on preview when viewModel.isVideo (copy from ShareExtensionRootView)
    - NO share sheet (.sheet) — removed [D-02 no share step]
    - NO multi-item progress bar — single-item only
    - NO unsupported type alert — extension only opens supported types via Photos
  </action>
  <verify>
    <automated>
      # Verify Xcode project builds (check that xcodebuild can parse the project):
      xcodebuild -project Watermark.xcodeproj -list 2>&amp;1 | grep -c "PhotoEditExtension"
      xcodebuild -project Watermark.xcodeproj -list 2>&amp;1 | grep -c "ShareExtension"
      # Verify new files exist on disk:
      test -f "PhotoEditExtension/PhotoEditingViewController.swift" && echo "VC exists"
      test -f "PhotoEditExtension/PhotosExtensionViewModel.swift" && echo "VM exists"
      test -f "PhotoEditExtension/PhotosExtensionRootView.swift" && echo "RootView exists"
      test -f "PhotoEditExtension/Info.plist" && echo "Info.plist exists"
      test -f "PhotoEditExtension/PhotoEditExtension.entitlements" && echo "Entitlements exist"
      # Verify Info.plist contains correct extension point:
      grep -c "com.apple.photo-editing" PhotoEditExtension/Info.plist
      # Verify entitlements have App Group:
      grep -c "group.com.watermark.app" PhotoEditExtension/PhotoEditExtension.entitlements
      # Verify NSExtensionPrincipalClass in Info.plist:
      grep -c "PhotoEditingViewController" PhotoEditExtension/Info.plist
    </automated>
  </verify>
  <done>
    Xcode project lists both PhotoEditExtension and ShareExtension targets. All 5 PhotoEditExtension files exist on disk with correct content. Info.plist has NSExtensionPointIdentifier=com.apple.photo-editing and PHSupportedMediaTypes. Entitlements declare group.com.watermark.app. PhotoEditingViewController conforms to PHContentEditingController and hosts SwiftUI via UIHostingController. PhotosExtensionViewModel conforms to WatermarkConfigurable with photo-only processing path. PhotosExtensionRootView has 60/40 split layout with ControlsView and Done toolbar button.
  </done>
</task>

<task type="auto">
  <name>Task 3: Implement render-and-commit pipeline + PHAdjustmentData + format preservation</name>
  <files>
    PhotoEditExtension/PhotosExtensionViewModel.swift
    PhotoEditExtension/PhotosExtensionRootView.swift
  </files>
  <read_first>
    @PhotoEditExtension/PhotosExtensionViewModel.swift (current state from Task 2 — has renderAndCommit stub, finishEditing wrapper, config save pattern)
    @PhotoEditExtension/PhotosExtensionRootView.swift (current state from Task 2 — Done button calls finishEditing, needs Done hookup to commit flow)
    @Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift (process() returns ProcessingResult with url, data, outputUTI)
    @Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift (ProcessingResult fields: url, data, outputUTI, videoValidation)
    @Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift (Codable — used for PHAdjustmentData JSON)
    @Packages/WatermarkCore/Sources/WatermarkCore/Input/FormatDetector.swift (source format detection API — used for D-07 format preservation)
    @.planning/phases/04-photos-edit-extension-polish/04-RESEARCH.md (Pitfall 3: renderedContentURL format, Pattern 2: PHAdjustmentData serialization)
    @.planning/phases/04-photos-edit-extension-polish/04-PATTERNS.md (Section: PHAdjustmentData encode/decode pattern, AdjustmentConstants, finish editing pattern)
  </read_first>
  <action>
    Implement the full render-and-commit pipeline in PhotosExtensionViewModel. This connects the Done button → engine rendering → PHContentEditingOutput → Photos framework commit with PHAdjustmentData for undo.

    **PART 1: renderAndCommit() async implementation** (replaces stub from Task 2)

    In PhotosExtensionViewModel, implement the private renderAndCommit() method:
    - Guard: input and sourceURL must be non-nil; if nil, call finishHandler?(nil), return
    - Set renderingState = .rendering
    - Try block:
      a. Determine media type via WatermarkEngine.mediaType(for: sourceURL)
      b. If isVideo → skip for now (will be Plan 04-02); call finishHandler?(nil), set renderingState = .idle, return
      c. For photos: call `let result = try await engine.process(sourceURL: sourceURL, config: config)` [D-06]
      d. Create PHContentEditingOutput: `let output = PHContentEditingOutput(contentEditingInput: input)`
      e. Copy rendered data to renderedContentURL: if let renderedURL = result.url, `let renderedData = try Data(contentsOf: renderedURL)`, `try renderedData.write(to: output.renderedContentURL, options: .atomic)`, cleanup temp: `try? TempFileManager.cleanup(url: renderedURL)`
      f. Attach PHAdjustmentData [D-04]: create PHAdjustmentData(formatIdentifier:"com.watermark.app.adjustment", formatVersion:"1.0", data: encodedConfig) where encodedConfig = try JSONEncoder().encode(config) (text-only configs are < 5 KB — safe per Pitfall 1)
      g. Set output.adjustmentData = adjustmentData
      h. Set renderingState = .done, hasUnsavedChanges = false
      i. Call finishHandler?(output)
    - Catch block: set renderingState = .error(error), errorMessage = error.localizedDescription, showError = true, finishHandler?(nil)

    **PART 2: Source format preservation (D-07)**

    Before calling engine.process(), detect source format:
    - Use FormatDetector to determine source format (HEIC/JPEG/PNG) from sourceURL
    - Set config.outputFormat = .preserveSource (which is the default, but explicitly set it)
    - Note: engine.process() already preserves source format when outputFormat is .preserveSource. No additional work needed — the engine handles this. The resulting ProcessingResult.outputUTI will match the source format.
    - Add a comment explaining: renderedContentURL receives the engine's output directly. Per Research Pitfall 3, JPEG is the safest format for renderedContentURL compatibility, but the engine's format detection handles HEIC→HEIC correctly if the Photos framework on iOS 18 accepts it. Non-destructive editing (PHAdjustmentData + undo) means the original format is always recoverable via "Revert to Original".

    **PART 3: Done button hookup in PhotosExtensionRootView**

    Revisit the Done button from Task 2. It currently calls `viewModel.finishEditing(completionHandler: { _ in })` which does nothing with the handler. Fix this:
    - Create a `doneTapped` action that shows rendering progress
    - During rendering (renderingState == .rendering): replace the Done button with ProgressView
    - The finishEditing closure passed to viewModel is handled internally (Photos framework dismisses the extension after finishContentEditing callback)
    - Add: `.disabled(viewModel.renderingState == .rendering)` on the Done button to prevent double-tap [Pitfall 4 in RESEARCH.md]

    **PART 4: shouldShowCancelConfirmation guard**

    Set hasUnsavedChanges = true in the config didSet (already exists from Task 2 pattern). The didSet already triggers AppGroupConfigSync.save() — add the hasUnsavedChanges flag set:
    ```swift
    var config: WatermarkConfiguration {
        didSet {
            AppGroupConfigSync.save(config)
            hasUnsavedChanges = true
        }
    }
    ```
    In cancelContentEditing in the ViewController → ViewModel.cancelEditing(), reset hasUnsavedChanges = false.

    **PART 5: Preview regeneration after placeholderImage**

    In startEditing(), after setting isLoadingMedia=false and generating the full-res preview:
    - The placeholderImage is displayed immediately (from startContentEditing parameter)
    - generatePreview() runs in background Task → replaces placeholderImage with watermarked preview
    - Ensure previewIdentifier changes trigger .task(id:) refresh in RootView (already wired in Task 2)
  </action>
  <verify>
    <automated>
      # Verify renderAndCommit exists with full implementation:
      grep -c "engine.process(sourceURL:" PhotoEditExtension/PhotosExtensionViewModel.swift
      grep -c "PHContentEditingOutput" PhotoEditExtension/PhotosExtensionViewModel.swift
      grep -c "PHAdjustmentData" PhotoEditExtension/PhotosExtensionViewModel.swift
      # Verify format preservation mention:
      grep -c "preserveSource" PhotoEditExtension/PhotosExtensionViewModel.swift
      # Verify Done button disabled during rendering:
      grep -c "\.rendering" PhotoEditExtension/PhotosExtensionRootView.swift
      # Verify hasUnsavedChanges flag:
      grep -c "hasUnsavedChanges = true" PhotoEditExtension/PhotosExtensionViewModel.swift
      # Verify no share sheet in root view (D-02):
      test $(grep -c "\.sheet" PhotoEditExtension/PhotosExtensionRootView.swift) -eq 0 && echo "No share sheet — correct"
      # Tests from Task 1 should now compile (but may not pass without running engine):
      swift test --filter "PhotosExtensionTests" 2>&amp;1 | head -5
    </automated>
    <human-check>
      Review the PhotosExtensionViewModel.renderAndCommit() method:
      1. Confirm it creates PHContentEditingOutput correctly
      2. Confirm it attaches PHAdjustmentData with formatIdentifier "com.watermark.app.adjustment" and formatVersion "1.0"
      3. Confirm temp files are cleaned up after writing to renderedContentURL
      4. Confirm error path calls finishHandler?(nil)
    </human-check>
  </verify>
  <done>
    PhotosExtensionViewModel.renderAndCommit() is fully implemented: loads photo from sourceURL, calls engine.process(), writes result to PHContentEditingOutput.renderedContentURL, attaches PHAdjustmentData with config JSON, calls finishHandler with output. Done button in PhotosExtensionRootView calls finishEditing, shows progress during rendering, and is disabled during active render. Cancel confirmation shows when hasUnsavedChanges is true. Source format preservation is handled by the engine's .preserveSource output format.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PHContentEditingInput → ViewModel | System-provided media data crosses into app code; validate nil for fullSizeImageURL and audiovisualAsset |
| PHAdjustmentData.decode → config | Serialized config from Photos library (possible iCloud sync); validate formatIdentifier + formatVersion before decoding |
| App Group UserDefaults → config load | Shared storage between app and extensions; validate config on decode (scale range 0.01-0.90, etc.) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | PHAdjustmentData decode | mitigate | Validate formatIdentifier=="com.watermark.app.adjustment" and formatVersion=="1.0" before JSON decode; return false from canHandle() on mismatch |
| T-04-02 | Tampering | WatermarkConfiguration decode from JSON | mitigate | Codable decoding validates WatermarkLayer enum cases; scale validation (0.01-0.90) enforced in ImageWatermarkInput.init; invalid configs fall back to defaults |
| T-04-03 | Denial of Service | Large source asset processing | accept | Extension sandbox memory ceiling (~120 MB) limits attack surface; engine uses streaming pipelines (lazy CIImage filter graphs, single CGImage output); guard sourceURL validity |
| T-04-04 | Information Disclosure | App Group UserDefaults cross-read | accept | App Group group.com.watermark.app is shared only between our own targets (main app + share extension + photo extension); no third-party access |
| T-04-05 | Tampering | renderedContentURL path manipulation | accept | URL is system-provided by PHContentEditingOutput(contentEditingInput:); Data.write(to:) fails for paths outside sandbox |
| T-04-SC | Tampering | npm/pip/cargo installs | accept | No third-party packages — only Apple system frameworks and local WatermarkCore Swift Package; no external supply chain risk |
</threat_model>

<verification>
**Automated verification:**
- Swift Testing: `swift test --filter "PhotosExtensionTests"` — all tests pass after Task 3
- Structure: `grep` assertions on key symbols (PHContentEditingOutput, PHAdjustmentData, engine.process, etc.)
- Xcode: `xcodebuild -project Watermark.xcodeproj -list` shows both extension targets

**Human verification:**
- Review renderAndCommit() for correct PHContentEditingOutput + PHAdjustmentData construction
- Review Done button disabled state during rendering (double-tap protection)
</verification>

<success_criteria>
After this plan completes:
1. Xcode project has both PhotoEditExtension and ShareExtension targets buildable and embedded in WatermarkApp
2. PhotoEditingViewController implements PHContentEditingController, hosts SwiftUI via UIHostingController
3. PhotosExtensionViewModel processes photos via WatermarkEngine.process(), creates PHContentEditingOutput with PHAdjustmentData
4. PhotosExtensionRootView presents ControlsView with Done toolbar button, 60/40 split layout
5. Text watermark configuration committed via Done button creates undoable Photos edit entry
6. Automated tests in PhotosExtensionTests.swift pass for photo pipeline correctness and PHAdjustmentData round-trip
7. Cancel confirmation dialog appears when user has made config changes
</success_criteria>

<output>
Create `.planning/phases/04-photos-edit-extension-polish/04-01-SUMMARY.md` when done
</output>
