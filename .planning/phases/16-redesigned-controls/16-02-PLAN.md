---
phase: 16-redesigned-controls
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/TextWatermarkInputView.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/ScaleStepperView.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/WhiteFrameToggleView.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift
autonomous: true
requirements:
  - CTL-02
  - CTL-03
  - CTL-04
  - CTL-05
  - CTL-06

must_haves:
  truths:
    - "Text watermark input renders as a clean inset grouped row with glass backing and semantic typography"
    - "Scale control shows a live percentage value readout using .markepiTypography(.value) with monospaced digits"
    - "Logo picker Add button uses .markepiPrimary() icon+text; Remove button uses .markepiDestructive() text-only"
    - "Signature picker Add/Edit buttons use .markepiPrimary() icon+text; Remove button uses .markepiDestructive() text-only"
    - "White Frame toggle is an integrated grouped row with .markepiTypography(.controlLabel) title and .metadata subtitle"
    - "Layer list shows active-layer highlight via accentColor.opacity(0.08) background and icon-only .markepiDestructive() remove button"
    - "All sub-views preserve existing accessibility labels"
    - "Zero raw .font() calls in any of the 6 refactored files"
    - "Zero .buttonStyle(.bordered), .buttonStyle(.borderedProminent), .foregroundStyle(.red), .foregroundStyle(.blue), .tint() calls on buttons in these files"
  artifacts:
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/TextWatermarkInputView.swift"
      provides: "Restyled text input with glass-backed container, .markepiTypography(.sectionHeader) header, .controlLabel row label, .metadata placeholder"
      contains: ".markepiGlass"
      contains: ".markepiTypography(.sectionHeader)"
      contains: ".markepiTypography(.controlLabel)"
      contains: "@Environment(\.accessibilityReduceTransparency)"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/ScaleStepperView.swift"
      provides: "Restyled scale row with .markepiTypography(.value) live readout"
      contains: ".markepiTypography(.controlLabel)"
      contains: ".markepiTypography(.value)"
      contains: "Int(currentScale * 100)"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift"
      provides: "MarkepiButtonStyle add/remove logo buttons"
      contains: ".markepiPrimary()"
      contains: ".markepiDestructive()"
      contains: ".markepiTypography(.controlLabel)"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift"
      provides: "MarkepiButtonStyle add/edit/remove signature buttons"
      contains: ".markepiPrimary()"
      contains: ".markepiDestructive()"
      contains: ".markepiTypography(.controlLabel)"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/WhiteFrameToggleView.swift"
      provides: "Restyled white frame toggle row with typography"
      contains: ".markepiTypography(.controlLabel)"
      contains: ".markepiTypography(.metadata)"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift"
      provides: "Restyled layer list with glass container, active-layer highlight, icon-only remove"
      contains: ".markepiGlass"
      contains: "accentColor.opacity(0.08)"
      contains: ".markepiDestructive()"
      contains: ".markepiTypography(.sectionHeader)"
      contains: ".markepiTypography(.controlLabel)"
      contains: ".markepiTypography(.metadata)"
  key_links:
    - from: "TextWatermarkInputView.body"
      to: "viewModel.config.watermarks (via textBinding)"
      via: "@Bindable var viewModel — unchanged binding pattern"
      pattern: "viewModel\\.config\\.watermarks"
    - from: "ScaleStepperView scaleBinding"
      to: "viewModel.updateLayerScale(at:scale:)"
      via: "Binding setter — unchanged"
      pattern: "updateLayerScale\\(at:"
    - from: "LogoPickerView addLogoButton"
      to: "viewModel.addLogoLayer(pngData:)"
      via: "PhotosPicker .onChange / fileImporter — unchanged flow"
      pattern: "addLogoLayer\\(pngData:"
    - from: "SignatureCaptureView addSignatureButton"
      to: "viewModel.addSignatureLayer(strokeData:inkColor:strokeWidth:)"
      via: "PencilKit canvas → Save button — unchanged flow"
      pattern: "addSignatureLayer\\(strokeData:"
    - from: "WhiteFrameToggleView Toggle"
      to: "viewModel.setWhiteFrameEnabled(_:)"
      via: "Binding setter — unchanged"
      pattern: "setWhiteFrameEnabled\\(\\$0\\)"
    - from: "LayerListView layerRow Button"
      to: "viewModel.removeLayer(at:)"
      via: "xmark.circle.fill Button action — unchanged"
      pattern: "removeLayer\\(at:"
---

<objective>
Restyles all 6 sub-view files consumed by ControlsView — TextWatermarkInputView (CTL-02), ScaleStepperView (CTL-03), LogoPickerView (CTL-04), SignatureCaptureView (CTL-04), WhiteFrameToggleView (CTL-05), and LayerListView (CTL-06) — onto the Phase 15 Markepi design system. Each sub-view is refactored independently into the inset grouped row pattern with glass backing (where it creates its own container), MarkepiTypography semantic font styles, and MarkepiButtonStyle capsules on all buttons. Zero behavior changes — all @Bindable viewModel bindings, protocol method calls, and data flow remain untouched.

Purpose: Every control inside ControlsView's three pill-bar sections renders with the new visual language — clean, grouped, professional. These are the "leaf" views that Plan 01's ControlsView body arranges into sections.

Output: 6 refactored Swift files, each ~50–150 lines, all building cleanly across all 3 targets.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
<!-- Phase 16 design decisions and patterns -->
@.planning/phases/16-redesigned-controls/16-CONTEXT.md
@.planning/phases/16-redesigned-controls/16-PATTERNS.md (lines 155-553 for sub-view refactor patterns)
@.planning/phases/16-redesigned-controls/16-RESEARCH.md (lines 289-341 for Pattern 2: Inset Grouped Row; lines 458-520 for Pitfalls)

<!-- Current source files being refactored -->
@Packages/WatermarkCore/Sources/WatermarkCore/UI/TextWatermarkInputView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/ScaleStepperView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/WhiteFrameToggleView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift

<!-- Design system primitives consumed -->
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ButtonStyles/MarkepiButtonStyle.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/Typography/MarkepiTypography.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/GlassEffect/MarkepiGlassModifier.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restyle TextWatermarkInputView, ScaleStepperView, and WhiteFrameToggleView</name>
  <files>
    Packages/WatermarkCore/Sources/WatermarkCore/UI/TextWatermarkInputView.swift
    Packages/WatermarkCore/Sources/WatermarkCore/UI/ScaleStepperView.swift
    Packages/WatermarkCore/Sources/WatermarkCore/UI/WhiteFrameToggleView.swift
  </files>

  <read_first>
    <!-- Current implementations to understand binding patterns and what stays -->
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/TextWatermarkInputView.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/ScaleStepperView.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/WhiteFrameToggleView.swift
    <!-- Exact before/after patterns for each file -->
    @.planning/phases/16-redesigned-controls/16-PATTERNS.md (lines 155-312 for TextWatermarkInputView + ScaleStepperView; lines 495-553 for WhiteFrameToggleView)
    <!-- Pitfall 4: VoiceOver label preservation -->
    @.planning/phases/16-redesigned-controls/16-RESEARCH.md (lines 494-506)
  </read_first>

  <action>
    **0. ZERO BEHAVIOR CHANGES.** All @State properties, @Bindable var viewModel, Binding get/set closures, ViewModel method calls, init, imports, conditional compilation (#if canImport(UIKit)), struct signatures, and generic constraints remain exactly as-is. Only visual styling changes.

    ---

    **1. TextWatermarkInputView (CTL-02) — inset grouped row with glass backing**

    Current body (lines 36-63): VStack with .font(.title3.weight(.semibold)) header + TextEditor in .overlay(RoundedRectangle.stroke) border.

    New body structure:
    - Replace the outer VStack with: VStack(spacing: 0) containing a section header + glass-backed row container
    - Section header: `Text("Text").markepiTypography(.sectionHeader).padding(.horizontal, 16).padding(.bottom, 8)`
    - Row container: VStack(spacing: 0) wrapping the text input content, with `.markepiGlass(shape: RoundedRectangle(cornerRadius: 12, style: .continuous), isEnabled: !reduceTransparency)` and `.clipShape(RoundedRectangle(cornerRadius: 12, style: .continuous)).padding(.horizontal, 16)`
    - Inside the row container: VStack(alignment: .leading, spacing: 4) with:
      - Label row: `Text("Watermark Text").markepiTypography(.controlLabel)` — was `.font(.title3.weight(.semibold))`
      - ZStack overlay pattern for placeholder+TextEditor — preserve existing ZStack logic (placeholder when currentText.isEmpty, TextEditor with `.scrollContentBackground(.hidden)`)
    - Remove the `.overlay(RoundedRectangle.stroke(separatorColor))` — the glass backing replaces the border
    - Row internal padding: `.padding(.horizontal, 16).padding(.vertical, 12)`
    - Preserve existing: placeholderColor, textBinding, .onChange truncation at 500 chars, the ZStack with topLeading alignment, .frame(minHeight: 80, maxHeight: 120)

    Add to struct: `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency`
    Remove: `separatorColor` computed property (no longer used — glass replaces border).

    **D-08 typography mapping:**
    | Old | New |
    |-----|-----|
    | `.font(.title3.weight(.semibold))` on "Watermark Text" | `.markepiTypography(.sectionHeader)` on "Text" |
    | (no control label before) | `.markepiTypography(.controlLabel)` on "Watermark Text" label |
    | `.font(.body)` on TextEditor | keep — TextEditor needs raw font; it's not a semantic label |
    | `.foregroundColor(placeholderColor)` on placeholder | keep — placeholder uses semantic UIKit color, not typography |

    **Accessibility:** Current TextWatermarkInputView has no explicit .accessibilityLabel — implicit from TextEditor. This is fine; VoiceOver reads the editor content naturally. No change needed.

    ---

    **2. ScaleStepperView (CTL-03) — live value readout row**

    Current body (lines 20-43): VStack with .font(.title3.weight(.semibold)) header + HStack with .font(.body).foregroundStyle(.secondary) label + Stepper.

    New body: Single HStack row — no outer VStack, no section header. This view is just a row that goes inside a ControlSection in ControlsView's watermark section.

    Replace body with:
    ```
    HStack {
        Text("Scale").markepiTypography(.controlLabel)
        Spacer()
        Text("\(Int(currentScale * 100))%").markepiTypography(.value)
            // .value provides monospacedDigit + secondary foreground automatically
        Stepper("", value: scaleBinding, in: 0.01...0.90, step: 0.05)
            .labelsHidden()
            .frame(width: 100)
    }
    .padding(.horizontal, 16)
    .padding(.vertical, 12)
    ```

    **D-08 typography mapping:**
    | Old | New |
    |-----|-----|
    | `.font(.title3.weight(.semibold))` on "Scale" | `.markepiTypography(.controlLabel)` on "Scale" |
    | `.font(.body).foregroundStyle(.secondary)` on percentage | `.markepiTypography(.value)` on "\(Int(currentScale * 100))%" |
    | `.monospacedDigit()` (wasn't on scale, but should be) | `.markepiTypography(.value)` auto-applies monospacedDigit |

    Remove: The outer VStack wrapper, the old "Scale" header text line (replaced by .controlLabel in HStack).

    **Accessibility (Pitfall 4):** Preserve `.accessibilityLabel("Watermark scale")` and `.accessibilityHint("Adjust watermark size. Current value: \(Int(currentScale * 100)) percent")` on the HStack.

    Preserve: scaleBinding (lines 50-55), currentScale (lines 45-48), layerIndex (lines 14-18) — all unchanged.

    ---

    **3. WhiteFrameToggleView (CTL-05) — integrated grouped row**

    Current body (lines 14-33): VStack with .font(.title3.weight(.semibold)) Toggle label + .font(.caption).foregroundStyle(.secondary) subtitle.

    New body: Single Toggle row — no section header, no outer VStack. This is a row inside ControlsView's style section.

    Replace body with:
    ```
    let isEnabled = viewModel.whiteFrameEnabled
    return VStack(spacing: 0) {
        Toggle(isOn: Binding(
            get: { isEnabled },
            set: { viewModel.setWhiteFrameEnabled($0) }
        )) {
            VStack(alignment: .leading, spacing: 2) {
                Text("White Frame").markepiTypography(.controlLabel)
                Text("Adds a white border with device name").markepiTypography(.metadata)
            }
        }
    }
    .padding(.horizontal, 16)
    .padding(.vertical, 10)
    ```

    **D-08 typography mapping:**
    | Old | New |
    |-----|-----|
    | `.font(.title3.weight(.semibold))` on "White Frame" | `.markepiTypography(.controlLabel)` |
    | `.font(.caption).foregroundStyle(.secondary)` on subtitle | `.markepiTypography(.metadata)` — metadata auto-applies secondary foreground |

    Remove: The `return` keyword and explicit `VStack(alignment: .leading, spacing: 8)` — replaced by the compact toggle row.

    **Accessibility (Pitfall 4):** Preserve `.accessibilityLabel("White frame")` and `.accessibilityHint("Add a white border with device model text to your photo")` on the VStack.

    Preserve: The `let isEnabled = viewModel.whiteFrameEnabled` read in body, the Binding to `setWhiteFrameEnabled(_:)`.

    ---

    **BUILD CHECK AFTER TASK:** Run `bash scripts/build-gate.sh` to verify all 3 files compile.
  </action>

  <acceptance_criteria>
    <!-- TextWatermarkInputView -->
    - TextWatermarkInputView.swift contains `.markepiGlass(shape: RoundedRectangle(cornerRadius: 12, style: .continuous)`
    - TextWatermarkInputView.swift contains `.markepiTypography(.sectionHeader)` (1 occurrence — on "Text" header)
    - TextWatermarkInputView.swift contains `.markepiTypography(.controlLabel)` (on "Watermark Text" label)
    - TextWatermarkInputView.swift contains `@Environment(\.accessibilityReduceTransparency)`
    - TextWatermarkInputView.swift does NOT contain `.overlay(RoundedRectangle` (border removed, replaced by glass)
    - TextWatermarkInputView.swift does NOT contain `.font(.title3.weight(.semibold))`
    - `textBinding` computed property unchanged (lines 73-94 original)

    <!-- ScaleStepperView -->
    - ScaleStepperView.swift contains `.markepiTypography(.controlLabel)` (on "Scale" label)
    - ScaleStepperView.swift contains `.markepiTypography(.value)` (on percentage readout)
    - ScaleStepperView.swift does NOT contain `.font(.title3.weight(.semibold))`
    - ScaleStepperView.swift does NOT contain `.foregroundStyle(.secondary)` (value typography handles this)
    - `.accessibilityLabel("Watermark scale")` preserved
    - `scaleBinding` and `currentScale` unchanged

    <!-- WhiteFrameToggleView -->
    - WhiteFrameToggleView.swift contains `.markepiTypography(.controlLabel)` (on "White Frame")
    - WhiteFrameToggleView.swift contains `.markepiTypography(.metadata)` (on subtitle)
    - WhiteFrameToggleView.swift does NOT contain `.font(.title3.weight(.semibold))`
    - WhiteFrameToggleView.swift does NOT contain `.font(.caption)` or `.foregroundStyle(.secondary)`
    - `.accessibilityLabel("White frame")` preserved
    - Toggle Binding to `setWhiteFrameEnabled` unchanged
  </acceptance_criteria>

  <verify>
    <automated>bash scripts/build-gate.sh</automated>
  </verify>

  <done>
    Text watermark input renders as a glass-backed inset grouped row with clean typography. Scale control shows a live monospaced-digit percentage readout. White frame toggle is a compact row with semantic label+metadata. All bindings and protocol calls unchanged. Build passes all 3 targets.
  </done>
</task>

<task type="auto">
  <name>Task 2: Restyle LogoPickerView, SignatureCaptureView, and LayerListView</name>
  <files>
    Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift
    Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift
    Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift
  </files>

  <read_first>
    <!-- Current implementations -->
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift
    <!-- Exact before/after patterns for each file -->
    @.planning/phases/16-redesigned-controls/16-PATTERNS.md (lines 326-489 for LogoPickerView + SignatureCaptureView; lines 555-670 for LayerListView)
    <!-- Button role mapping (D-10) and label conventions (D-11) -->
    @.planning/phases/16-redesigned-controls/16-CONTEXT.md (lines 36-44 for D-09/D-10/D-11)
    <!-- Pitfall 3: label content mismatch; Pitfall 4: VoiceOver loss -->
    @.planning/phases/16-redesigned-controls/16-RESEARCH.md (lines 479-506)
  </read_first>

  <action>
    **0. ZERO BEHAVIOR CHANGES.** All @State properties, @Bindable var viewModel, PhotosPicker/fileImporter flows, PencilKit canvas, confirmationDialog, ViewModel method calls, init, imports, conditional compilation (#if canImport(UIKit)), struct signatures, and generic constraints remain exactly as-is. Only visual styling changes.

    ---

    **1. LogoPickerView (CTL-04) — MarkepiButtonStyle add/remove**

    Current: `.buttonStyle(.bordered)` on addLogoButton, `.foregroundStyle(.red)` on remove button in logoSelectedView.

    **addLogoButton (lines 84-96):**
    - Replace label `HStack { Image(systemName: "photo.badge.plus"); Text("Add Logo") }` with `Label("Add Logo", systemImage: "photo.badge.plus")` — standard SwiftUI Label for icon+text
    - Replace `.buttonStyle(.bordered)` with `.buttonStyle(.markepiPrimary())` — D-10: Add Logo = primary role
    - D-11: primary = icon+text ✓ (Label provides this)
    - Preserve `.accessibilityLabel("Add logo watermark")` and `.accessibilityHint(...)`

    **logoSelectedView (lines 98-119):**
    - Replace the `Image(systemName: "photo").foregroundStyle(.secondary)` — keep the icon, but add `.frame(width: 24)` for consistent icon sizing
    - Replace `Text("Logo").font(.body).foregroundStyle(.primary)` with `Text("Logo").markepiTypography(.controlLabel)`
    - Replace remove Button: was `Text("Remove").font(.body).foregroundStyle(.red)` → `Text("Remove").font(.body)` with `.buttonStyle(.markepiDestructive())` — D-10: Remove = destructive, D-11: destructive = text-only
    - Remove `.foregroundStyle(.red)` — MarkepiDestructive applies .red foreground automatically
    - Row internal padding: `.padding(.horizontal, 16).padding(.vertical, 10)`

    **D-08 typography mapping:**
    | Old | New |
    |-----|-----|
    | `.font(.title3.weight(.semibold))` on "Logo Watermark" section header | `.markepiTypography(.sectionHeader)` on "Logo" |
    | `.font(.body).foregroundStyle(.primary)` on "Logo" in selected state | `.markepiTypography(.controlLabel)` |

    **Struct-level:** The outer VStack(alignment: .leading, spacing: 8) becomes VStack(spacing: 0) with a section header `Text("Logo").markepiTypography(.sectionHeader).padding(.horizontal, 16).padding(.bottom, 8)` followed by a ControlSection-like container for the add/selected view. The glass container for this file is just a VStack with `.markepiGlass(shape: RoundedRectangle(...), isEnabled: !reduceTransparency)` since LogoPickerView renders its own section.

    Add to struct: `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency`

    ---

    **2. SignatureCaptureView (CTL-04) — MarkepiButtonStyle add/edit/remove**

    Current: `.buttonStyle(.bordered)` on addSignatureButton, `.foregroundStyle(.blue)` on Edit, `.foregroundStyle(.red)` on Remove.

    **addSignatureButton (lines 64-76):**
    - Replace label `HStack { Image(systemName: "signature"); Text("Add Signature") }` with `Label("Add Signature", systemImage: "signature")`
    - Replace `.buttonStyle(.bordered)` with `.buttonStyle(.markepiPrimary())` — D-10: Add Signature = primary role
    - Preserve `.accessibilityLabel("Add signature watermark")` and `.accessibilityHint(...)`

    **signatureSelectedView (lines 78-109):**
    - Icon: `Image(systemName: "signature").foregroundStyle(.secondary).frame(width: 24)`
    - Label: `Text("Signature").markepiTypography(.controlLabel)`
    - Edit button: was `Text("Edit").font(.body).foregroundStyle(.blue)` → `Label("Edit", systemImage: "pencil")` with `.buttonStyle(.markepiPrimary())` and `.labelStyle(.iconAndText)` — D-10: Edit Signature = primary, D-11: primary = icon+text
    - Remove button: was `Text("Remove").font(.body).foregroundStyle(.red)` → `Text("Remove")` with `.buttonStyle(.markepiDestructive())` — D-10: Remove = destructive, D-11: destructive = text-only
    - Row internal padding: `.padding(.horizontal, 16).padding(.vertical, 10)`

    **Struct-level:** Same treatment as LogoPickerView — VStack(spacing: 0) with `Text("Signature").markepiTypography(.sectionHeader)...` header + glass-backed container.

    Add to struct: `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency`

    **IMPORTANT — #else branch (non-UIKit stub, lines 234-278):** Also restyle the stub's addSignatureButton with `.buttonStyle(.markepiPrimary())` (was `.bordered`) to prevent compilation errors on the non-UIKit path. Keep the `.disabled(true)`.

    **Signature capture sheet (lines 113-186):** The PencilKit canvas and toolbar are NOT restyled in this phase — they inherit system styling. The toolbar buttons ("Cancel", "Save", "Clear", etc.) are part of the sheet's NavigationStack toolbar and use built-in styles — no MarkepiButtonStyle changes needed for intra-sheet buttons.

    ---

    **3. LayerListView (CTL-06) — active-layer highlight + icon-only remove**

    Current: VStack with .font(.title3) header + VStack with .background(.regularMaterial) list.

    **Struct-level body (lines 14-35):**
    New structure:
    ```
    if viewModel.config.watermarks.isEmpty { EmptyView() }
    else {
        VStack(spacing: 0) {
            Text("Layers").markepiTypography(.sectionHeader)
                .padding(.horizontal, 16).padding(.bottom, 8)
            VStack(spacing: 0) {
                ForEach(...) { index, layer in
                    layerRow(index: index, layer: layer)
                    if index < viewModel.config.watermarks.count - 1 {
                        Divider().padding(.leading, 52) // Pitfall 2: align with text after icon
                    }
                }
            }
            .markepiGlass(shape: RoundedRectangle(...), isEnabled: !reduceTransparency)
            .clipShape(RoundedRectangle(...))
            .padding(.horizontal, 16)
        }
    }
    ```
    - Replace `.background(.regularMaterial, in: RoundedRectangle(cornerRadius: 10))` with `.markepiGlass(shape: RoundedRectangle(cornerRadius: 12, style: .continuous), isEnabled: !reduceTransparency)`
    - Replace `.clipShape(RoundedRectangle(cornerRadius: 10))` with `.clipShape(RoundedRectangle(cornerRadius: 12, style: .continuous))`
    - Replace "Watermark Layers" header with "Layers" using `.markepiTypography(.sectionHeader)`

    Add to struct: `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency`

    **layerRow function (lines 37-70):**
    New structure:
    ```
    Button { viewModel.activeLayerIndex = index } label: {
        HStack(spacing: 12) {
            Image(systemName: layerIcon(for: layer))
                .foregroundStyle(viewModel.activeLayerIndex == index ? .accentColor : .secondary)
                .frame(width: 24)
            VStack(alignment: .leading, spacing: 2) {
                Text(layerTypeName(for: layer)).markepiTypography(.controlLabel)
                Text(layerSubtitle(for: layer)).markepiTypography(.metadata).lineLimit(1)
            }
            Spacer()
            Button {
                withAnimation(.easeOut(duration: 0.25)) { viewModel.removeLayer(at: index) }
            } label: {
                Image(systemName: "xmark.circle.fill").font(.title3)
            }
            .buttonStyle(.markepiDestructive())  // ← D-10 destructive, D-11 icon-only
            .frame(width: 44, height: 44)
            .accessibilityLabel("Remove layer: \(layerDescription(for: layer))")
            .accessibilityHint("Double tap to remove this watermark layer")
        }
        .padding(.horizontal, 16)
        .padding(.vertical, 10)
        .background(viewModel.activeLayerIndex == index ? Color.accentColor.opacity(0.08) : Color.clear)
    }
    .buttonStyle(.plain) // main row button is plain — not Markepi styled
    ```

    Key changes:
    - **Active-layer highlight:** Row background shows `Color.accentColor.opacity(0.08)` when `viewModel.activeLayerIndex == index`, `Color.clear` otherwise. This was NOT in the current code.
    - **Active-layer icon:** SF Symbol color changes between `.accentColor` (active) and `.secondary` (inactive) — was always `.secondary` before.
    - **Row label split:** Current has single `Text(layerDescription(for: layer))`. New has VStack with `.markepiTypography(.controlLabel)` title + `.markepiTypography(.metadata)` subtitle. Add `layerTypeName(for:)` function: returns "Text" for .text, "Logo" for .image, "Signature" for .signature. Add `layerSubtitle(for:)` function: returns truncated text for .text, "Image watermark" for .image, "Hand-drawn" for .signature.
    - **Remove button:** Was `Image(systemName: "xmark.circle.fill").foregroundStyle(.red)` — replaced with `.buttonStyle(.markepiDestructive())`. Per D-11: layer remove = icon-only (no text label).
    - **Main row button:** `.buttonStyle(.plain)` so the row itself doesn't get capsule treatment — only the nested remove button gets Markepi styling.
    - **Divider alignment:** `.padding(.leading, 52)` — 24pt icon + 12pt spacing + 16pt padding = 52pt indent (Pitfall 2).

    **D-08 typography mapping:**
    | Old | New |
    |-----|-----|
    | `.font(.title3.weight(.semibold))` on "Watermark Layers" | `.markepiTypography(.sectionHeader)` on "Layers" |
    | `.font(.body).foregroundStyle(.primary)` on layer name | `.markepiTypography(.controlLabel)` on type name |
    | (no metadata before) | `.markepiTypography(.metadata)` on subtitle |

    **Accessibility (Pitfall 4):** Preserve `.accessibilityLabel("Remove layer: \(layerDescription(for: layer))")` and `.accessibilityHint(...)` on remove button. Keep `layerDescription` function for accessibility labels.

    ---

    **BUILD CHECK AFTER TASK:** Run `bash scripts/build-gate.sh` to verify all 3 files compile.
  </action>

  <acceptance_criteria>
    <!-- LogoPickerView -->
    - LogoPickerView.swift contains `.buttonStyle(.markepiPrimary())` (add button)
    - LogoPickerView.swift contains `.buttonStyle(.markepiDestructive())` (remove button)
    - LogoPickerView.swift contains `.markepiTypography(.controlLabel)` (selected logo label)
    - LogoPickerView.swift contains `.markepiGlass` (glass section container)
    - LogoPickerView.swift does NOT contain `.buttonStyle(.bordered)`
    - LogoPickerView.swift does NOT contain `.foregroundStyle(.red)`
    - LogoPickerView.swift does NOT contain `.font(.title3.weight(.semibold))`
    - `.accessibilityLabel("Add logo watermark")` preserved
    - PhotosPicker/.onChange fileImporter flows unchanged

    <!-- SignatureCaptureView -->
    - SignatureCaptureView.swift main branch contains `.buttonStyle(.markepiPrimary())` (add + edit buttons)
    - SignatureCaptureView.swift main branch contains `.buttonStyle(.markepiDestructive())` (remove button)
    - SignatureCaptureView.swift main branch contains `.markepiTypography(.controlLabel)`
    - SignatureCaptureView.swift #else stub contains `.buttonStyle(.markepiPrimary())`
    - SignatureCaptureView.swift does NOT contain `.foregroundStyle(.blue)` on edit
    - SignatureCaptureView.swift does NOT contain `.foregroundStyle(.red)` on remove
    - SignatureCaptureView.swift does NOT contain `.buttonStyle(.bordered)` in main branch
    - PencilKit canvas and toolbar code unchanged (lines 113-186)

    <!-- LayerListView -->
    - LayerListView.swift contains `.markepiGlass` (replaces .regularMaterial)
    - LayerListView.swift contains `Color.accentColor.opacity(0.08)` (active-layer highlight)
    - LayerListView.swift contains `.buttonStyle(.markepiDestructive())` (remove button)
    - LayerListView.swift contains `.buttonStyle(.plain)` (main row button)
    - LayerListView.swift contains `.markepiTypography(.sectionHeader)` (on "Layers" header)
    - LayerListView.swift contains `.markepiTypography(.controlLabel)` (on layer type name)
    - LayerListView.swift contains `.markepiTypography(.metadata)` (on layer subtitle)
    - LayerListView.swift contains `layerTypeName(for:)` function (new)
    - LayerListView.swift contains `layerSubtitle(for:)` function (new)
    - LayerListView.swift contains `Divider().padding(.leading, 52)` (Pitfall 2 alignment)
    - LayerListView.swift does NOT contain `.foregroundStyle(.red)` on remove button icon
    - LayerListView.swift does NOT contain `.background(.regularMaterial`
    - LayerListView.swift does NOT contain `.font(.title3.weight(.semibold))`
    - `.accessibilityLabel("Remove layer:")` preserved on remove button

    <!-- Cross-file -->
    - Zero raw `.buttonStyle(.bordered)` or `.buttonStyle(.borderedProminent)` in any of the 6 files
    - Zero `.foregroundStyle(.red)` or `.foregroundStyle(.blue)` used as button tint in any of the 6 files
    - Zero `.font(.title3.weight(.semibold))` in any of the 6 files
    - Zero `.tint(.green)`, `.tint(.orange)`, `.tint(.accentColor)` on buttons in any of the 6 files
  </acceptance_criteria>

  <verify>
    <automated>bash scripts/build-gate.sh</automated>
  </verify>

  <done>
    Logo and Signature pickers use MarkepiButtonStyle for add/remove/edit with consistent icon+text (primary) and text-only (destructive) labels. Layer list shows active-layer highlight with accentColor.opacity(0.08) background and icon-only destructive remove button. All 6 sub-view files compile. All bindings and protocol calls unchanged. Build passes all 3 targets.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sub-view → ViewModel | Each sub-view reads/writes ViewModel config through WatermarkConfigurable protocol. Data flow unchanged — only presentation changes. |
| LogoPickerView → PhotosPicker/fileImporter | External file data enters through system pickers, passed to ViewModel. Input validation happens in WatermarkEngine (frozen). UI restyling does not add new input paths. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-04 | Information Disclosure (accessibility gap) | All 6 sub-views | mitigate | Preserve all existing .accessibilityLabel/.accessibilityHint modifiers from current sub-views . Specifically: LogoPickerView addLogoButton labels (lines 94-95), SignatureCaptureView addSignatureButton labels (lines 74-75), ScaleStepperView accessibility modifiers (lines 41-42), WhiteFrameToggleView accessibility modifiers (lines 31-32), LayerListView remove button labels (lines 64-65). New UI elements (position menu, export menu) add their own accessibility labels per RESEARCH.md Pitfall 4. |
| T-16-05 | Tampering (button role mismatch) | LogoPickerView, SignatureCaptureView, LayerListView | mitigate | Verify every button's MarkepiButtonRole against D-10 mapping: Add Logo/Signature = .primary (icon+text per D-11), Edit Signature = .primary (icon+text), Remove Logo/Signature = .destructive (text-only per D-11), Layer remove = .destructive (icon-only per D-11). No button should use .secondary for destructive actions or .destructive for non-destructive actions. |
</threat_model>

<verification>
1. `bash scripts/build-gate.sh` exits 0 with "BUILD GATE: PASSED" across WatermarkApp, ShareExtension, PhotoEditExtension
2. `grep -rn "\.buttonStyle(\.borderedProminent)\|\.buttonStyle(\.bordered)" Packages/WatermarkCore/Sources/WatermarkCore/UI/TextWatermarkInputView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/ScaleStepperView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/WhiteFrameToggleView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift` returns zero matches
3. `grep -rn "\.font(\.title3\.weight(\.semibold))" Packages/WatermarkCore/Sources/WatermarkCore/UI/TextWatermarkInputView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/ScaleStepperView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/WhiteFrameToggleView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift` returns zero matches
4. `grep -rn "\.foregroundStyle(\.red)\|\.foregroundStyle(\.blue)" Packages/WatermarkCore/Sources/WatermarkCore/UI/LogoPickerView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/SignatureCaptureView.swift Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift` returns zero matches (only in PencilKit toolbar buttons within SignatureCaptureView sheet are exempt)
</verification>

<success_criteria>
All 6 sub-view files render with inset grouped row pattern, MarkepiTypography, and MarkepiButtonStyle. Every button uses the correct role per D-10. All existing accessibility labels are preserved. Zero behavior changes to ViewModel interactions. Build passes all 3 targets.
</success_criteria>

<output>
Create `.planning/phases/16-redesigned-controls/16-02-SUMMARY.md` when done
</output>
