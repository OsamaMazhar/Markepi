---
phase: 16-redesigned-controls
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift
  - Packages/WatermarkCore/Sources/WatermarkCore/UI/PositionGridView.swift
autonomous: true
requirements:
  - CTL-01
  - CTL-07
  - CTL-08

must_haves:
  truths:
    - "A single button shows the current watermark position name with a disclosure arrow (chevron.up.chevron.down)"
    - "Tapping the position button presents a Menu listing all 9 position names as plain text items"
    - "Controls are grouped into 3 pill bar sections — Watermark (text+position+scale), Style (logo+signature+whiteFrame+layers), Output (export+template)"
    - "Export Format uses a tap-to-open Menu row with format labels (HEIC, JPEG, PNG, TIFF, Match Source), not an inline DisclosureGroup"
    - "Quality slider is a separate row below the format row"
    - "Share/WatermarkAll (.idle), Ready to Share (.done), Retry (.error), Cancel (.renderingVideo), Stop Processing (.batchProcessing), Save-as-Template buttons all use MarkepiButtonStyle capsules"
    - "Share button Label uses SF Symbol + text for primary, text-only for secondary"
    - "HDR→JPEG warning alert still appears when user selects JPEG with an HDR source"
    - "Content scrolls beneath the glass-backed pill bar with scroll-edge blur"
  artifacts:
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift"
      provides: "Rewritten control hub — pill bar + section switching + scroll-edge protection + position menu + export menu + share button state machine + template button"
      contains: "MarkepiScrollEdgeProtection"
      contains: "MarkepiPillBar"
      contains: "positionMenuRow"
      contains: "exportFormatRow"
      contains: "showHDRLossWarning"
      contains: "section: ControlsSection"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift"
      provides: "displayName extension mapping rawValue cases to human-readable names"
      contains: "var displayName: String"
      contains: "Top Left"
      contains: "Bottom Right"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/UI/PositionGridView.swift"
      provides: "DELETED — replaced by inline Menu in ControlsView"
      exists: false
  key_links:
    - from: "MarkepiPillBar(selection: $section)"
      to: "ControlsView.body switch section { case .watermark/.style/.output }"
      via: "@State private var section: ControlsSection binding"
      pattern: "MarkepiPillBar\\(selection: \\$section\\)"
    - from: "positionMenuRow Menu Button"
      to: "viewModel.updateLayerPosition(at:position:)"
      via: "Menu ForEach WatermarkPosition.allCases"
      pattern: "updateLayerPosition\\(at:"
    - from: "exportFormatRow Menu Button"
      to: "viewModel.config.outputFormat"
      via: "Menu Button actions setting .heic/.jpeg/.png/.tiff/.preserveSource"
      pattern: "viewModel\\.config\\.outputFormat ="
---

<objective>
Rewrites the ControlsView architecture from a flat `ScrollView > VStack` into a pill-bar-driven layout with three section-switched content areas, replaces the 3×3 `PositionGridView` with a single Menu button, replaces the Export Options `DisclosureGroup` with a Menu row, and restyles every button (Share, template, cancel, retry) with `MarkepiButtonStyle`. The underlying `WatermarkConfigurable` protocol calls and `RenderingState` state machine are preserved unchanged — this is a pure UI reskin.

Purpose: Establish the control hub structure that Phase 16's sub-view refactors (Plan 02) will slot into, and deliver the position picker (CTL-01), export menu (CTL-07), and button restyling (CTL-08) in one atomic unit.

Output: Rewritten ControlsView.swift (~300 lines), WatermarkPosition.swift with displayName extension appended, PositionGridView.swift deleted.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
<!-- Phase 16 source of truth -->
@.planning/phases/16-redesigned-controls/16-CONTEXT.md
@.planning/phases/16-redesigned-controls/16-RESEARCH.md
@.planning/phases/16-redesigned-controls/16-PATTERNS.md

<!-- Current source files being modified -->
@Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/PositionGridView.swift

<!-- Design system primitives consumed -->
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/MarkepiPillBar.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ScrollEdge/MarkepiScrollEdgeProtection.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ButtonStyles/MarkepiButtonStyle.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/Typography/MarkepiTypography.swift
@Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/GlassEffect/MarkepiGlassModifier.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete PositionGridView, add WatermarkPosition.displayName extension</name>
  <files>
    Packages/WatermarkCore/Sources/WatermarkCore/UI/PositionGridView.swift
    Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift
  </files>

  <read_first>
    <!-- Understand the current grid being replaced -->
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/PositionGridView.swift
    <!-- Understand where to append the extension -->
    @Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift
    <!-- Reference implementation for the displayName extension shape -->
    @.planning/phases/16-redesigned-controls/16-PATTERNS.md (lines 720-740)
  </read_first>

  <action>
    Delete PositionGridView.swift (94 lines, entire file). This 3×3 grid of text-labeled buttons (TL/TC/TR/ML/C/MR/BL/BC/BR) is fully replaced by the inline Menu in ControlsView (Task 2).

    Append a `displayName` extension on `WatermarkPosition` to the end of WatermarkPosition.swift (after line 80, the closing brace of the WatermarkPosition enum). The extension maps each of the 9 rawValue cases to a human-readable display name for the Menu picker:

    - topLeft → "Top Left"
    - topCenter → "Top Center"
    - topRight → "Top Right"
    - middleLeft → "Middle Left"
    - center → "Center"
    - middleRight → "Middle Right"
    - bottomLeft → "Bottom Left"
    - bottomCenter → "Bottom Center"
    - bottomRight → "Bottom Right"

    The extension is `internal` (no access modifier — default). It uses `var displayName: String { switch self { ... } }` with all 9 cases exhaustively. No default case — the compiler enforces exhaustiveness.

    Per D-02: plain text position names only. No directional icons, no thumbnails, no image rendering. These strings appear in the Menu items and the current-selection label.
  </action>

  <acceptance_criteria>
    - PositionGridView.swift does not exist on disk
    - WatermarkPosition.swift line count ≥ 118 (80 original + ~38 extension lines)
    - `grep -c "var displayName" Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift` returns 1
    - `grep -c "case \.topLeft:" Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift` returns 1
    - `grep -c "case \.bottomRight:" Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift` returns 1
    - All 9 displayName cases are present (grep confirms 9 "return" lines in the displayName switch)
    - No `default:` case in the displayName switch (grep confirms 0 matches for "default:" inside the extension)
  </acceptance_criteria>

  <verify>
    <automated>bash scripts/build-gate.sh</automated>
  </verify>

  <done>
    WatermarkPosition.displayName is available for ControlsView's position Menu. PositionGridView.swift is gone — no compilation references remain.
  </done>
</task>

<task type="auto">
  <name>Task 2: Rewrite ControlsView body with pill bar, section switching, position menu, export menu, button restyling</name>
  <files>
    Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
  </files>

  <read_first>
    <!-- Current implementation to understand what must be preserved vs replaced -->
    @Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
    <!-- Pattern map with exact before/after code for each section -->
    @.planning/phases/16-redesigned-controls/16-PATTERNS.md (lines 25-153 for ControlsView rewrite patterns)
    <!-- RESEARCH.md for position menu, export menu, button restyling patterns -->
    @.planning/phases/16-redesigned-controls/16-RESEARCH.md (lines 194-286 for Menu patterns, lines 578-677 for button restyling)
    <!-- Design system APIs: pill bar signature, scroll-edge signature, button convenience modifiers -->
    @Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/MarkepiPillBar.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ScrollEdge/MarkepiScrollEdgeProtection.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/ButtonStyles/MarkepiButtonStyle.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/Typography/MarkepiTypography.swift
    @Packages/WatermarkCore/Sources/WatermarkCore/DesignSystem/GlassEffect/MarkepiGlassModifier.swift
  </read_first>

  <action>
    Rewrite ControlsView.swift body (lines 33-63) and all private view builders (lines 65-279). The generic signature (line 12), @State viewModel (line 13), init (lines 29-31), and imports (lines 1-2) stay. The RenderingState state machine logic stays — only button styling changes.

    **ARCHITECTURE: Pill bar + scroll-edge protection (Pitfall 1: no nested ScrollViews)**

    Strip the `ScrollView(.vertical)` wrapper entirely (lines 34-62). The new body is:

    ```
    VStack(spacing: 0) {
        switch section {
        case .watermark:  watermarkSectionContent
        case .style:      styleSectionContent
        case .output:     outputSectionContent
        }
    }
    .markepiScrollEdgeProtection {
        MarkepiPillBar(selection: $section)
    }
    ```

    Do NOT wrap any content in another ScrollView — MarkepiScrollEdgeProtection creates its own internal ScrollView.

    **SECTION STATE:** Add `@State private var section: ControlsSection = .watermark` alongside the existing `@State var viewModel`. Add `@Environment(\.accessibilityReduceTransparency) private var reduceTransparency`.

    **CONTROLSECTION HELPER:** Add a private struct inside ControlsView.swift:

    ```
    private struct ControlSection<Content: View>: View {
        @Environment(\.accessibilityReduceTransparency) private var reduceTransparency
        @ViewBuilder let content: () -> Content
        var body: some View {
            VStack(spacing: 0) { content() }
                .markepiGlass(shape: RoundedRectangle(cornerRadius: 12, style: .continuous),
                              isEnabled: !reduceTransparency)
                .clipShape(RoundedRectangle(cornerRadius: 12, style: .continuous))
                .padding(.horizontal, 16)
        }
    }
    ```

    **SECTION 1 — watermarkSectionContent (CTL-01):**
    Contains the text input, position picker, and scale stepper in grouped rows.

    Build a `positionMenuRow` (private var) using SwiftUI `Menu` — replaces PositionGridView entirely per D-01/D-02/D-03:
    - Label: HStack with Text("Position").markepiTypography(.controlLabel) + Spacer() + HStack showing currentPos.displayName (via .markepiTypography(.value)) + chevron.up.chevron.down SF Symbol in .caption.weight(.medium) .foregroundStyle(.secondary)
    - Menu items: ForEach over WatermarkPosition.allCases, each a Button(position.displayName) calling viewModel.updateLayerPosition(at: idx, position: position)
    - Current position: derive from viewModel.config.watermarks at viewModel.activeLayerIndex (with bounds checking), defaulting to .center
    - Accessibility: .accessibilityLabel("Watermark position, currently \(currentPos.displayName)") and .accessibilityHint("Double tap to choose a different position")
    - Row padding: .horizontal 16, .vertical 12

    The watermark section layout (using ControlSection containers):

    1. ControlSection { TextWatermarkInputView(viewModel: viewModel) }
       (TextWatermarkInputView is already refactored by Plan 02 — it renders as a glass-backed row with .markepiTypography)
    2. ControlSection { positionMenuRow; Divider().padding(.leading, 52); ScaleStepperView(viewModel: viewModel) }
       (Divider with .leading 52 aligns with text after SF Symbol icons per Pitfall 2)

    **SECTION 2 — styleSectionContent:**
    Contains logo picker, signature capture, white frame toggle, and layer list.

    Layout (sub-views already refactored by Plan 02, each renders its own glass container or row):
    - LogoPickerView(viewModel: viewModel).padding(.bottom, 16)
    - SignatureCaptureView(viewModel: viewModel).padding(.bottom, 16)
    - WhiteFrameToggleView(viewModel: viewModel).padding(.bottom, 16)
    - LayerListView(viewModel: viewModel)

    Each sub-view is responsible for its own ControlSection or row container per Plan 02.

    **SECTION 3 — outputSectionContent (CTL-07, CTL-08):**
    Contains export options (format menu + quality slider) and save-as-template button.

    `exportFormatRow` (private var, replaces DisclosureGroup per D-07):
    - Label: HStack with Text("Format").markepiTypography(.controlLabel) + Spacer() + HStack showing currentFormatLabel (via .markepiTypography(.value)) + chevron.up.chevron.down SF Symbol
    - Menu items: "HEIC" → .heic, "JPEG" → HDR check + .jpeg, "PNG" → .png, "TIFF" → .tiff, "Match Source\(sourceFormatLabel)" → .preserveSource
    - Row padding: .horizontal 16, .vertical 12

    `qualitySliderRow` (private var):
    - HStack: Text("Quality").markepiTypography(.controlLabel) + Spacer() + Text("\(Int(viewModel.config.outputQuality * 100))%").markepiTypography(.value) (monospacedDigit auto)
    - Slider below, range 0.6...1.0, step 0.01, snap-to-1.0 at ≥0.98, disabled when outputFormat.isLossless

    Layout:
    - ControlSection { exportFormatRow; Divider().padding(.leading, 52); qualitySliderRow }
    - .alert("HDR Will Be Lost", isPresented: $showHDRLossWarning) { ... } message: { ... } attached to this ControlSection (preserve exact alert content from lines 115-125 of current ControlsView.swift)
    - ControlSection { saveTemplateButton } — separate container per RESEARCH.md Open Question 4

    **SHARE BUTTON STATE MACHINE (preserved logic, restyled buttons):**

    Keep the `@State private var showHDRLossWarning`, `@State private var pendingFormatSelection`, `private var sourceFormatLabel`, `private var isBatchMode` properties exactly as they are (lines 17-27).

    Keep the `shareButton` private var with the existing switch on `viewModel.renderingState` (lines 128-264). Apply ONLY these styling changes:

    | State | Old Styling | New Styling | Label |
    |-------|-------------|-------------|-------|
    | .idle | .borderedProminent | .buttonStyle(.markepiPrimary()) | Label("Share"/"Watermark All", systemImage: SF Symbol) .frame(maxWidth: .infinity) |
    | .rendering | .background(.regularMaterial, in: RoundedRectangle(cornerRadius: 10)) | .markepiGlass(shape: RoundedRectangle(cornerRadius: 12, style: .continuous), isEnabled: !reduceTransparency) | ProgressView() + Text("Rendering...").markepiTypography(.controlLabel) |
    | .renderingVideo | ProgressView .tint(.blue) + Cancel .bordered .tint(.red) | ProgressView .tint(.blue) unchanged + Cancel → .buttonStyle(.markepiSecondary()) with Text("Cancel") text-only | Cancel label: Text("Cancel").frame(maxWidth: .infinity) |
    | .batchProcessing | ProgressView .tint(.blue) + Stop .bordered .tint(.red) | ProgressView .tint(.blue) unchanged + Stop → .buttonStyle(.markepiSecondary()) with Text("Stop Processing") text-only | Stop label: Text("Stop Processing").frame(maxWidth: .infinity) |
    | .done | .borderedProminent .tint(.green) | .buttonStyle(.markepiPrimary()) | Label("Ready to Share"/"Ready to Share All", systemImage: "checkmark.circle.fill") .frame(maxWidth: .infinity) |
    | .error | .bordered .tint(.orange) | .buttonStyle(.markepiSecondary()) | Label("Retry", systemImage: "arrow.clockwise") .frame(maxWidth: .infinity) |

    Do NOT change: the switch cases, the Task { await ... } closures, the button action closures, the `withAnimation` in .done, the .animation modifier on the Group, all @State properties, viewModel method calls, or the isBatchMode computed property.

    **SAVE-AS-TEMPLATE BUTTON (CTL-08, D-10 secondary):**

    Keep the button action `viewModel.showSaveTemplateAlert = true`. Change:
    - Label: `Label("Save as Template", systemImage: "square.and.arrow.down.on.square")` with `.frame(maxWidth: .infinity)`
    - Style: `.buttonStyle(.markepiSecondary())` — was `.bordered .tint(.accentColor)`
    - Per D-11: secondary = icon+text per context (template uses icon+text)

    **HDR→JPEG WARNING PRESERVATION (Pitfall 5):**
    The `@State private var showHDRLossWarning` and `@State private var pendingFormatSelection` properties stay on ControlsView. The `.alert("HDR Will Be Lost", ...)` modifier (lines 115-125) moves from the old DisclosureGroup to the output section's ControlSection container. The JPEG Menu button action MUST check `viewModel.sourceHasHDR`, set `pendingFormatSelection = .jpeg`, and set `showHDRLossWarning = true` before setting `viewModel.config.outputFormat = .jpeg`.

    **QUALITY SLIDER SNAP-TO-1.0 LOGIC PRESERVATION:**
    Keep the existing snap logic (lines 103-109): when newValue ≥ 0.98 and < 1.0, snap to 1.0.

    **ANIMATION PRESERVATION:**
    Keep the `.animation(reduceMotion ? nil : .easeInOut(duration: 0.2), value: viewModel.renderingState)` on the share button Group.
  </action>

  <acceptance_criteria>
    <!-- Architecture -->
    - ControlsView.swift body does NOT contain `ScrollView(.vertical)` — MarkepiScrollEdgeProtection provides the scroll container
    - ControlsView.swift contains `@State private var section: ControlsSection = .watermark`
    - ControlsView.swift contains `.markepiScrollEdgeProtection { MarkepiPillBar(selection: $section) }`
    - ControlsView.swift contains a `private struct ControlSection` with `.markepiGlass(shape: RoundedRectangle(...))` and `.clipShape(RoundedRectangle(...))`
    - ControlsView.swift contains `switch section { case .watermark: ... case .style: ... case .output: ... }`

    <!-- Position picker (CTL-01) -->
    - ControlsView.swift contains `Menu { ForEach(WatermarkPosition.allCases` — position picker is inline
    - ControlsView.swift does NOT reference `PositionGridView` (no import, no usage)
    - Position menu label contains `currentPos.displayName` and `Image(systemName: "chevron.up.chevron.down")`
    - Position menu has `.accessibilityLabel` containing "Watermark position"

    <!-- Export menu (CTL-07) -->
    - ControlsView.swift does NOT contain `DisclosureGroup("Export Options")`
    - ControlsView.swift contains `Menu {` with format options (HEIC, JPEG, PNG, TIFF, Match Source)
    - Export format row label contains `Image(systemName: "chevron.up.chevron.down")`

    <!-- Button restyling (CTL-08) -->
    - Zero occurrences of `.buttonStyle(.borderedProminent)` in ControlsView.swift
    - Zero occurrences of `.buttonStyle(.bordered)` in ControlsView.swift
    - Zero occurrences of `.tint(.green)`, `.tint(.orange)`, `.tint(.red)`, `.tint(.accentColor)` in ControlsView.swift (bare `.tint()` calls on buttons)
    - Share idle button uses `.buttonStyle(.markepiPrimary())`
    - Done button uses `.buttonStyle(.markepiPrimary())`
    - Error/Retry button uses `.buttonStyle(.markepiSecondary())`
    - Cancel button (.renderingVideo) uses `.buttonStyle(.markepiSecondary())` with `Text("Cancel")`
    - Stop Processing button (.batchProcessing) uses `.buttonStyle(.markepiSecondary())` with `Text("Stop Processing")`
    - Save-as-template button uses `.buttonStyle(.markepiSecondary())`
    - Rendering progress uses `.markepiGlass(shape: RoundedRectangle(...))` instead of `.background(.regularMaterial)`

    <!-- HDR warning preservation -->
    - ControlsView.swift contains `@State private var showHDRLossWarning = false`
    - ControlsView.swift contains `.alert("HDR Will Be Lost", isPresented: $showHDRLossWarning)`
    - JPEG Menu button action checks `viewModel.sourceHasHDR` before setting format

    <!-- Section grouping (D-04) -->
    - Watermark section contains TextWatermarkInputView + positionMenuRow + ScaleStepperView
    - Style section contains LogoPickerView + SignatureCaptureView + WhiteFrameToggleView + LayerListView
    - Output section contains exportFormatRow + qualitySliderRow + saveTemplateButton
  </acceptance_criteria>

  <verify>
    <automated>bash scripts/build-gate.sh</automated>
  </verify>

  <done>
    ControlsView uses pill bar with 3 sections, position picker is a Menu button showing current position name, export options use a Menu row instead of DisclosureGroup, all buttons use MarkepiButtonStyle capsules, HDR→JPEG warning still fires, build passes all 3 targets.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ControlsView → ViewModel | ControlsView reads/writes ViewModel config state through WatermarkConfigurable protocol. The Menu picker surfaces position names (non-sensitive display labels). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-16-01 | Information Disclosure (accessibility gap) | Position Menu in ControlsView | mitigate | Preserve .accessibilityLabel("Watermark position, currently \(currentPos.displayName)") and .accessibilityHint on the Menu button. Without this, VoiceOver reads generic "Button" instead of semantic position info (RESEARCH.md Pitfall 4). |
| T-16-02 | Tampering (state inconsistency) | WatermarkPosition.displayName extension | mitigate | The 9-case displayName switch is exhaustive with no default case — the compiler enforces every position has a display name. Verify each displayName maps to the correct WatermarkPosition case (e.g., bottomLeft → "Bottom Left", not "Top Left"). |
| T-16-03 | Information Disclosure (missing warning) | HDR→JPEG alert in ControlsView | mitigate | The .alert("HDR Will Be Lost") modifier MUST be preserved on the output section container. The JPEG Menu button MUST check viewModel.sourceHasHDR and set showHDRLossWarning = true before changing format (RESEARCH.md Pitfall 5). Without this, users lose HDR gain maps silently. |
</threat_model>

<verification>
1. `bash scripts/build-gate.sh` exits 0 with "BUILD GATE: PASSED" across WatermarkApp, ShareExtension, PhotoEditExtension
2. `grep -c "PositionGridView" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift` returns 0
3. `grep -c "DisclosureGroup" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift` returns 0
4. `grep -c ".buttonStyle(.borderedProminent)" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift` returns 0
5. `grep -c ".buttonStyle(.bordered)" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift` returns 0
6. `grep -c "showHDRLossWarning" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift` returns ≥ 3 (declaration + alert binding + JPEG action)
</verification>

<success_criteria>
ControlsView compiles and renders in all 3 targets. Position picker is a single Menu button showing the current position name. Export format is a Menu row. Every button uses MarkepiButtonStyle. HDR→JPEG warning is preserved. Pill bar switches between 3 grouped sections.
</success_criteria>

<output>
Create `.planning/phases/16-redesigned-controls/16-01-SUMMARY.md` when done
</output>
