---
phase: 03-video-processing-share-extension
plan: 03
type: execute
wave: 2
depends_on: [03-01, 03-02]
files_modified: [ShareExtension/ShareExtensionViewModel.swift, ShareExtension/ShareExtensionRootView.swift, ShareExtension/ShareViewController.swift]
autonomous: true
requirements: [MEDI-02, QUAL-04]
must_haves:
  truths:
    - "User can receive a video from another app via the share sheet, see a static frame preview with watermark overlay, configure watermark, and share the watermarked result"
    - "Multi-item sequential processing: configure watermark once, apply to each shared item, show share sheet for each in sequence (D-14)"
    - "HDR fallback warning displayed when video HDR cannot be preserved through CALayer overlay (D-10)"
    - "Unsupported media types show dialog offering to open in main app (D-16)"
  artifacts:
    - path: "ShareExtension/ShareExtensionViewModel.swift"
      provides: "Video NSItemProvider loading, VideoProcessor delegation, multi-item orchestration, HDR warning state"
      contains: "loadVideoFromProvider"
    - path: "ShareExtension/ShareExtensionRootView.swift"
      provides: "Static frame video preview, HDR fallback warning UI, unsupported type dialog"
      contains: "hdrPreservationWarning"
  key_links:
    - from: "ShareExtensionViewModel.loadVideoFromProvider"
      to: "NSItemProvider.loadFileRepresentation(forTypeIdentifier: UTType.movie.identifier)"
      via: "async continuation"
      pattern: "loadFileRepresentation.*UTType\.movie"
    - from: "ShareExtensionViewModel.generateVideoPreview"
      to: "VideoFrameExtractor.extract(from:)"
      via: "async throws call"
      pattern: "VideoFrameExtractor\.extract"
    - from: "ShareExtensionViewModel.renderAndShareVideo"
      to: "WatermarkEngine.shared.processVideo"
      via: "async throws call"
      pattern: "engine\.processVideo"
---

<objective>
**As a user, I want to** watermark videos shared from other apps via the iOS share sheet, **so that** I can add branding to video content before sharing without saving to my camera roll.

Purpose: Extend the share extension (built in Plan 01) to handle video media — load videos from NSItemProvider via `loadFileRepresentation` (Pitfall 2), display a static frame preview with watermark overlay (D-03), render through the VideoProcessor (Plan 02), handle HDR fallback warnings (D-10), support multi-item sequential processing (D-14), and offer unsupported type fallback to main app (D-16).

Output: The share extension now handles both photos (from Plan 01) and videos. Users see a single static frame preview for videos, configure watermarks with the same controls, and share via the iOS share sheet. Multi-item shares process sequentially with config reuse.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-video-processing-share-extension/03-CONTEXT.md
@.planning/phases/03-video-processing-share-extension/03-RESEARCH.md
@.planning/phases/03-video-processing-share-extension/03-PATTERNS.md
# Files from Plan 01 (share extension base)
@ShareExtension/ShareExtensionViewModel.swift
@ShareExtension/ShareExtensionRootView.swift
@ShareExtension/ShareViewController.swift
# Files from Plan 02 (video engine)
@Packages/WatermarkCore/Sources/WatermarkCore/Processing/VideoProcessor.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Processing/VideoFrameExtractor.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Processing/ExportValidator.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ShareExtensionViewModel for video NSItemProvider loading + static frame preview + VideoProcessor rendering</name>
  <files>ShareExtension/ShareExtensionViewModel.swift</files>
  <read_first>
ShareExtension/ShareExtensionViewModel.swift — file from Plan 01; review current state (loadSharedMedia for photos, generatePreview, renderAndPrepareShare, layer management, config sync)
Packages/WatermarkCore/Sources/WatermarkCore/Processing/VideoProcessor.swift — VideoProcessor.process returns (URL, ExportValidationResult) from Plan 02 Task 3
Packages/WatermarkCore/Sources/WatermarkCore/Processing/VideoFrameExtractor.swift — VideoFrameExtractor.extract(from:at:maxPixelSize:) from Plan 02 Task 2
Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift — process(sourceURL:config:) for photos, processVideo(sourceURL:config:) for videos
Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift — ProcessingResult with videoValidation field
.planning/phases/03-video-processing-share-extension/03-RESEARCH.md — NSItemProvider video loading pattern (lines 784-829), Pitfall 2 (lines 557-569), Pitfall 5 (lines 592-598)
  </read_first>
  <action>
Extend ShareExtensionViewModel with video support. The ViewModel already handles photos from Plan 01. Add parallel video methods and media type detection.

1. Media type detection:
- Add method `func detectMediaType(from provider: NSItemProvider) -> MediaType`:
  - `MediaType` enum: `case photo, video, unsupported`
  - Check `provider.hasItemConformingToTypeIdentifier(UTType.movie.identifier)` for video (check video FIRST — some video formats also match image UTIs)
  - Check `provider.hasItemConformingToTypeIdentifier(UTType.image.identifier)` for photo
  - Return `.unsupported` if neither matches
  - This is called by ShareViewController to determine which load method to call

2. Video loading from NSItemProvider (Pitfall 2 + Pitfall 5):
- Add method `func loadVideoFromProvider(_ provider: NSItemProvider) async`:
  - Use `provider.loadFileRepresentation(forTypeIdentifier: UTType.movie.identifier)` per Pitfall 2 (loadFileRepresentation provides file URL, memory-efficient; do NOT use loadItem for video — Pitfall 2, RESEARCH.md lines 562-567)
  - Implementation pattern per RESEARCH.md lines 791-808:
    ```swift
    let url = try await withCheckedThrowingContinuation { (continuation: CheckedContinuation<URL, Error>) in
        provider.loadFileRepresentation(forTypeIdentifier: UTType.movie.identifier) { url, error in
            if let error = error { continuation.resume(throwing: error); return }
            guard let url = url else { continuation.resume(throwing: PipelineError.invalidSource); return }
            // Pitfall 5: copy immediately — temp URL expires after this block
            let destURL = FileManager.default.temporaryDirectory
                .appendingPathComponent("shared_video_\(UUID().uuidString).mp4")
            do {
                try FileManager.default.copyItem(at: url, to: destURL)
                continuation.resume(returning: destURL)
            } catch {
                continuation.resume(throwing: error)
            }
        }
    }
    ```
  - Set `sourceURL = url`, `isVideo = true`, `isLoadingMedia = false`
  - Call `await generateVideoPreview()` (new method)
  - On error: set `errorMessage`, `showError = true`, `isLoadingMedia = false`

3. Static frame video preview (D-03):
- Add method `func generateVideoPreview() async`:
  - Guard `sourceURL != nil` and `isVideo == true`
  - Set `isGeneratingPreview = true`; defer `isGeneratingPreview = false`
  - Call `let frame = try? await VideoFrameExtractor.extract(from: sourceURL!)` to get CGImage of midpoint frame
  - Convert CGImage → UIImage: `UIImage(cgImage: frame)`
  - If watermark layers exist, apply watermark to the static frame using WatermarkCore rendering:
    a. Convert frame UIImage → CIImage: `CIImage(image: previewUIImage)`
    b. Build watermark CIImage layers from `config.watermarks` (same pattern as WatermarkEngine.buildFilterGraph, but simplified — only the watermark layers, no HDR pipeline)
    c. For each watermark layer: render via TextWatermarkRenderer/ImageWatermarkRenderer → scale via watermark.scale → position via PositionCalculator.position()
    d. Composite via WatermarkRenderer.composite(layers:onto:) — same as the photo pipeline
    e. Render via CIContextProvider.shared → CGImage → UIImage → set `previewImage`
  - If no watermark layers (config.watermarks is empty): preview is the raw frame UIImage
  - This gives WYSIWYG preview — the watermark is composited exactly as it will appear in the final output
  - Note: White frame is NOT applied to preview (white frame compositing requires knowledge of the final frame dimensions which may differ in the export)

4. Video rendering and share:
- Add method `func renderAndShareVideo() async`:
  - Guard `sourceURL != nil`, `isVideo == true`
  - Set `renderingState = .rendering`
  - Call `let result = try await engine.processVideo(sourceURL: sourceURL!, config: config)`
  - On success:
    - `fullResResult = result`
    - `renderingState = .done`
    - Check `result.videoValidation`: if `!result.videoValidation.hdrPreserved && result.videoValidation.warnings.contains(where: { $0.contains("HDR") })` → set `showHDRWarning = true` (D-10)
    - If `!result.videoValidation.audioTrackCountMatch` → set `showAudioWarning = true` (informational, not blocking)
  - On error: `renderingState = .error(error)`, `errorMessage = error.localizedDescription`, `showError = true`

5. New state properties:
- `var isVideo: Bool = false` — already added in Plan 01, gate media type
- `var showHDRWarning: Bool = false` — D-10 flag, surfaced in root view
- `var showAudioWarning: Bool = false` — informational
- `var hdrWarningMessage: String?` — detailed HDR warning text from ExportValidationResult

6. Unified loadSharedMedia — update to support video:
- Modify the existing `loadSharedMedia(from provider: NSItemProvider) async` (from Plan 01) to detect media type:
  - Call `detectMediaType(from:)` first
  - If `.video`: call `await loadVideoFromProvider(provider)`
  - If `.photo`: use existing photo loading logic
  - If `.unsupported`: set `isLoadingMedia = false`, signal unsupported type (handled in Task 2)

7. Unified renderAndPrepareShare — branch by media type:
- Modify existing `renderAndPrepareShare()` to check `isVideo`:
  - If `isVideo == true`: call `await renderAndShareVideo()`
  - If `isVideo == false`: use existing photo render logic

8. Unified generatePreview — branch by media type:
- Modify existing `generatePreview()` to check `isVideo`:
  - If `isVideo == true`: call `await generateVideoPreview()`
  - If `isVideo == false`: use existing photo preview logic (engine.process)

9. Layer management parity (D-02, D-05):
- All layer management methods (`addLogoLayer`, `removeLayer`, `updateLayerPosition`, `updateLayerScale`, `toggleWhiteFrame`) from Plan 01 work unchanged for video — they modify `config.watermarks` which is media-type-agnostic
- When a layer change occurs during video preview, call `generateVideoPreview()` to update the static frame with the new watermark

10. Import additions:
- Add `import AVFoundation` (for UTType.movie, if not already available via UniformTypeIdentifiers)
- Add `import CoreImage` (for CIImage watermark rendering on static frame)

11. Temp file for video loading:
- Per Pitfall 5: the NSItemProvider temp URL is ephemeral — copy to extension sandbox `FileManager.default.temporaryDirectory` IMMEDIATELY inside the continuation. The file extension should be ".mp4" (safe default for most video containers).
</action>
  <acceptance_criteria>
- ShareExtensionViewModel has `detectMediaType(from:)` returning `MediaType` (photo/video/unsupported)
- ShareExtensionViewModel has `loadVideoFromProvider(_:)` using `provider.loadFileRepresentation(forTypeIdentifier: UTType.movie.identifier)` with immediate copy to extension sandbox
- ShareExtensionViewModel has `generateVideoPreview()` using VideoFrameExtractor + WatermarkCore compositing on static frame
- ShareExtensionViewModel has `renderAndShareVideo()` calling engine.processVideo()
- `renderAndShareVideo()` checks `result.videoValidation.hdrPreserved` and sets `showHDRWarning` per D-10
- `renderAndShareVideo()` checks `result.videoValidation.audioTrackCountMatch` and sets informational flag
- `loadSharedMedia(from:)` branches to photo or video loading based on `detectMediaType`
- `renderAndPrepareShare()` branches to photo or video rendering based on `isVideo`
- `generatePreview()` branches to photo or video preview based on `isVideo`
- Layer mutation methods trigger `generateVideoPreview()` when `isVideo == true`
- grep -c "loadFileRepresentation.*UTType.movie" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "VideoFrameExtractor.extract" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "engine.processVideo" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "showHDRWarning" ShareExtension/ShareExtensionViewModel.swift returns ≥ 2 (property + set)
- grep -c "detectMediaType" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "copyItem.*to.*destURL" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1 (Pitfall 5 mitigation)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Requires manual verification on iOS simulator: 1) Record a short video in Simulator Photos app, 2) Share video → Watermark extension, 3) Verify static frame preview displays, 4) Configure text watermark, 5) Verify watermark appears on static preview, 6) Tap Share, 7) Verify video renders and share sheet opens, 8) Share to Files app, 9) Verify output video has watermark</automated>
  </verify>
  <done>ShareExtensionViewModel handles video loading via loadFileRepresentation (memory-safe), generates static frame preview with watermark overlay, delegates to VideoProcessor for full-resolution rendering, and surfaces HDR fallback warnings when applicable</done>
</task>

<task type="auto">
  <name>Task 2: Multi-item sequential processing (D-14) + HDR warning UI (D-10) + unsupported type dialog (D-16)</name>
  <files>ShareExtension/ShareExtensionViewModel.swift, ShareExtension/ShareExtensionRootView.swift, ShareExtension/ShareViewController.swift</files>
  <read_first>
ShareExtension/ShareExtensionViewModel.swift — current state after Task 1 (media type detection, video loading, unified render flow)
ShareExtension/ShareExtensionRootView.swift — current preview + controls layout from Plan 01
ShareExtension/ShareViewController.swift — current NSItemProvider loading from Plan 01
App/Views/ContentView.swift — main app's error alert and sheet patterns (lines 46-67)
.planning/phases/03-video-processing-share-extension/03-RESEARCH.md — Open Questions #3 (lines 927-930 — multi-item failure handling), D-14 constraint (lines 38), D-16 constraint (lines 41)
  </read_first>
  <action>
1. Multi-item sequential processing (D-14):
- D-14: "Multi-item shares process all items sequentially — configure watermark once, apply to each item, show share sheet for each in sequence."
- Add properties to ShareExtensionViewModel:
  - `var sharedItems: [NSItemProvider] = []` — all providers from the extension context
  - `var currentItemIndex: Int = 0` — tracks which item is being processed
  - `var itemResults: [ProcessingResult] = []` — accumulated results (for cleanup)
  - `var totalItemCount: Int { sharedItems.count }`
  - `var isMultiItem: Bool { sharedItems.count > 1 }`
  - `var multiItemProgress: String { "Item \(currentItemIndex + 1) of \(totalItemCount)" }`

- Modify ShareViewController.loadSharedMedia():
  - Get ALL NSExtensionItems from `extensionContext?.inputItems`
  - Collect ALL NSItemProviders from all items' attachments: `items.flatMap { ($0 as? NSExtensionItem)?.attachments ?? [] }`
  - Set `viewModel.sharedItems = allProviders`
  - Load first item: if `sharedItems.count > 0`, call `viewModel.loadSharedMedia(from: sharedItems[0])`
  - The viewModel now tracks `currentItemIndex = 0`

- Add method `func processNextItem() async`:
  - Called after share sheet dismisses for current item (in `handleShareDismiss`)
  - Increment `currentItemIndex`
  - If `currentItemIndex < sharedItems.count`:
    - Reset state: `sourceURL = nil`, `previewImage = nil`, `fullResResult = nil`, `renderingState = .idle`, `isVideo = false`, `isLoadingMedia = true`, `showHDRWarning = false`, `showAudioWarning = false`
    - Load next item: `await loadSharedMedia(from: sharedItems[currentItemIndex])`
    - User sees loading spinner → preview → configures → shares → repeat
  - If `currentItemIndex >= sharedItems.count`:
    - All items processed. Call `completeRequest?()` per D-07 (one-shot — close extension after all items done)

- Modify `handleShareDismiss()`:
  - Clean up current item's temp file
  - Append current fullResResult to `itemResults` (for batch cleanup tracking)
  - Call `processNextItem()` — which either loads next item or completes the extension
  - Do NOT call `completeRequest()` immediately for multi-item — only after all items done

- Error handling for multi-item (per RESEARCH.md Open Question #3):
  - If renderAndPrepareShare fails for an item: record the failure, show a brief error, then proceed to next item
  - Track failures: `var failedItemIndices: [Int] = []`
  - After all items processed: if any failures, show a summary alert: "Processed \(successCount) of \(totalItemCount) items. \(failureCount) failed." before completing the extension
  - User taps "Done" → completeRequest called

2. Sequential processing UI (ShareExtensionRootView):
- Show progress indicator when `isMultiItem == true`:
  - Text: "Item \(viewModel.currentItemIndex + 1) of \(viewModel.totalItemCount)" displayed above the controls area
  - A horizontal progress bar: `ProgressView(value: Double(viewModel.currentItemIndex + 1), total: Double(viewModel.totalItemCount))`
- The Share button text should be "Share (\(viewModel.currentItemIndex + 1) of \(viewModel.totalItemCount))" when multi-item

3. HDR fallback warning UI (D-10):
- In ShareExtensionRootView, add a warning banner when `viewModel.showHDRWarning == true`:
  - Yellow/orange background with warning icon (Image(systemName: "exclamationmark.triangle.fill"))
  - Text: "HDR could not be preserved. Video was exported in standard dynamic range."
  - Dismissible: user can tap an "X" button to dismiss the banner
  - Position: between preview area and controls area (above the divider line)
- The warning appears only after export completes (renderingState == .done) and only for videos
- Also surface audio warnings if audioTrackCountMatch is false: "Audio track count changed during export."

4. Unsupported type handling (D-16):
- In ShareExtensionRootView, when `viewModel.unsupportedTypeDetected == true` (set by ViewModel when `detectMediaType` returns `.unsupported`):
  - Show a full-screen overlay or alert: "This file type isn't supported yet."
  - Two buttons: "Open in App" and "Cancel"
  - "Open in App": calls `viewModel.openInMainApp()` which constructs `watermark://open` URL and calls `self.extensionContext?.open(url, completionHandler: nil)`
  - "Cancel": calls `completeRequest?()` — dismisses extension
- Add property `var unsupportedTypeDetected: Bool = false` to ViewModel
- Add method `func openInMainApp()` to ViewModel:
  - Construct URL: `URL(string: "watermark://open")` — the URL scheme registered in the main app's Info.plist (see Plan 01 Task 1 Xcode setup)
  - Call `extensionContext?.open(url, completionHandler: { success in ... })`
  - After opening URL: call `completeRequest?()` to dismiss extension
- The `extensionContext` is accessible from ShareViewController. Pass it to ViewModel via a closure: `var openURL: ((URL) -> Void)?` set by ShareViewController

5. ShareViewController updates:
- `loadSharedMedia()`: collect ALL providers, set on viewModel, load first
- Set `openURL` closure: `viewModel.openURL = { [weak self] url in self?.extensionContext?.open(url, completionHandler: nil) }`
- Set `completeRequest` closure already done in Plan 01 — remains unchanged

6. Root view layout updates for video (ShareExtensionRootView):
- Preview area now handles two states for the content type:
  - Photo: existing `Image(uiImage:)` display (from Plan 01)
  - Video: same `Image(uiImage:)` display (static frame) + optional "▶" play icon overlay to indicate video content (purely visual indicator, no interactivity)
- When video is loading: ProgressView with "Loading video..."
- Video-specific info: show video duration if available (read from `sourceURL` via AVAsset, display formatted duration). Low priority — can be omitted for Plan 03.

7. Temp file cleanup for multi-item:
- Track all output URLs in `itemResults`
- During `completeRequest`: call `TempFileManager.cleanup(url:)` for each item's URL
- Also clean up the copied input files (the shared_video_<UUID>.mp4 and shared_photo_<UUID>.jpg files)
</action>
  <acceptance_criteria>
- ShareExtensionViewModel.sharedItems collects all NSItemProviders from extension context
- ShareExtensionViewModel.processNextItem() loads next item after current share dismisses
- handleShareDismiss() calls processNextItem() for multi-item; calls completeRequest() only after all items done
- Failed items tracked in failedItemIndices; summary shown after all items processed
- ShareExtensionRootView shows "Item X of Y" + ProgressView when isMultiItem
- ShareExtensionRootView shows HDR warning banner when showHDRWarning == true (D-10)
- ShareExtensionRootView shows audio warning when showAudioWarning == true
- ShareExtensionRootView shows "Unsupported type" dialog when unsupportedTypeDetected == true (D-16)
- "Open in App" button constructs watermark://open URL and calls extensionContext.open(url:)
- All temp files cleaned up on completeRequest (both input copies and output files)
- grep -c "sharedItems" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "processNextItem" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "currentItemIndex" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "showHDRWarning" ShareExtension/ShareExtensionRootView.swift returns ≥ 1
- grep -c "unsupportedTypeDetected" ShareExtension/ShareExtensionRootView.swift returns ≥ 1
- grep -c "watermark://open" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "open.*url.*completionHandler" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Requires manual verification on iOS simulator: 1) Share 3 photos + 2 videos (5 items total), 2) Verify extension shows "Item 1 of 5", 3) Configure watermark, share item 1, 4) Verify item 2 loads automatically, 5) Repeat for all 5 items, 6) After item 5 shared, verify extension closes. Also test: share a PDF file, verify "Unsupported type" dialog appears, tap "Open in App", verify Watermark app opens.</automated>
  </verify>
  <done>Multi-item shares process sequentially with config reuse. HDR preservation failures show user-visible warning. Unsupported media types offer fallback to the main app via URL scheme. All temp files cleaned up after the complete sequence.</done>
</task>

<task type="auto">
  <name>Task 3: Controls sharing refactor — move shared UI views to WatermarkCore package for extension reuse + video preview polish</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift, ShareExtension/ShareExtensionRootView.swift, App/Views/ContentView.swift</files>
  <read_first>
App/Views/Controls/ControlsView.swift — the control views to move/refactor (imports: SwiftUI, WatermarkCore)
App/Views/Controls/TextWatermarkInputView.swift — text control
App/Views/Controls/PositionGridView.swift — position grid
App/Views/Controls/ScaleStepperView.swift — scale stepper
App/Views/Controls/LogoPickerView.swift — logo picker
App/Views/Controls/WhiteFrameToggleView.swift — frame toggle
App/Views/Controls/LayerListView.swift — layer list
App/ViewModels/WatermarkViewModel.swift — how controls bind to ViewModel (lines 149-205)
ShareExtension/ShareExtensionViewModel.swift — how extension ViewModel mirrors main app ViewModel
App/Views/ContentView.swift — main app's usage of ControlsView (line 107)
</read_first>
  <action>
Refactor the shared control views so both the main app and the share extension can reuse them without code duplication (D-05 — full watermarking UI in extension with complete parity).

Option: Move all control views from `App/Views/Controls/` into the WatermarkCore Swift Package under `Sources/WatermarkCore/UI/`. This makes them available to all targets that link WatermarkCore (main app, share extension, future Photos extension).

Step 1 — Create UI directory in WatermarkCore package:
- `Packages/WatermarkCore/Sources/WatermarkCore/UI/` — new directory

Step 2 — Move control views (7 files):
- Copy each file from `App/Views/Controls/` to `Packages/WatermarkCore/Sources/WatermarkCore/UI/`
- Files: ControlsView.swift, TextWatermarkInputView.swift, PositionGridView.swift, ScaleStepperView.swift, LogoPickerView.swift, WhiteFrameToggleView.swift, LayerListView.swift
- These files import `SwiftUI` and `WatermarkCore` — both available in the package. The WatermarkCore Package.swift already targets iOS .v18 which includes SwiftUI.
- The files currently import from the WatermarkCore module, which is their own module — self-imports work fine in Swift packages.

Step 3 — Refactor ControlsView to accept a generic binding:
- Current: `ControlsView(viewModel: WatermarkViewModel)` — tied to the main app's ViewModel type
- Refactored: Create a protocol `WatermarkConfigurable` that both ViewModels conform to:

```swift
// In WatermarkCore UI/WatermarkConfigurable.swift (new file)
import Foundation
import SwiftUI

@MainActor
public protocol WatermarkConfigurable: AnyObject {
    var config: WatermarkConfiguration { get set }
    var activeLayerIndex: Int { get set }
    var renderingState: RenderingState { get }
    var whiteFrameEnabled: Bool { get }
    
    func addLogoLayer(pngData: Data)
    func removeLayer(at index: Int)
    func updateLayerPosition(at index: Int, position: WatermarkPosition)
    func updateLayerScale(at index: Int, scale: CGFloat)
    func toggleWhiteFrame()
}
```

- Make `ControlsView` accept `@State var viewModel: any WatermarkConfigurable` instead of the concrete type
- Update ControlsView body to use protocol methods (same method signatures already exist on both ViewModels)
- Update WatermarkViewModel to conform to WatermarkConfigurable (add `: WatermarkConfigurable` to class declaration)
- Update ShareExtensionViewModel to conform to WatermarkConfigurable (add `: WatermarkConfigurable` to class declaration)
- Both ViewModels already implement all protocol methods — this is a pure protocol conformance addition

Step 4 — Update imports in main app:
- After moving controls to WatermarkCore, the main app imports them via `import WatermarkCore` (already imported)
- ControlsView is now `WatermarkCore.ControlsView` — same module, no import changes needed (self-import)
- ContentView.swift line 107 (`ControlsView(viewModel: viewModel)`) — no change needed if ControlsView is in the same module. But if ControlsView moved to WatermarkCore, and WatermarkCore is imported, the view is accessible as `ControlsView` (no module prefix needed in Swift when the module is imported).

Step 5 — Update share extension root view:
- After moving controls to WatermarkCore, ShareExtensionRootView imports `import WatermarkCore` and can use `ControlsView(viewModel: viewModel)` directly
- Remove any duplicated control code from the extension (if Plan 01 Task 1 duplicated controls)
- ShareExtensionRootView uses the same ControlsView as the main app — guaranteed UI parity per D-05

Step 6 — Video preview polish (D-03):
- Add a "▶" overlay on the video preview to indicate it's video content:
  - `Image(systemName: "play.rectangle.fill")` with `.font(.largeTitle)` and semi-transparent white color
  - Positioned in the center of the preview ZStack, only shown when `viewModel.isVideo == true`
- Add a tap interaction on the preview to show full-screen preview (optional, low priority):
  - `.onTapGesture { viewModel.showFullscreenPreview = true }` → fullScreenCover with zoomed image

Step 7 — Ensure iOS 18 compatibility:
- All moved views use SwiftUI components available in iOS 18
- `@Observable` macro (iOS 17+) — both ViewModels use this
- `@State` with protocol existential (`any WatermarkConfigurable`) — Swift 6 supports this
- Verify no UIKit dependencies in the moved views (SwiftUI-only views)

Step 8 — Clean up App target:
- Remove original files from `App/Views/Controls/` (already moved to WatermarkCore)
- Update any internal references in the main app
- The main app's Xcode project must be updated to remove the old file references
</action>
  <acceptance_criteria>
- 7 control view files exist in Packages/WatermarkCore/Sources/WatermarkCore/UI/
- WatermarkConfigurable protocol exists in WatermarkCore package (UI/WatermarkConfigurable.swift)
- ControlsView accepts `@State var viewModel: any WatermarkConfigurable` (not concrete WatermarkViewModel)
- WatermarkViewModel conforms to WatermarkConfigurable
- ShareExtensionViewModel conforms to WatermarkConfigurable
- ShareExtensionRootView uses ControlsView(viewModel: viewModel) from WatermarkCore (no duplication)
- Main app ContentView uses ControlsView from WatermarkCore (no duplication)
- Video preview shows "▶" overlay icon when isVideo == true
- Main app compiles with `import WatermarkCore` providing ControlsView
- Share extension compiles with same ControlsView import
- grep -c "WatermarkConfigurable" Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift returns ≥ 1
- grep -c "protocol WatermarkConfigurable" Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift returns 1
- grep -c ": WatermarkConfigurable" App/ViewModels/WatermarkViewModel.swift returns 1
- grep -c ": WatermarkConfigurable" ShareExtension/ShareExtensionViewModel.swift returns 1
- grep -c "any WatermarkConfigurable" Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift returns 1
- ls Packages/WatermarkCore/Sources/WatermarkCore/UI/ returns 8 files (7 controls + 1 protocol)
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Build both targets: `xcodebuild -project Watermark.xcodeproj -scheme Watermark -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1` (main app) and `xcodebuild -project Watermark.xcodeproj -scheme ShareExtension -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1` (extension). Both must compile without errors. Verify UI parity: open extension with a video, compare controls appearance against main app photo controls.</automated>
  </verify>
  <done>All watermarking control views are shared via the WatermarkCore package, eliminating code duplication between main app and extension. Video preview shows play indicator. Both targets compile with unified control views.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| NSItemProvider (video) → Extension Sandbox | Video file from untrusted sending app loaded via loadFileRepresentation, copied to extension sandbox |
| AVAssetExportSession (extension) → Temp Output | Rendered video exported to extension sandbox temp directory |
| Extension → Main App (URL Scheme) | Unsupported type fallback opens main app via `watermark://` URL scheme |
| Multi-Item Sequential Processing | Multiple untrusted media items processed in sequence; temp files accumulate until cleanup |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | ShareExtensionViewModel (video loading) | mitigate | Use loadFileRepresentation (streaming file URL, not in-memory) per Pitfall 2. Reject video files > 500MB before processing. Copy to extension sandbox immediately (Pitfall 5). Extension memory ceiling ~120MB — never load video into memory. |
| T-03-11 | Tampering | ShareExtensionViewModel (crafted video) | mitigate | AVFoundation validates codec parameters during AVAsset.load(). Malformed files fail at AVAsset load step. `loadFileRepresentation` provides a system-validated URL. Additional check: verify AVAsset.duration is non-zero and video track count > 0 before processing. |
| T-03-12 | Information Disclosure | ShareExtensionRootView (watermark://open URL) | mitigate | The `watermark://open` URL scheme carries no data — it only triggers app open. No media or configuration data is passed via URL (data stays in extension sandbox). Main app does not process URL query parameters beyond the scheme check. |
| T-03-13 | Denial of Service | Multi-item processing | mitigate | Process items one at a time (sequential). Clean up each item's temp files before loading the next. This prevents accumulation of large temp files during multi-item shares. Failed items don't block remaining items. |
| T-03-14 | Elevation of Privilege | App Group config sync (extension writes) | accept | Same as T-03-03 — App Group container restricted by Apple to same team ID. Extension writes config to UserDefaults; main app reads it. Config validated on deserialization (scale range, position enum values). |
</threat_model>

<verification>
Manual end-to-end verification required on iOS simulator (extension cannot be fully automated): share photo → verify photo watermarking works; share video → verify static frame preview and video rendering; share 3+ items → verify sequential processing; share PDF → verify unsupported type dialog. Build both targets to confirm compilation.
</verification>

<success_criteria>
1. Video shared from Photos app loads in extension via loadFileRepresentation (not in-memory)
2. Static frame preview shows watermarked video frame (WYSIWYG per D-03)
3. User configures watermark and taps Share → VideoProcessor renders full video → share sheet opens
4. HDR preservation failure shows user-visible warning banner (D-10)
5. Audio track mismatch shows informational warning
6. Multi-item share (3 photos + 1 video) processes sequentially: config configured once, applied to each item
7. Progress indicator shows "Item X of Y" during multi-item processing
8. Failed item processing doesn't block remaining items
9. Unsupported file types (PDF, etc.) show dialog with "Open in App" option (D-16)
10. All temp files cleaned up after complete sequence (no disk accumulation)
</success_criteria>

<output>
Create `.planning/phases/03-video-processing-share-extension/03-03-SUMMARY.md` when done
</output>
