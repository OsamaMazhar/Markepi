---
phase: 03-video-processing-share-extension
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [ShareExtension/ShareViewController.swift, ShareExtension/ShareExtensionRootView.swift, ShareExtension/ShareExtensionViewModel.swift, ShareExtension/Info.plist, ShareExtension/ShareExtension.entitlements, Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift]
autonomous: true
requirements: [MEDI-02]
must_haves:
  truths:
    - "User can receive a photo from another app via the iOS share sheet, configure a watermark, and share the watermarked result"
    - "Watermark configuration syncs between the main app and the share extension via App Group UserDefaults"
    - "Share extension appears in the share sheet for photos (public.image) and accepts up to 20 items"
  artifacts:
    - path: "ShareExtension/ShareViewController.swift"
      provides: "UIKit entry point hosting SwiftUI watermarking views"
    - path: "ShareExtension/ShareExtensionRootView.swift"
      provides: "SwiftUI root view composing preview + controls + share flow"
    - path: "ShareExtension/ShareExtensionViewModel.swift"
      provides: "@Observable state: NSItemProvider loading, config, rendering, share orchestration"
    - path: "ShareExtension/Info.plist"
      provides: "NSExtensionActivationRule accepting photos and videos"
    - path: "Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift"
      provides: "Codable JSON serialization of WatermarkConfiguration to/from shared UserDefaults"
  key_links:
    - from: "ShareViewController.viewDidLoad()"
      to: "ShareExtensionRootView"
      via: "UIHostingController(rootView:)"
      pattern: "UIHostingController.*ShareExtensionRootView"
    - from: "ShareExtensionViewModel.processAndShare()"
      to: "WatermarkEngine.shared.process"
      via: "async throws call"
      pattern: "engine\.process\(sourceURL:"
    - from: "ShareExtensionViewModel"
      to: "AppGroupConfigSync.load() / .save()"
      via: "init and config didSet"
      pattern: "AppGroupConfigSync\.(load|save)"
---

<objective>
**As a user, I want to** watermark photos shared from other apps via the iOS share sheet with the same controls as the main app, **so that** I can add branding before sharing without saving to my camera roll.

Purpose: Deliver the first vertical slice of the share extension — end-to-end photo watermarking via the share sheet. Users open a photo in Photos, tap Share → Watermark, configure their watermark in the extension UI, and share the result immediately.

Output: A working share extension target that hosts the same SwiftUI watermarking UI as the main app, loads photos from NSItemProvider, processes them via the existing WatermarkEngine, presents the share sheet, and closes after sharing. App Group config sync ensures the extension and main app share the same WatermarkConfiguration defaults.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-video-processing-share-extension/03-CONTEXT.md
@.planning/phases/03-video-processing-share-extension/03-RESEARCH.md
@.planning/phases/03-video-processing-share-extension/03-PATTERNS.md
# Existing patterns to follow
@Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift
@App/ViewModels/WatermarkViewModel.swift
@App/Views/Share/ShareSheetView.swift
@App/Views/ContentView.swift
@App/Models/PhotoItem.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create share extension target scaffold — ShareViewController (UIHostingController), root SwiftUI view shell, Info.plist, entitlements, and App Group config sync module</name>
  <files>ShareExtension/ShareViewController.swift, ShareExtension/ShareExtensionRootView.swift, ShareExtension/ShareExtensionViewModel.swift, ShareExtension/Info.plist, ShareExtension/ShareExtension.entitlements, Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift</files>
  <read_first>
ShareExtension/Info.plist, ShareExtension/ShareExtension.entitlements — files being created; review template below
Packages/WatermarkCore/Package.swift — confirms swift-tools-version:6.0 / iOS .v18 target / no external dependencies needed; WatermarkCore already consumed by main app target, so adding a new source file to the existing package target requires no Package.swift changes
App/WatermarkApp.swift — confirms app entry point and bundle structure for URL scheme inference
App/Views/Share/ShareSheetView.swift — the UIActivityViewController bridge used in the main app; identical bridge reused in the extension
App/ViewModels/WatermarkViewModel.swift — the @Observable @MainActor ViewModel pattern to mirror; note RenderingState enum (PhotoItem.swift lines 18-34), config properties (lines 14-20), engine delegation pattern (lines 35, 104-122), layer management methods (lines 149-201), cleanup method (lines 129-135)
Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift — WatermarkConfiguration struct (lines 9-37) is Sendable but NOT Codable; JSONEncoder will fail on CGColor and CGFloat unless wrapped; AppGroupConfigSync must use a Codable surrogate or JSON-safe encoding
  </read_first>
  <action>
Create the share extension target files. Xcode project configuration (adding the target to the .xcodeproj, linking WatermarkCore, enabling App Group capability in Signing & Capabilities) must be done in Xcode manually — the agent should create the Swift source files and configuration files, then provide step-by-step Xcode instructions.

File 1: ShareExtension/ShareExtensionRootView.swift
- Import SwiftUI and WatermarkCore
- Create the SwiftUI root view struct: accepts `@State var viewModel: ShareExtensionViewModel`
- Layout: VStack with 60/40 split (previewArea / controlsArea) — mirror ContentView.swift lines 9-18 but WITHOUT NavigationStack (extension has no nav bar)
- Preview area: ZStack showing either (a) loading spinner while media loads, (b) error text if loading failed, (c) an `Image(uiImage:)` from `viewModel.previewImage` scaled to `.fit` when loaded
- Controls area: `ControlsView(viewModel: viewModel)` — reuse the existing main app ControlsView (from App/Views/Controls/ControlsView.swift). The ControlsView accepts `@State var viewModel: WatermarkViewModel`, so create a protocol that both ViewModels conform to OR refactor ControlsView to accept the config binding directly. Simplest approach: pass `$viewModel.config` and closure callbacks. Do NOT duplicate the controls — share them.
- Add a "Share" button at the bottom of the controls area styled as a prominent button (`.buttonStyle(.borderedProminent)`). Text: "Share". Disabled when `viewModel.renderingState != .done`. On tap: calls `viewModel.presentShareSheet()`.
- Error alert: `.alert` presented when `viewModel.showError` is true, showing `viewModel.errorMessage`
- Share sheet: `.sheet(isPresented: $viewModel.showShareSheet)` presenting `ShareSheetView(activityItems: [url]) { viewModel.handleShareDismiss() }` — reuse the existing ShareSheetView from App/Views/Share/ShareSheetView.swift (it is in the main app target, NOT in WatermarkCore). For the extension to use it, either copy it into the extension target or move it to the WatermarkCore package. Given that it imports SwiftUI and UIKit (both available in extensions), copying into the extension target is simplest. Add it as ShareExtension/ShareSheetView.swift.

File 2: ShareExtension/ShareExtensionViewModel.swift
- Import Foundation, Observation, SwiftUI, UIKit, UniformTypeIdentifiers, WatermarkCore
- Annotate with `@Observable @MainActor final class`
- Properties:
  - `var config: WatermarkConfiguration` — initialized with default config (mirror WatermarkViewModel.swift lines 14-20). On init, call `AppGroupConfigSync.load()` to restore saved config.
  - `var previewImage: UIImage?` — watermarked preview
  - `var isGeneratingPreview: Bool = false`
  - `var renderingState: RenderingState = .idle` — reuse the existing RenderingState enum from App/Models/PhotoItem.swift (must be accessible from extension; either move to WatermarkCore or duplicate)
  - `var fullResResult: ProcessingResult?`
  - `var showShareSheet: Bool = false`
  - `var errorMessage: String?`
  - `var showError: Bool = false`
  - `var activeLayerIndex: Int = 0`
  - `var sourceURL: URL?` — the file URL of the shared media after NSItemProvider loading
  - `var isVideo: Bool = false` — gated by media type detection (always false for Plan 01; will be true in Plan 03)
  - `var isLoadingMedia: Bool = true` — true while NSItemProvider is loading; drives the loading spinner in the root view
- Private: `private let engine = WatermarkEngine.shared`
- Methods (following the exact patterns from WatermarkViewModel.swift):
  - `func loadSharedMedia(from provider: NSItemProvider) async` — D-13: For photos, use `provider.loadItem(forTypeIdentifier: UTType.image.identifier)` to get Data or URL, write to extension's caches directory temp file, set `sourceURL`. After loading, set `isLoadingMedia = false` and call `generatePreview()`. Handle errors by setting `errorMessage` and `showError`.
  - `func generatePreview() async` — same debounce pattern as WatermarkViewModel lines 88-102 (350ms sleep, Task.isCancelled guard, engine.process call, load result data into UIImage)
  - `func renderAndPrepareShare() async` — mirror WatermarkViewModel lines 104-122
  - `func presentShareSheet()` — mirror WatermarkViewModel lines 124-127
  - `func handleShareDismiss()` — cleanup temp file (mirror lines 129-135), then call `completeRequest()` from the extension context
  - `var completeRequest: (() -> Void)?` — closure set by ShareViewController; called after share dismiss per D-07
  - Layer management methods (`addLogoLayer`, `removeLayer`, `updateLayerPosition`, `updateLayerScale`, `toggleWhiteFrame`, `whiteFrameEnabled`) — same implementations as WatermarkViewModel lines 149-205
  - Config didSet: whenever `config` changes, call `AppGroupConfigSync.save(config)` per D-08
- NSItemProvider loading for photos (D-13): try `provider.loadItem(forTypeIdentifier: UTType.image.identifier)` with `withCheckedThrowingContinuation`. The completion handler receives `(item: NSSecureCoding?, error: Error?)`. Handle both `URL` and `Data` item types. If URL: read data from URL. If Data: use directly. Write data to `FileManager.default.temporaryDirectory.appendingPathComponent("shared_photo_\(UUID()).\(ext)")` where ext is "jpg". Set `sourceURL = destURL`, `isVideo = false`.
- Move RenderingState enum to WatermarkCore (Packages/WatermarkCore/Sources/WatermarkCore/Models/PhotoItem.swift is in App target; create a new file or add the enum to an existing WatermarkCore model file). Simplest: add `RenderingState` enum to WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift so both main app and extension can import it.

File 3: ShareExtension/ShareViewController.swift
- Import UIKit, SwiftUI
- Subclass UIViewController
- Properties: `private var hostingController: UIHostingController<ShareExtensionRootView>?`, `private let viewModel = ShareExtensionViewModel()`
- `override func viewDidLoad()`: call `super.viewDidLoad()`, then `setupHostingController()` (per RESEARCH.md Pattern 4, lines 432-463), then `Task { await loadSharedMedia() }`
- `private func setupHostingController()`: create `ShareExtensionRootView(viewModel: viewModel)`, wrap in `UIHostingController(rootView:)`, add as child, add view, set `translatesAutoresizingMaskIntoConstraints = false`, constrain to all 4 edges
- `private func loadSharedMedia() async`: get first `NSExtensionItem` from `extensionContext?.inputItems`, get first `NSItemProvider` from its `attachments`, call `viewModel.loadSharedMedia(from: provider)` — but loadSharedMedia needs to know the provider. Two options: (a) set the provider on the viewModel before calling, or (b) pass the provider as a parameter. Use approach (b): add a `func loadSharedMedia(from provider: NSItemProvider) async` on the viewModel and call it.
- Set `viewModel.completeRequest = { [weak self] in self?.extensionContext?.completeRequest(returningItems: nil) }` — this is set BEFORE the viewModel uses it (the viewModel stores it as a closure property)
- Load the shared media: call `viewModel.loadSharedMedia(from: provider)`

File 4: ShareExtension/Info.plist
- Standard plist with NSExtension dictionary
- NSExtensionPrincipalClass: `$(PRODUCT_MODULE_NAME).ShareViewController`
- NSExtensionPointIdentifier: `com.apple.share-services`
- NSExtensionActivationRule: dictionary with `NSExtensionActivationSupportsImageWithMaxCount` = 20, `NSExtensionActivationSupportsMovieWithMaxCount` = 5 (per D-15 — accepts photos, videos; Live Photos are covered by the image count since they have an image representation). Do NOT use TRUEPREDICATE per Pitfall 6.
- Bundle display name: "Watermark"

File 5: ShareExtension/ShareExtension.entitlements
- App Groups capability: array containing `group.com.watermark.app` (the placeholder App Group ID — developer must configure the actual ID in Xcode)

File 6: ShareExtension/ShareSheetView.swift
- Copy the existing App/Views/Share/ShareSheetView.swift into the extension target. It is a small file (33 lines), uses only SwiftUI + UIKit (both available in extensions). The excludedActivityTypes already excludes `.saveToCameraRoll` — perfect.

File 7: Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift
- Import Foundation
- Public struct with static methods per RESEARCH.md Pattern 5 (lines 473-489)
- `public static let suiteName = "group.com.watermark.app"` — placeholder; matches entitlements
- `private static let configKey = "watermarkConfiguration"`
- `public static func save(_ config: WatermarkConfiguration)` — use `JSONEncoder().encode(config)`. WARNING: WatermarkConfiguration is NOT currently Codable. It contains CGColor and CGFloat which are not directly Codable. Two approaches: (a) add Codable conformance to WatermarkConfiguration with custom encoding/decoding for CGColor (encode as RGBA components array) and CGFloat (native Codable), or (b) create a `CodableWatermarkConfig` surrogate struct.
  - Recommended approach: add `Codable` conformance directly to `WatermarkConfiguration` and `WatermarkLayer` with manual `encode(to:)` / `init(from:)` handling for CGColor. CGColor.encode: extract components as [CGFloat] array + colorSpace name string. CGColor.decode: reconstruct from components + colorSpace.
  - This requires modifying `WatermarkConfiguration.swift` and related model files in the WatermarkCore package to add Codable support.
  - If CGColor Codable is too complex, fallback: use a simpler encoding of just the config properties that are JSON-safe (text string, position rawValue, scale, padding, whiteFrame isEnabled boolean). This loses font/color/opacity fidelity on sync but gets the job done.
- `public static func load() -> WatermarkConfiguration?` — read data from UserDefaults(suiteName:suiteName) for key configKey, JSONDecoder().decode
- Write to `UserDefaults(suiteName: suiteName)` and guard with nil check
- Load: return nil if no data (extension defaults to standard config)

File 8: Move RenderingState to WatermarkCore
- Add the `RenderingState` enum (from App/Models/PhotoItem.swift lines 18-34) to `Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift` (before or after the ProcessingResult struct). This makes it accessible from both the main app and the extension.

Xcode project setup (manual, documented for developer):
1. File → New → Target → Share Extension → name "ShareExtension", embed in Watermark app
2. In ShareExtension target → General → Frameworks and Libraries → add WatermarkCore
3. In ShareExtension target → Signing & Capabilities → add App Groups → check "group.com.watermark.app"
4. In main app target → Signing & Capabilities → add App Groups → check "group.com.watermark.app" (must match)
5. In main app target → Info → URL Types → add URL scheme "watermark" (for D-16 fallback in Plan 03)
6. In ShareExtension target → Build Settings → set SWIFT_VERSION to 6.0
7. Verify the extension's Info.plist NSExtensionPrincipalClass matches `$(PRODUCT_MODULE_NAME).ShareViewController`
</action>
  <acceptance_criteria>
- ShareExtension/ShareViewController.swift exists with `UIHostingController<ShareExtensionRootView>` as child VC, constrains to all 4 edges
- ShareExtension/ShareExtensionRootView.swift exists with VStack(previewArea + controlsArea) layout, `ControlsView` binding, error alert, share sheet presentation
- ShareExtension/ShareExtensionViewModel.swift exists with `@Observable @MainActor final class`, `WatermarkEngine.shared.process()` delegation, `loadSharedMedia(from:)` using NSItemProvider, `RenderingState` usage, `completeRequest` closure
- ShareExtension/Info.plist exists with `NSExtensionActivationSupportsImageWithMaxCount=20`, `NSExtensionActivationSupportsMovieWithMaxCount=5`
- ShareExtension/ShareExtension.entitlements exists with App Groups array containing `group.com.watermark.app`
- ShareExtension/ShareSheetView.swift exists (copy of App/Views/Share/ShareSheetView.swift)
- ShareExtensionViewModel.swift contains `config: WatermarkConfiguration` with AppGroupConfigSync load on init
- AppGroupConfigSync.swift exists at Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift, save/load static methods
- RenderingState enum is in a WatermarkCore model file (e.g., ProcessingResult.swift)
- WatermarkConfiguration and WatermarkLayer have Codable conformance or a surrogate encoding approach in AppGroupConfigSync
- grep -c "NSExtensionActivationSupportsImageWithMaxCount" ShareExtension/Info.plist returns 1
- grep -c "UIHostingController" ShareExtension/ShareViewController.swift returns ≥ 1
- grep -c "import WatermarkCore" ShareExtension/ShareExtensionViewModel.swift returns 1
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Wave 0 must create ShareExtension build validation: verify `xcodebuild -project Watermark.xcodeproj -scheme ShareExtension -destination 'platform=iOS Simulator,name=iPhone 16' build 2>&1` compiles without errors after manual Xcode project setup</automated>
  </verify>
  <done>Share extension target files exist and follow the pattern: UIHostingController hosts SwiftUI root view, ViewModel delegates to WatermarkEngine.shared.process(), NSItemProvider loads shared photos, ControlsView reused, ShareSheetView presented, completeRequest called after dismiss</done>
</task>

<task type="auto">
  <name>Task 2: Wire NSItemProvider photo loading, WatermarkEngine integration, and share sheet flow — end-to-end Configure→Render→Share</name>
  <files>ShareExtension/ShareExtensionViewModel.swift, ShareExtension/ShareExtensionRootView.swift, ShareExtension/ShareViewController.swift</files>
  <read_first>
ShareExtension/ShareExtensionViewModel.swift — file being modified; read current state after Task 1
App/ViewModels/WatermarkViewModel.swift — reference for engine delegation, debounced preview, render-and-share flow, cleanup, layer management (lines 35-205)
App/Models/PhotoItem.swift — RenderingState enum definition (now also in WatermarkCore after Task 1)
Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift — process(sourceURL:config:) async throws -> ProcessingResult signature
Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessingResult.swift — ProcessingResult struct (url, data, outputUTI)
Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift — createTempFile(uti:) and cleanup(url:) methods
</read_first>
  <action>
Complete the NSItemProvider photo loading and full Configure→Render→Share flow in ShareExtensionViewModel.

NSItemProvider photo loading (D-13 — existing WatermarkEngine):
- Implement `func loadSharedMedia(from provider: NSItemProvider) async`:
  1. Verify `provider.hasItemConformingToTypeIdentifier(UTType.image.identifier)` — throw or show error if not
  2. Load item using `withCheckedThrowingContinuation` with `provider.loadItem(forTypeIdentifier: UTType.image.identifier) { (item, error) in ... }`
  3. Handle both `URL` and `Data` return types (item as? URL → read Data(contentsOf:), item as? Data → use directly)
  4. Write to temp file: `FileManager.default.temporaryDirectory.appendingPathComponent("shared_photo_\(UUID().uuidString).jpg")`
  5. Set `sourceURL = destURL`, `isLoadingMedia = false`
  6. Validate: if sourceURL is nil or file doesn't exist, set error
  7. On success: call `await generatePreview()`
  8. On any error: set `errorMessage`, `showError = true`, `isLoadingMedia = false`
- The temp file URL from NSItemProvider is ephemeral — copy to extension sandbox immediately inside the completion handler (per Pitfall 5 — RESEARCH.md lines 593-598)

Debounced preview generation (D-03 for photos — uses existing WatermarkEngine):
- Implement `func generatePreview() async` — exact same pattern as WatermarkViewModel lines 88-102:
  1. Guard `isGeneratingPreview` and `sourceURL != nil`
  2. Set `isGeneratingPreview = true`; defer `isGeneratingPreview = false`
  3. `try? await Task.sleep(for: .milliseconds(350))` — debounce
  4. Guard `!Task.isCancelled`
  5. Call `let result = try? await engine.process(sourceURL: sourceURL!, config: config)`
  6. Load result url data into `UIImage(data:)` → set `previewImage`
  7. Errors are silently ignored for preview (preview is best-effort)

Full-resolution render and share (D-06: Configure → Render → Share):
- Implement `func renderAndPrepareShare() async` — mirror WatermarkViewModel lines 104-122:
  1. Guard `sourceURL != nil`
  2. Set `renderingState = .rendering`
  3. Call `let result = try await engine.process(sourceURL: sourceURL!, config: config)`
  4. On success: `fullResResult = result`, `renderingState = .done`, update `previewImage` from result data
  5. On error: `renderingState = .error(error)`, `errorMessage = error.localizedDescription`, `showError = true`

Share sheet presentation (D-06: share sheet opens within extension):
- Implement `func presentShareSheet()`: guard `renderingState == .done`, set `showShareSheet = true`
- Implement `func handleShareDismiss()`: cleanup temp file via `TempFileManager.cleanup(url:)`, set `fullResResult = nil`, `renderingState = .idle`, call `completeRequest?()` per D-07 (one-shot — extension closes after share)

Layer management (D-05: full watermarking UI in extension):
- Implement `addLogoLayer(pngData:)`, `removeLayer(at:)`, `updateLayerPosition(at:position:)`, `updateLayerScale(at:scale:)`, `toggleWhiteFrame()`, `whiteFrameEnabled` — identical implementations to WatermarkViewModel lines 149-205. These operate on `config.watermarks` array and `config.whiteFrame`.

Config sync (D-08):
- Add `didSet` observer on config: whenever config changes, call `AppGroupConfigSync.save(config)`. Since this is an @Observable class, use a `willSet`/`didSet` on the stored property or call save in each mutation method. Simplest: override `config` with a computed property backed by a stored `_config`:
  ```swift
  private var _config: WatermarkConfiguration = ...
  var config: WatermarkConfiguration {
      get { _config }
      set { _config = newValue; AppGroupConfigSync.save(newValue) }
  }
  ```

Root view wiring:
- ShareExtensionRootView: The Share button calls `viewModel.renderAndPrepareShare()` on tap. The button should show "Processing..." and be disabled when `renderingState == .rendering`. When `renderingState == .done`, button text is "Share". On tap when done, calls `viewModel.presentShareSheet()`.
- Share sheet dismiss handler: `viewModel.handleShareDismiss()` (one-shot per D-07).

Controls sharing:
- Reuse existing ControlsView from the main app. The simplest approach: extract ControlsView to the WatermarkCore package. If that's impractical, create a thin wrapper in the extension that binds to the same config properties. 
- ACTUALLY: ControlsView from App/Views/Controls/ControlsView.swift imports WatermarkCore (for WatermarkConfiguration, WatermarkLayer types) and Observation (for @Observable). It accepts `@State var viewModel: WatermarkViewModel`. To reuse it:
  Option A: Make ControlsView generic over a protocol that both ViewModels conform to. Create `protocol WatermarkConfigurable` with all the config/layer methods both ViewModels need.
  Option B: Copy ControlsView and its sub-views into the extension target. All sub-views (TextWatermarkInputView, PositionGridView, ScaleStepperView, LogoPickerView, WhiteFrameToggleView, LayerListView) would also need to be copied or shared.
  Option C: Move all control views into the WatermarkCore package (they only depend on WatermarkCore models, SwiftUI, and Observation — all available in extensions).
  RECOMMENDED: Option C — move `App/Views/Controls/` directory into WatermarkCore package under a new `Sources/WatermarkCore/UI/` directory. This makes all control views available to both targets. Update WatermarkCore Package.swift to ensure SwiftUI and Observation are available (they're system frameworks, no manifest changes needed for Swift packages that target iOS). Then both main app and extension can import the same views.
  FALLBACK: If moving files is too complex, copy ControlsView + all sub-views into the ShareExtension target (6 files total). Accept the code duplication for Plan 01; refactor to shared package in Plan 03.

Preview area in ShareExtensionRootView:
- ZStack with: (a) if `viewModel.isLoadingMedia`: ProgressView("Loading..."), (b) if `viewModel.previewImage != nil`: Image(uiImage:) with `.resizable().scaledToFit()`, (c) if error and no preview: Text viewModel.errorMessage
</action>
  <acceptance_criteria>
- ShareExtensionViewModel.loadSharedMedia(from:) uses `provider.loadItem(forTypeIdentifier: UTType.image.identifier)`, handles Data and URL returns, writes to extension temp dir, sets sourceURL
- ShareExtensionViewModel.generatePreview() debounces 350ms, calls engine.process, sets previewImage
- ShareExtensionViewModel.renderAndPrepareShare() sets renderingState to .rendering, calls engine.process, sets .done on success, .error on failure
- ShareExtensionViewModel.presentShareSheet() guards renderingState == .done
- ShareExtensionViewModel.handleShareDismiss() cleans up temp file, calls completeRequest?()
- Layer management methods (addLogoLayer, removeLayer, updateLayerPosition, updateLayerScale, toggleWhiteFrame) mirror WatermarkViewModel
- Config changes auto-save via AppGroupConfigSync.save
- Share button in root view shows "Processing..." when rendering, "Share" when done
- ControlsView (or equivalent) displays text input, position picker, scale stepper, logo picker, white frame toggle, layer list
- grep -c "loadItem.*UTType.image" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "engine.process" ShareExtension/ShareExtensionViewModel.swift returns ≥ 2 (preview + render)
- grep -c "completeRequest?" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
- grep -c "AppGroupConfigSync.save" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Requires manual verification on iOS simulator with share extension. Steps: 1) Open Photos app in simulator, 2) Select a photo, 3) Tap Share, 4) Verify "Watermark" appears in share sheet, 5) Tap Watermark, 6) Verify extension opens with photo preview, 7) Configure watermark text, 8) Tap Share, 9) Verify share sheet appears with watermarked image, 10) After sharing, extension closes</automated>
  </verify>
  <done>End-to-end photo watermarking via share extension works: user shares photo from Photos app → extension opens → preview shows watermarked photo → user configures watermark → taps Share → share sheet presents with output → extension closes after share</done>
</task>

<task type="auto">
  <name>Task 3: App Group config sync, URL scheme fallback, TempFileManager extension path, and Config Sync Codable implementation</name>
  <files>Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift, Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift, Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift</files>
  <read_first>
Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift — modify to implement proper Codable serialization
Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift — must add Codable conformance if not already done in Task 1
Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift — add directory parameter overload (existing pattern lines 16-25, 29-35)
Packages/WatermarkCore/Sources/WatermarkCore/Input/FormatDetector.swift — fileExtension(for:) static method used by TempFileManager
</read_first>
  <action>
Complete the remaining infrastructure:

1. TempFileManager extension path overload (used by extension to write to its own sandbox):
- Add new static method: `public static func createTempFile(uti: CFString, in directory: URL) throws -> URL`
- Uses the same filename pattern as the existing method: `"watermark_\(UUID().uuidString).\(FormatDetector.fileExtension(for: uti))"`
- Appends to the provided directory instead of cachesDirectory
- Does NOT throw if directory doesn't exist — caller validates directory

2. WatermarkConfiguration Codable conformance:
- Add `Codable` to `WatermarkConfiguration` struct declaration: `public struct WatermarkConfiguration: Sendable, Codable`
- Add `Codable` to `WatermarkLayer` enum: `public enum WatermarkLayer: Sendable, Codable`
- CGColor is not directly Codable. Implement custom encoding:
  - Encode CGColor as a JSON object: `{ "r": Float, "g": Float, "b": Float, "a": Float }`
  - Use `UnsafePointer` to get components from CGColor (safe: CGColor always has 4 components for RGBA)
  - Implement `init(from:)` for WatermarkConfiguration: manually decode, construct CGColor from RGBA values using `CGColor(srgbRed:green:blue:alpha:)`
- TextWatermarkInput must also be Codable: add Codable conformance with custom encoding for CGColor
- ImageWatermarkInput must be Codable: pngData is Data (native Codable), needs Codable conformance
- WhiteFrameConfig must be Codable: isEnabled is Bool (native Codable)
- WatermarkPosition: add `Codable` conformance (rawValue is Int, native Codable). May need to use `RawRepresentable` conformance.

3. AppGroupConfigSync implementation:
- `save(_:)`: encode config to JSON Data via `JSONEncoder()`, write to `UserDefaults(suiteName: suiteName)?.set(data, forKey: configKey)`. Guard with nil check on defaults and encode result.
- `load()`: read `UserDefaults(suiteName: suiteName)?.data(forKey: configKey)`, decode via `JSONDecoder()`. Return nil if any step fails (no data, decode error).
- Use `os_log` for load/save failures (not user-visible errors; graceful fallback).

4. Main app config sync integration:
- In WatermarkViewModel.swift init: after setting default config, try `if let saved = AppGroupConfigSync.load() { config = saved }`
- In WatermarkViewModel: add config didSet or save calls on mutation methods to sync to App Group
- This is a MODIFICATION to App/ViewModels/WatermarkViewModel.swift (existing file)

5. URL scheme for unsupported type fallback (D-16):
- In ShareExtensionViewModel, add a method `func openInMainApp()` that constructs a URL like `watermark://open` and calls `extensionContext?.open(url, completionHandler: nil)`
- Add a computed property `var unsupportedType: Bool` that is true when NSItemProvider has no matching image or movie UTI
- In ShareExtensionRootView, when `unsupportedType` is true, show a dialog/alert: "This file type isn't supported yet. Open in the Watermark app?" with "Open in App" and "Cancel" buttons. "Open in App" calls `viewModel.openInMainApp()`.
- Note: `NSExtensionContext.open(url:)` is the API for opening the containing app from an extension

6. Config sync on main app launch (D-08):
- WatermarkApp.swift or WatermarkViewModel init: load saved config from AppGroupConfigSync
- Save config to AppGroupConfigSync whenever WatermarkViewModel.config is mutated
- This ensures bidirectional sync: extension saves → main app reads; main app saves → extension reads

IMPORTANT Codable implementation details:
- CGColor encoding: write as `[r, g, b, a]` array of CGFloat
- CGColor decoding: `CGColor(red: green: blue: alpha:)` or `CGColor(srgbRed: green: blue: alpha:)`
- WatermarkLayer enum coding: use a CodingKeys enum with `type` discriminator field + associated value keys. Encode as: `{ "type": "text", "textConfig": {...}, "position": 5, "scale": 0.15 }`
- If full Codable is too complex, use a surrogate struct `CodableWatermarkConfig` in AppGroupConfigSync that has JSON-safe types only (String for text, Int for position rawValue, Double for scale, Bool for frame enabled). This loses font/color/opacity fidelity but is much simpler. The surrogate approach is acceptable for v1 config sync — most users want position/scale/text sync, not exact color values.
</action>
  <acceptance_criteria>
- TempFileManager has `createTempFile(uti:in:)` overload accepting a directory URL parameter
- WatermarkConfiguration, WatermarkLayer, WatermarkPosition conform to Codable (or AppGroupConfigSync uses Codable surrogate)
- AppGroupConfigSync.save() writes JSON-encoded WatermarkConfiguration to UserDefaults(suiteName:"group.com.watermark.app")
- AppGroupConfigSync.load() reads and decodes WatermarkConfiguration from shared UserDefaults; returns nil on failure/missing
- WatermarkViewModel loads config from AppGroupConfigSync on init and saves on mutation
- ShareExtensionViewModel saves config to AppGroupConfigSync on every mutation
- ShareExtensionViewModel.openInMainApp() constructs watermark://open URL and calls extensionContext?.open(url:)
- ShareExtensionRootView shows unsupported type dialog with "Open in App" / "Cancel" buttons
- grep -c "createTempFile.*in:" Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift returns ≥ 1
- grep -c "Codable" Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift returns ≥ 2 (struct + enum)
- grep -c "suiteName.*group.com.watermark.app" Packages/WatermarkCore/Sources/WatermarkCore/Storage/AppGroupConfigSync.swift returns ≥ 1
- grep -c "openInMainApp" ShareExtension/ShareExtensionViewModel.swift returns ≥ 1
  </acceptance_criteria>
  <verify>
    <automated>MISSING — Requires manual verification: 1) Configure watermark in main app, 2) Share a photo, 3) Verify extension shows saved config, 4) Change config in extension, 5) Close extension, 6) Open main app, 7) Verify config changes persisted. Also: 1) Try sharing an unsupported file type (e.g., PDF), 2) Verify "Open in App" dialog appears</automated>
  </verify>
  <done>Config sync works bidirectionally between main app and extension via App Group UserDefaults. Unsupported media types show a dialog offering to open in the main app. TempFileManager supports custom directory output paths.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Host App → Share Extension | NSItemProvider from untrusted sending app crosses into extension process |
| Extension → WatermarkCore Engine | File URL from NSItemProvider passed to rendering engine |
| Extension → App Group Container | WatermarkConfiguration serialized to shared UserDefaults; readable by main app |
| Extension → Share Sheet | Watermarked output file URL passed to UIActivityViewController |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Denial of Service | ShareExtensionViewModel.loadSharedMedia | mitigate | Validate file size before copying NSItemProvider data to extension sandbox. Reject files > 500MB (extension memory ceiling ~120MB). Use `loadItem` not `loadFileRepresentation` for photos (photos are small enough for in-memory loading). |
| T-03-02 | Elevation of Privilege | ShareExtensionViewModel.loadSharedMedia | mitigate | Copy NSItemProvider-provided URL data to extension sandbox temp directory immediately — never use the provided URL directly. The temp URL from NSItemProvider may point to a location outside the extension sandbox. Always write to `FileManager.default.temporaryDirectory`. |
| T-03-03 | Spoofing | AppGroupConfigSync | accept | App Group container restricted by Apple to apps signed with same team ID — OS-level enforcement. Deserialized WatermarkConfiguration is validated for scale (0.01-0.90), position enum values, and text string length < 500 chars before applying. Invalid config falls back to defaults. |
| T-03-04 | Information Disclosure | ShareExtensionRootView (share sheet) | mitigate | ShareSheetView excludes `.saveToCameraRoll` activity type. Watermarked temp file written to extension sandbox (not camera roll). Temp file cleaned up after share dismiss + 60s grace. |
| T-03-05 | Elevation of Privilege | ShareViewController | mitigate | NSExtensionActivationRule restricts accepted types to public.image (max 20) and public.movie (max 5). The extension does not accept arbitrary file types. Using the dictionary form (not TRUEPREDICATE) per App Store requirements. |
</threat_model>

<verification>
After manual Xcode project setup: build ShareExtension scheme (`xcodebuild -project Watermark.xcodeproj -scheme ShareExtension -destination 'platform=iOS Simulator,name=iPhone 16' build`). Run the extension on simulator: share a photo from Photos app, verify extension opens, watermark UI appears, watermark renders, share sheet presents, extension closes after share.
</verification>

<success_criteria>
1. Share extension target compiles and runs on iOS 18 simulator
2. User can share a photo from Photos → Watermark extension opens with loading indicator → photo preview displays
3. User can configure text watermark (text, position, scale), image/logo watermark, and white frame toggle — all controls functional
4. User taps "Share" button → engine renders full-resolution watermarked output → iOS share sheet opens with watermarked image
5. Upon share sheet dismiss (or cancel), extension calls completeRequest and closes (D-07 one-shot)
6. WatermarkConfiguration set in extension persists to App Group UserDefaults; main app loads it on next launch
7. Share extension appears in share sheet for any photo (public.image UTI) shared from Photos, Files, Safari, etc.
</success_criteria>

<output>
Create `.planning/phases/03-video-processing-share-extension/03-01-SUMMARY.md` when done
</output>
