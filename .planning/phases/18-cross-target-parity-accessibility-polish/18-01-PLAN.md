---
phase: 18-cross-target-parity-accessibility-polish
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/TestHelpers/SnapshotTestViewModel.swift
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/ExtensionSnapshotTests.swift
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/share-ext-idle.png
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/share-ext-preview.png
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/share-ext-multi-item.png
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/photos-ext-idle.png
  - Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/photos-ext-preview.png
autonomous: true
requirements:
  - XTG-01
  - XTG-02
user_setup: []

must_haves:
  truths:
    - "ShareExtensionRootView snapshot renders correctly with ControlsView visible at 60/40 layout on iPhone 16 Pro Max (430×932)"
    - "PhotosExtensionRootView snapshot renders correctly with Done toolbar button visible at 60/40 layout"
    - "All 5 snapshot comparison tests pass when executed via xcodebuild test"
    - "Snapshot reference images are committed to the repo and the test suite passes against them"
  artifacts:
    - path: "Packages/WatermarkCore/Tests/WatermarkCoreTests/TestHelpers/SnapshotTestViewModel.swift"
      provides: "Test-only ViewModel conforming to WatermarkConfigurable & Observable with pre-populated config"
    - path: "Packages/WatermarkCore/Tests/WatermarkCoreTests/ExtensionSnapshotTests.swift"
      provides: "5 snapshot tests covering both extension root views at idle and rendered states"
      exports: ["ExtensionSnapshotTests"]
    - path: "Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/"
      provides: "Reference PNG images for snapshot comparison"
      contains: "share-ext-idle.png"
  key_links:
    - from: "SnapshotTestViewModel"
      to: "WatermarkConfigurable protocol"
      via: "protocol conformance with @Observable"
      pattern: "final class SnapshotTestViewModel.*WatermarkConfigurable"
    - from: "ExtensionSnapshotTests"
      to: "ShareExtensionRootView"
      via: "UIHostingController rendering at 430×932"
      pattern: "ShareExtensionRootView\\(viewModel:"
    - from: "ExtensionSnapshotTests"
      to: "PhotosExtensionRootView"
      via: "UIHostingController in UINavigationController rendering at 430×932"
      pattern: "PhotosExtensionRootView\\(viewModel:"
---

<objective>
Build automated snapshot test infrastructure and verify both extension root views render correctly with the redesigned `ControlsView`.

**Purpose:** Prove XTG-01 and XTG-02 — the redesigned shared `ControlsView` renders and functions correctly in the Share Extension and Photos Edit Extension — via XCTest snapshot tests per D-01, not manual verification. This gates all subsequent Phase 18 work because it establishes baseline extension rendering correctness.

**Output:** SnapshotTestViewModel (test-only mock), SnapshotRenderer helper with pixel-difference comparison, 5 snapshot tests with committed reference images, runnable via `xcodebuild test -scheme WatermarkCore`.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-cross-target-parity-accessibility-polish/18-CONTEXT.md
@.planning/phases/18-cross-target-parity-accessibility-polish/18-RESEARCH.md
@Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift
@Packages/WatermarkCore/Tests/WatermarkCoreTests/TestHelpers/MockRenderingViewModel.swift
@ShareExtension/ShareExtensionRootView.swift
@PhotoEditExtension/PhotosExtensionRootView.swift
@Packages/WatermarkCore/Tests/WatermarkCoreTests/InspectorSheetViewTests.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SnapshotTestViewModel + SnapshotRenderer Helper</name>
  <files>Packages/WatermarkCore/Tests/WatermarkCoreTests/TestHelpers/SnapshotTestViewModel.swift</files>
  <read_first>Packages/WatermarkCore/Sources/WatermarkCore/UI/WatermarkConfigurable.swift</read_first>
  <read_first>Packages/WatermarkCore/Tests/WatermarkCoreTests/TestHelpers/MockRenderingViewModel.swift</read_first>
  <read_first>.planning/phases/18-cross-target-parity-accessibility-polish/18-RESEARCH.md#L482-L529</read_first>
  <behavior>
    - Test 1: SnapshotTestViewModel conforms to WatermarkConfigurable & Observable and compiles
    - Test 2: SnapshotTestViewModel has 2 watermark layers (text bottomRight + image bottomRight) pre-populated
    - Test 3: SnapshotTestViewModel has whiteFrame enabled (WhiteFrameConfig(isEnabled: true))
    - Test 4: SnapshotRenderer.render(view:size:scale:inNavigationController:) returns non-nil PNG Data for a simple Text view
    - Test 5: Pixel-difference comparison returns true when comparing identical images (0% difference)
    - Test 6: Pixel-difference comparison returns true when difference is below 2% tolerance threshold
    - Test 7: Pixel-difference comparison returns false when images differ significantly (e.g., different views)
  </behavior>
  <action>
    Create test-only infrastructure files. The plan produces two artifacts but both live in the same directory — create them together.

    **SnapshotTestViewModel** (`Tests/WatermarkCoreTests/TestHelpers/SnapshotTestViewModel.swift`):
    - `@MainActor @Observable final class SnapshotTestViewModel: WatermarkConfigurable`
    - Pre-populated `WatermarkConfiguration` per D-04: text watermark layer at `.bottomRight` with text "Sample Watermark" at scale 0.15 opacity 1.0, image/logo watermark layer at `.bottomRight` with scale 0.15 opacity 1.0 (use a 1×1 transparent PNG Data constant — the snapshot test validates layout not pixel content), `whiteFrame = WhiteFrameConfig(isEnabled: true)`, `outputFormat = .preserveSource`, `outputQuality = 1.0`
    - Stub all protocol methods (empty bodies for `renderAndPrepareShare()`, `presentShareSheet()`, etc.) — no spy counters needed for snapshot tests
    - `renderingState` settable: defaults to `.idle`, tests will set to `.done` for rendered states
    - `hasMultiplePhotos` property: defaults to `false` but settable (Share Extension multi-item test sets it to `true`)
    - `config.watermarks.count == 2`, `activeLayerIndex = 1`

    **SnapshotRenderer helper** (test-only struct, bottom of SnapshotTestViewModel.swift or separate file in TestHelpers):
    - `@MainActor static func render<V: View>(_ view: V, size: CGSize, scale: CGFloat, inNavigationController: Bool) throws -> Data`
    - Uses `UIHostingController(rootView: view.frame(width:size.width, height:size.height))` — NOT `ImageRenderer`, per RESEARCH.md Pitfall 1
    - When `inNavigationController == true`: wrap host in `UINavigationController(rootViewController: host)`, use nav.view.bounds for renderer bounds
    - `UIGraphicsImageRenderer` with `format.scale = scale` (3.0 for iPhone 16 Pro Max)
    - `container.view.layoutIfNeeded()` before render
    - `container.view.drawHierarchy(in: afterScreenUpdates: true)` — NOT `ImageRenderer`
    - Returns `image.pngData()` or throws `SnapshotError.renderingFailed`
    - `enum SnapshotError: Error { case renderingFailed }` nested in helper

    **Pixel-difference comparator** (same file):
    - `@MainActor static func compare(actual: Data, reference: Data, pixelTolerance: Double = 0.02) -> Bool`
    - Load both as `UIImage(data:) → .cgImage`, guard same width/height
    - For each pixel (4-byte RGBA): compare actual vs reference, count different pixels
    - Returns `Double(differentPixelCount) / Double(totalPixels) <= pixelTolerance`
    - Use `CGContext` with `CGBitmapInfo.byteOrder32Big` for pixel data extraction — standard Core Graphics pixel walk
    - Both images must have `bitsPerComponent == 8` — guard, return false otherwise

    Per RESEARCH.md: 2% pixel tolerance (planner's discretion) handles antialiasing/font rendering drift across OS versions while catching genuine layout regressions.
  </action>
  <verify>
    <automated>cd Packages/WatermarkCore && xcodebuild test -scheme WatermarkCore -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:WatermarkCoreTests/ExtensionSnapshotTests 2>&1 | grep -c "passed" | xargs test 0 -lt</automated>
  </verify>
  <acceptance_criteria>
    - SnapshotTestViewModel compiles and conforms to `WatermarkConfigurable & Observable`
    - SnapshotRenderer.render() produces valid PNG Data for a simple `Text("hello")` SwiftUI view at 100×50
    - SnapshotRenderer.render() with `inNavigationController: true` produces valid PNG Data (wrapping does not crash)
    - Pixel comparator returns true for identical images, false for clearly different images
    - SnapshotError.renderingFailed enum exists
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Extension Snapshot Tests (5 Tests + Reference Images)</name>
  <files>
    Packages/WatermarkCore/Tests/WatermarkCoreTests/ExtensionSnapshotTests.swift
    Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/share-ext-idle.png
    Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/share-ext-preview.png
    Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/share-ext-multi-item.png
    Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/photos-ext-idle.png
    Packages/WatermarkCore/Tests/WatermarkCoreTests/__Snapshots__/photos-ext-preview.png
  </files>
  <read_first>Packages/WatermarkCore/Tests/WatermarkCoreTests/TestHelpers/SnapshotTestViewModel.swift</read_first>
  <read_first>ShareExtension/ShareExtensionRootView.swift</read_first>
  <read_first>PhotoEditExtension/PhotosExtensionRootView.swift</read_first>
  <read_first>.planning/phases/18-cross-target-parity-accessibility-polish/18-RESEARCH.md#L270-L281</read_first>
  <action>
    Create the test suite and reference images. Per D-01: automated snapshot tests. Per D-02: full root view snapshots at extension 60/40 layout. Per D-06: 5 snapshots total across 3 states.

    **ExtensionSnapshotTests.swift** (`Tests/WatermarkCoreTests/ExtensionSnapshotTests.swift`):
    - `import Testing; import SwiftUI; @testable import WatermarkCore`
    - All tests are `@MainActor` (ImageRenderer + UIHostingController require main actor)
    - Use Swift Testing framework (`@Test` / `#expect`), matching existing test style (see `InspectorSheetViewTests.swift`)

    **Snapshot recording mode:** Add a compile-time flag pattern:
    - `private static let recordMode: Bool` — set to `false` by default
    - When `recordMode == true`: render and write PNG to `__Snapshots__/` directory, test passes
    - When `recordMode == false`: render, load reference PNG from test bundle, compare with 2% tolerance
    - Reference images are committed to repo — tests run against committed references

    **Helper methods in test struct:**
    - `func snapshotPath(_ name: String) -> URL` — resolves `__Snapshots__/\(name).png` relative to test file's source directory (use `#filePath` to derive)
    - `func assertSnapshot(name: String, view: some View, inNavigationController: Bool = false)` — renders view at 430×932 scale 3.0, compares against reference

    **Test 1 — Share Extension: idle state (empty/loading state before media loads):**
    - Create `SnapshotTestViewModel` with `renderingState = .idle`, `config.watermarks = []` (empty — simulates no media)
    - Render `ShareExtensionRootView(viewModel: vm)` at 430×932
    - Reference: `share-ext-idle.png`
    - `inNavigationController: false` (ShareExtensionRootView has no toolbar)

    **Test 2 — Share Extension: preview rendered (controls visible):**
    - Create `SnapshotTestViewModel` with `renderingState = .done` (to show controls), full pre-populated config (2 watermarks, white frame)
    - Set `vm.previewImage` — but wait, `SnapshotTestViewModel` doesn't have `previewImage` property. The `WatermarkConfigurable` protocol doesn't include `previewImage`.
    - **Resolution:** SnapshotTestViewModel needs additional properties beyond WatermarkConfigurable for preview rendering: `var previewImage: UIImage?`, `var isVideo: Bool`, `var isLoadingMedia: Bool`, `var isMultiItem: Bool`, `var currentItemIndex: Int`, `var totalItemCount: Int`, `var multiItemProgress: String`, `var showHDRWarning: Bool`, `var hdrWarningMessage: String?`, `var showAudioWarning: Bool`. The extension root views access these directly on their specific ViewModel types (ShareExtensionViewModel / PhotosExtensionViewModel). Since our test ViewModel is generic, we need these properties to be settable on SnapshotTestViewModel.
    - **Add to SnapshotTestViewModel (Task 1):** `var previewImage: UIImage? = nil`, `var isVideo: Bool = false`, `var isLoadingMedia: Bool = false`, `var isMultiItem: Bool = false`, `var currentItemIndex: Int = 0`, `var totalItemCount: Int = 0`, `var multiItemProgress: String = ""`, `var showHDRWarning: Bool = false`, `var hdrWarningMessage: String? = nil`, `var showAudioWarning: Bool = false`, `var sourceURL: URL? = nil`, `var previewIdentifier: String = UUID().uuidString`
    - For Test 2: set `previewImage = UIImage(systemName: "photo.fill")`, `renderingState = .done`
    - Reference: `share-ext-preview.png`

    **Test 3 — Share Extension: multi-item progress bar:**
    - Same VM as Test 2 plus: `isMultiItem = true`, `currentItemIndex = 2`, `totalItemCount = 5`, `multiItemProgress = "Item 3 of 5"`
    - Reference: `share-ext-multi-item.png`

    **Test 4 — Photos Extension: idle state:**
    - Create fresh SnapshotTestViewModel with `renderingState = .idle`, empty watermarks
    - Render `PhotosExtensionRootView(viewModel: vm)` at 430×932
    - `inNavigationController: true` — per RESEARCH.md Pitfall 1, the Done toolbar button only renders inside UINavigationController
    - Reference: `photos-ext-idle.png`

    **Test 5 — Photos Extension: preview rendered:**
    - Same VM as Test 2 (full config + preview image)
    - Render `PhotosExtensionRootView(viewModel: vm)` at 430×932
    - `inNavigationController: true`
    - Reference: `photos-ext-preview.png`

    **Creating reference images (first run):**
    - Run test suite once with `recordMode = true` to generate initial reference PNGs in `__Snapshots__/`
    - Set `recordMode = false` for normal validation runs
    - The `__Snapshots__/` directory must be added to the test target's resources or accessed via file path (use `#filePath`-relative resolution — do not rely on Bundle resources since these are raw PNGs committed alongside the test file)

    Per D-05: all snapshots at 430pt × 932pt (iPhone 16 Pro Max). Layout is size-class driven, not pixel-dependent — this single size covers all iPhone variants.

    Per RESEARCH.md Q1 resolution: reference images are committed to the repo (not generated on first CI run). This matches standard snapshot testing conventions and avoids per-machine recording passes.
  </action>
  <verify>
    <automated>cd Packages/WatermarkCore && xcodebuild test -scheme WatermarkCore -destination 'platform=iOS Simulator,name=iPhone 16 Pro' -only-testing:WatermarkCoreTests/ExtensionSnapshotTests 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - All 5 `@Test` functions exist in `ExtensionSnapshotTests.swift` and compile
    - `recordMode = true` produces 5 PNG files in `__Snapshots__/` directory, each non-empty (>1KB)
    - `recordMode = false` with committed reference images: all 5 tests pass with `#expect(actual == true)` for pixel comparison
    - `xcodebuild test -scheme WatermarkCore -only-testing:WatermarkCoreTests/ExtensionSnapshotTests` exits 0
    - Share extension snapshots: idle state shows ControlsView content (VStack layout visible at bottom 40%), preview state shows preview image + ControlsView with controls, multi-item state shows progress bar + ControlsView
    - Photos extension snapshots: idle + preview states, both have Done button visible in toolbar area
    - `__Snapshots__/` directory contains exactly 5 PNG files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test → Test File System | Snapshot reference images are read from repo and written in record mode |
| Test VM → Snapshot PNG | SnapshotTestViewModel uses synthetic config data only — no real user photos, PII, or secrets |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-18-01 | Information Disclosure | SnapshotTestViewModel config | accept | Pre-populated config uses synthetic "Sample Watermark" text and 1×1 transparent PNG — no real user data, no PII, no secrets. Test-only code, never shipped. |
| T-18-02 | Tampering | Reference PNG files in __Snapshots__/ | accept | Reference images committed to git alongside tests — git history provides tamper detection. If references are corrupted, tests fail with clear pixel-difference output; investigation is straightforward. |
| T-18-03 | Information Disclosure | VoiceOver-accessible test UI | accept | Snapshot rendering captures pixel output only — not accessibility tree. No VoiceOver-visible data leakage path. Test VM uses synthetic labels only. |
</threat_model>

<verification>
- `xcodebuild test -scheme WatermarkCore -destination 'platform=iOS Simulator,name=iPhone 16 Pro'` passes with ExtensionSnapshotTests included
- All 5 snapshot tests produce a pass/fail result (not skipped, not runtime-crashed)
- `git status` shows 5 committed PNG files in `__Snapshots__/`
</verification>

<success_criteria>
Snapshot test infrastructure complete when:
- [ ] `SnapshotTestViewModel` compiles and conforms to `WatermarkConfigurable & Observable`
- [ ] `SnapshotRenderer.render()` produces valid PNG Data via `UIHostingController` + `UIGraphicsImageRenderer`
- [ ] Pixel comparator handles identical, similar (within tolerance), and clearly different images
- [ ] 5 snapshot tests exist covering Share Ext (idle + preview + multi-item) and Photos Ext (idle + preview)
- [ ] All 5 tests pass against committed reference images
- [ ] `xcodebuild test -scheme WatermarkCore` exits 0
</success_criteria>

<output>
Create `.planning/phases/18-cross-target-parity-accessibility-polish/18-01-SUMMARY.md` when done
</output>
