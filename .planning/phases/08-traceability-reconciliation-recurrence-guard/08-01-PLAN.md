---
phase: 08-traceability-reconciliation-recurrence-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.0-REQUIREMENTS.md
autonomous: true
requirements:
  - TRACE-01

must_haves:
  truths:
    - "Every v1.0 validated requirement in the archive has a checked [x] checkbox — 0 unmapped, 0 stale entries"
    - "All 15 drifted requirement checkboxes/traceability entries are reconciled against MILESTONE-AUDIT.md evidence"
    - "The 7 shipped Phase 7 requirements (LIVE, SIGN, IMPS, SYSI) are reclassified from ## v2 Requirements into ## v1 Requirements"
    - "Coverage counts reflect the corrected structure: v1 = 35, v2 = 6"
    - "A Reconciliation Note at the top documents what changed, when, and against what evidence"
  artifacts:
    - path: ".planning/milestones/v1.0-REQUIREMENTS.md"
      provides: "Reconciled v1.0 requirements archive"
      contains: "## Reconciliation Note"
      contains: "[x] **EXPT-01**:"
      contains: "[x] **LIVE-01**:"
      contains: "[x] **SIGN-01**:"
      contains: "v1 requirements: 35 total"
      contains: "v2 requirements: 6 total"
      contains: "| EXPT-01 | Phase 6 | Complete |"
      contains: "| LIVE-01 | Phase 7 | Complete |"
      contains: "| SYSI-02 | Phase 7 | Complete |"
      not_contains: "[ ] **EXPT-"
      not_contains: "Pending (v2)"
      not_contains: "Pending |"
  key_links:
    - from: "Reconciliation Note"
      to: "v1.0-MILESTONE-AUDIT.md"
      via: "explicit reference in note text"
      pattern: "MILESTONE-AUDIT\\.md"
    - from: "### Live Photos (LIVE)"
      to: "## v1 Requirements"
      via: "section placement after ### Video Export UX"
      pattern: "### Live Photos"
---

<objective>
Reconcile the archived v1.0 REQUIREMENTS.md traceability — flip 10 unchecked checkboxes, add 5 missing checkboxes, update 15 stale traceability table entries, reclassify 7 shipped Phase 7 requirements from v2 up into v1, fix coverage counts, and document the reconciliation.

Purpose: Close the documentation drift that left 10/35 v1.0 requirements unchecked at milestone close (retrospective Lesson 1 root cause). The archive must accurately reflect what shipped.
Output: A reconciled `.planning/milestones/v1.0-REQUIREMENTS.md` with every shipped requirement checked and correctly classified, committed as a single atomic commit per D-06.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/milestones/v1.0-REQUIREMENTS.md
@.planning/milestones/v1.0-MILESTONE-AUDIT.md
@.planning/phases/08-traceability-reconciliation-recurrence-guard/08-CONTEXT.md
@.planning/phases/08-traceability-reconciliation-recurrence-guard/08-PATTERNS.md
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Flip checkboxes, add missing checkboxes, update traceability table</name>
  <files>.planning/milestones/v1.0-REQUIREMENTS.md</files>
  <read_first>
    - .planning/milestones/v1.0-REQUIREMENTS.md — full file read (178 lines). Know the current state of every checkbox line, traceability table entry, and the exact ordering of sections before editing.
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md — full file read. Authoritative source of truth for each req's status (satisfied), phase (06 or 07), and code evidence. Every edit must be grounded in this file's `gaps.requirements[]` frontmatter array, not assumptions.
    - .planning/phases/08-traceability-reconciliation-recurrence-guard/08-CONTEXT.md — read D-05 and `<specifics>` for the exact list of 10 requirement IDs with their phases and the 4 category headings to move.
  </read_first>
  <action>
    Edit `.planning/milestones/v1.0-REQUIREMENTS.md` to fix all checkbox and traceability table drift. The file is 178 lines; edits are confined to the `## v1 Requirements` and `## Traceability` sections. Do NOT touch `## v2 Requirements` in this task (that's Task 2).

    All changes are deterministic — the target state is derived from MILESTONE-AUDIT.md `gaps.requirements[]` which confirms every listed req has status: satisfied.

    **Part A — Flip 10 unchecked v1 checkboxes `[ ]` → `[x]`:**
    These 10 lines currently have `- [ ] **ID**:`. Change `[ ]` to `[x]` for:
    - EXPT-01 (line 65), EXPT-02 (line 66), EXPT-03 (line 67)
    - COMP-01 (line 71), COMP-02 (line 72)
    - VIDX-01 (line 76), VIDX-02 (line 77), VIDX-03 (line 78)
    - LIVE-01 (line 82), LIVE-02 (line 83)

    **Part B — Update 8 traceability table Status entries `Pending` → `Complete`:**
    In the traceability table (lines 155-162), change the Status column from `Pending` to `Complete` for these 8 rows (phase column stays unchanged):
    - EXPT-01 (line 155), EXPT-02 (line 156), EXPT-03 (line 157)
    - COMP-01 (line 158), COMP-02 (line 159)
    - VIDX-01 (line 160), VIDX-02 (line 161), VIDX-03 (line 162)

    **Part C — Update 2 v2-status traceability entries `Pending (v2)` → `Complete`:**
    LIVE-01 and LIVE-02 currently have status `Pending (v2)` at lines 163-164. Change `Pending (v2)` to `Complete`. Phase column stays `Phase 7`.

    **Part D — For the 5 requirements in v2 that lack checkboxes entirely (SIGN×1, IMPS×2, SYSI×2):**
    The MILESTONE-AUDIT.md confirms all 5 are satisfied (Phase 7). In the `## v2 Requirements` section (lines 85-113), find each of these 5 lines that currently use the pattern `- **ID**:` (no `[ ]` checkbox wrapper) and change them to `- [x] **ID**:`:
    - SIGN-01 (line 103): `- **SIGN-01**:` → `- [x] **SIGN-01**:`
    - IMPS-01 (line 107): `- **IMPS-01**:` → `- [x] **IMPS-01**:`
    - IMPS-02 (line 108): `- **IMPS-02**:` → `- [x] **IMPS-02**:`
    - SYSI-01 (line 112): `- **SYSI-01**:` → `- [x] **SYSI-01**:`
    - SYSI-02 (line 113): `- **SYSI-02**:` → `- [x] **SYSI-02**:`

    **Part E — Update 5 v2 traceability entries `Pending (v2)` → `Complete`:**
    In the traceability table, change status for remaining entries at lines 165-169: SIGN-01, IMPS-01, IMPS-02, SYSI-01, SYSI-02 — change `Pending (v2)` to `Complete`. Phase stays `Phase 7`.

    **Important editing rules:**
    - Preserve exact indentation and spacing of every line you do NOT change.
    - The `**ID**:` part including the asterisks must remain exactly as-is — only change the checkbox brackets or Status column text.
    - Do not reorder lines or sections in this task. Category reclassification happens in Task 2.
  </action>
  <verify>
    <automated>grep -c '\[ \] \*\*' .planning/milestones/v1.0-REQUIREMENTS.md</automated>
    Expected: 0 (no unchecked boxes remain for shipped reqs). Also verify via:
    <automated>grep -c '\[x\] \*\*EXPT-01\*\*' .planning/milestones/v1.0-REQUIREMENTS.md</automated>
    Expected: 1. Repeat for all 15 req IDs.
  </verify>
  <acceptance_criteria>
    - grep `- \[ \]` in v1.0-REQUIREMENTS.md returns 0 matches for EXPT, COMP, VIDX, LIVE requirement lines (no shipped requirement remains unchecked)
    - grep `- \[x\] \*\*EXPT-01\*\*` returns exactly 1 match
    - grep `- \[x\] \*\*SIGN-01\*\*` returns exactly 1 match (was missing checkbox entirely)
    - grep `- \[x\] \*\*SYSI-02\*\*` returns exactly 1 match (was missing checkbox entirely)
    - grep `\| EXPT-01 \| Phase 6 \| Complete \|` returns exactly 1 match (was `Pending`)
    - grep `\| LIVE-01 \| Phase 7 \| Complete \|` returns exactly 1 match (was `Pending (v2)`)
    - grep `Pending (v2)` in the traceability table returns 0 matches (all 7 reclassified from `Pending (v2)`)
    - grep `Pending |` in the traceability table returns 0 matches (all 8 reclassified from `Pending`)
    - 15 total requirement IDs confirmed as checked + traceability Complete: EXPT×3, COMP×2, VIDX×3, LIVE×2, SIGN×1, IMPS×2, SYSI×2
  </acceptance_criteria>
  <done>All 15 shipped-but-drifted requirements have [x] checkboxes and traceability table status "Complete". No unchecked shipped requirement remains anywhere in the file.</done>
</task>

<task type="auto">
  <name>Task 2: Reclassify categories v2→v1, fix coverage counts, add Reconciliation Note, commit</name>
  <files>.planning/milestones/v1.0-REQUIREMENTS.md</files>
  <read_first>
    - .planning/milestones/v1.0-REQUIREMENTS.md — read the file in its post-Task-1 state. Verify Task 1 edits are present before proceeding.
    - .planning/milestones/v1.0-MILESTONE-AUDIT.md — re-read for evidence references used in the Reconciliation Note.
    - .planning/phases/08-traceability-reconciliation-recurrence-guard/08-CONTEXT.md — re-read D-05 category reclassification instructions and D-06 Reconciliation Note requirements.
    - .planning/phases/08-traceability-reconciliation-recurrence-guard/08-PATTERNS.md — read lines 171-267 for exact Reconciliation Note template text, coverage count target values, and footer format.
  </read_first>
  <action>
    This task completes the structural reorganization of the v1.0 archive. All edits are to `.planning/milestones/v1.0-REQUIREMENTS.md`. Work on the post-Task-1 state of the file.

    **Part A — Insert Reconciliation Note section (D-06):**
    Insert immediately after the archive header block (after line 7: `For current requirements, see...` and the `---` separator at line 8), before the `# Requirements: Watermark` heading (line 10). The inserted section follows the exact template from PATTERNS.md lines 187-199:
    - Heading: `## Reconciliation Note`
    - Sub-heading: `**Reconciled:** 2026-06-18`
    - Sub-heading: `**Evidence:** \`.planning/milestones/v1.0-MILESTONE-AUDIT.md\` (authoritative per-requirement audit)`
    - Bullet list of changes made:
      1. "Flipped 10 unchecked \`[ ]\` checkboxes → \`[x]\` for EXPT×3, COMP×2, VIDX×3 (Phase 6), LIVE×2 (Phase 7)"
      2. "Reclassified 7 shipped Phase 7 requirements from \`## v2 Requirements\` up into \`## v1 Requirements\`: LIVE×2, SIGN×1, IMPS×2, SYSI×2"
      3. "Moved 4 category headings (### Live Photos, ### Signature, ### Additional Import Sources, ### System Integration) from v2 → v1 section, appended after ### Video Export UX"
      4. "Updated traceability table: 8 \`Pending\` → \`Complete\`, 7 \`Pending (v2)\` → \`Complete\`"
      5. "Corrected Coverage counts: v1 = 35 (28 + 7), v2 = 6 (CUST×4 + BATC×2)"
      6. "Updated footer to 2026-06-18"

    **Part B — Move 4 category headings + their items from v2→v1 (D-05):**
    The `## v2 Requirements` section currently (post-Task-1) contains these categories in this order: Customization, Batch Processing, Signature, Additional Import Sources, System Integration. Move these 4 categories (with all their requirement items, now checked [x] per Task 1) wholesale from `## v2 Requirements` up into `## v1 Requirements`:
    - `### Signature (SIGN)` — includes SIGN-01 (now `- [x] **SIGN-01**:`)
    - `### Additional Import Sources (IMPS)` — includes IMPS-01, IMPS-02 (now `- [x]`)
    - `### System Integration (SYSI)` — includes SYSI-01, SYSI-02 (now `- [x]`)

    Also move `### Live Photos (LIVE)` — it's already physically in the v1 section but was listed as v2 in the traceability table. It stays put in v1 (where it already is on line 80-83).

    **Placement within v1:** Append these 3 moved categories (Signature, Additional Import Sources, System Integration) after `### Live Photos (LIVE)` (line 83) and before `## v2 Requirements` (line 85), preserving this order:
    1. ### Live Photos (LIVE) — already in v1, stays
    2. ### Signature (SIGN) — moved from v2
    3. ### Additional Import Sources (IMPS) — moved from v2
    4. ### System Integration (SYSI) — moved from v2

    **Part C — Trim v2 Requirements to deferred-only (D-05):**
    After moving the 3 categories out, the `## v2 Requirements` section should retain only:
    - `### Customization` with CUST-01, CUST-02, CUST-03, CUST-04 (still `- **ID**:` — no checkbox, truly deferred)
    - `### Batch Processing` with BATC-01, BATC-02 (still `- **ID**:` — no checkbox, truly deferred)
    Rename the heading from `## v2 Requirements` to `## v2 Requirements (deferred)`. Add a brief intro line after the heading: `Deferred to future release. Not in current roadmap.`

    **Part D — Fix Coverage counts:**
    Locate the `**Coverage:**` block near the end of the file. Replace the current two lines:
    - Change `v1 requirements: 28 total (14 original + 14 new)` → `v1 requirements: 35 total (28 original + 7 reclassified from v2)`
    - Change `v2 requirements: 13 total (6 original + 7 new)` → `v2 requirements: 6 total (CUST×4 + BATC×2, truly deferred)`

    **Part E — Update footer:**
    Locate the `*Last updated:*` line (line ~178). Replace the date and description:
    - Change `2026-06-17` → `2026-06-18`
    - Change the description after the date to: `reconciled against v1.0-MILESTONE-AUDIT.md: 10 checkboxes flipped, 7 Phase 7 reqs reclassified v2→v1, traceability table corrected`

    **Part F — Single atomic commit per D-06:**
    Stage the file and commit with the exact locked commit message:
    ```
    docs(v1.0): reconcile archived REQUIREMENTS.md traceability
    ```
    Do NOT split this into multiple commits. D-06 mandates a single atomic commit.

    **Important:** Do NOT change any requirement descriptions, phase numbers in the traceability table, category heading names, or text outside the specified edits. The edit is structural reorganization and status correction only — not content rewriting.
  </action>
  <verify>
    <automated>grep -c '\[ \]' .planning/milestones/v1.0-REQUIREMENTS.md</automated>
    Expected: 0 (confirms no unchecked box anywhere in the file). Also:
    <automated>grep -c 'Pending (v2)' .planning/milestones/v1.0-REQUIREMENTS.md</automated>
    Expected: 0 (all reclassified). Also:
    <automated>grep -c 'Pending |' .planning/milestones/v1.0-REQUIREMENTS.md</automated>
    Expected: 0 (all updated to Complete).
  </verify>
  <acceptance_criteria>
    - `## Reconciliation Note` section exists near top of file (between line 7 `---` separator and line 10 `# Requirements:` heading)
    - Reconciliation Note contains the text `v1.0-MILESTONE-AUDIT.md` and `2026-06-18`
    - `### Signature (SIGN)` appears within `## v1 Requirements` section, NOT within `## v2 Requirements`
    - `### Additional Import Sources (IMPS)` appears within `## v1 Requirements` section
    - `### System Integration (SYSI)` appears within `## v1 Requirements` section
    - Category ordering within v1: Live Photos → Signature → Additional Import Sources → System Integration (in that sequence)
    - `## v2 Requirements (deferred)` heading exists (renamed from `## v2 Requirements`)
    - v2 section contains ONLY `### Customization` (CUST×4) and `### Batch Processing` (BATC×2)
    - v2 section does NOT contain SIGN, IMPS, SYSI, or LIVE (grep `-c 'SIGN-01'` within v2 section = 0)
    - Coverage block reads: `v1 requirements: 35 total (28 original + 7 reclassified from v2)`
    - Coverage block reads: `v2 requirements: 6 total (CUST×4 + BATC×2, truly deferred)`
    - Footer `*Last updated: 2026-06-18*` with correct description
    - `git log -1 --oneline` shows `docs(v1.0): reconcile archived REQUIREMENTS.md traceability`
    - No other files modified in this commit (verify with `git diff --name-only HEAD~1`)
  </acceptance_criteria>
  <done>The archived v1.0 REQUIREMENTS.md is fully reconciled: all shipped requirements are checked and classified under v1 with Complete status, Coverage counts are accurate, a Reconciliation Note documents the audit, and the file is committed as a single atomic commit per D-06.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manual editor → archive file | The human/agent editing the file is the sole source of changes. No automated tooling modifies the archive in this plan. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | v1.0-REQUIREMENTS.md manual edit | mitigate | All edits are grounded in MILESTONE-AUDIT.md authoritative evidence. Every checkbox flip and status change is determined by the audit's `gaps.requirements[]` frontmatter, not subjective judgment. Task acceptance criteria grep for exact strings. Single atomic commit provides clean rollback via `git revert`. |
| T-08-02 | Information Disclosure | Reconciliation Note | accept | The Reconciliation Note contains only summary counts and file references — no secrets, credentials, or PII. |
| T-08-03 | Tampering | Accidental edit of wrong section | mitigate | Task actions specify exact line numbers and content patterns for each edit. Grep-based acceptance criteria verify only the intended sections changed (check grep `Pending` = 0 but grep `Complete` increases). |
</threat_model>

<verification>
After both tasks complete, run the comprehensive verification:
```bash
# 1. Zero unchecked shipped requirements
grep -c '\[ \]' .planning/milestones/v1.0-REQUIREMENTS.md
# Expected: 0

# 2. Zero stale traceability statuses
grep -c 'Pending' .planning/milestones/v1.0-REQUIREMENTS.md
# Expected: 0 (all "Pending" and "Pending (v2)" gone)

# 3. All 15 reconciled IDs have [x] and Complete
for ID in EXPT-01 EXPT-02 EXPT-03 COMP-01 COMP-02 VIDX-01 VIDX-02 VIDX-03 LIVE-01 LIVE-02 SIGN-01 IMPS-01 IMPS-02 SYSI-01 SYSI-02; do
  grep -q "\[x\] \*\*${ID}\*\*" .planning/milestones/v1.0-REQUIREMENTS.md || echo "MISSING: [x] for $ID"
  grep -q "| ${ID} | Phase .* | Complete |" .planning/milestones/v1.0-REQUIREMENTS.md || echo "MISSING: Complete for $ID"
done

# 4. Structural correctness
grep -q '## Reconciliation Note' .planning/milestones/v1.0-REQUIREMENTS.md || echo "MISSING: Reconciliation Note"
grep -q 'v1 requirements: 35 total' .planning/milestones/v1.0-REQUIREMENTS.md || echo "WRONG: v1 count"
grep -q 'v2 requirements: 6 total' .planning/milestones/v1.0-REQUIREMENTS.md || echo "WRONG: v2 count"
grep -q '## v2 Requirements (deferred)' .planning/milestones/v1.0-REQUIREMENTS.md || echo "MISSING: deferred label"

# 5. Commit message
git log -1 --oneline | grep -q "reconcile archived REQUIREMENTS.md traceability" || echo "WRONG COMMIT MSG"
```
</verification>

<success_criteria>
1. Every v1.0 validated requirement in `.planning/milestones/v1.0-REQUIREMENTS.md` has a checked `[x]` checkbox and an accurate `Complete` traceability status — 0 unmapped, 0 stale entries
2. The 7 shipped Phase 7 requirements (LIVE-01/02, SIGN-01, IMPS-01/02, SYSI-01/02) are reclassified from v2 up into v1
3. The `## v2 Requirements` section is trimmed to only truly deferred items (CUST, BATC) and relabeled `## v2 Requirements (deferred)`
4. Coverage counts match: v1 = 35, v2 = 6
5. A Reconciliation Note section documents the audit date, evidence source, and changes made
6. Single atomic commit with the locked message: `docs(v1.0): reconcile archived REQUIREMENTS.md traceability`
</success_criteria>

<output>
Create `.planning/phases/08-traceability-reconciliation-recurrence-guard/08-01-SUMMARY.md` when done
</output>
