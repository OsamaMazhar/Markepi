---
phase: 02-main-app-photo-watermark-share
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - App/WatermarkApp.swift
  - App/ViewModels/WatermarkViewModel.swift
  - App/Models/PhotoItem.swift
  - App/Views/ContentView.swift
  - App/Views/PreviewArea/PreviewView.swift
  - App/Views/Controls/ControlsView.swift
  - App/Views/Controls/TextWatermarkInputView.swift
  - App/Views/Controls/PositionGridView.swift
  - App/Views/Controls/ScaleStepperView.swift
  - App/Views/Navigation/ThumbnailStripView.swift
  - App/Views/Share/ShareSheetView.swift
  - App/Views/Common/AsyncButton.swift
  - App/Views/Common/ErrorAlertModifier.swift
  - Watermark.xcodeproj/project.pbxproj
autonomous: true
requirements: [MEDI-01, WMRK-04, SHAR-01]

must_haves:
  truths:
    - "User can select photos from their library using PhotosPicker"
    - "User sees a real-time preview that updates when watermark text, position, or scale changes"
    - "User can share the watermarked photo via iOS share sheet without output saved to camera roll"
  artifacts:
    - path: "App/ViewModels/WatermarkViewModel.swift"
      provides: "@Observable @MainActor state coordinator for photo selection, watermark config, preview, and share"
      min_lines: 150
    - path: "App/Views/ContentView.swift"
      provides: "Root 60/40 split layout with PhotosPicker trigger and share sheet presentation"
      min_lines: 60
    - path: "App/Views/Controls/TextWatermarkInputView.swift"
      provides: "Multi-line text input for watermark text per D-11"
      min_lines: 30
    - path: "App/Views/Controls/PositionGridView.swift"
      provides: "3×3 grid of 9 position preset buttons"
      min_lines: 40
    - path: "App/Views/Controls/ScaleStepperView.swift"
      provides: "±5% scale stepper, range 0.01–0.90, accessibility fallback per D-08/D-18"
      min_lines: 25
    - path: "App/Views/Share/ShareSheetView.swift"
      provides: "UIViewControllerRepresentable wrapping UIActivityViewController"
      min_lines: 30
    - path: "App/Views/Common/AsyncButton.swift"
      provides: "Button with ProgressView spinner, disabled during async work per D-07"
      min_lines: 25
    - path: "App/Views/Navigation/ThumbnailStripView.swift"
      provides: "Horizontal scrollable thumbnail strip with current-index highlight per D-13"
      min_lines: 40
    - path: "App/Views/Common/ErrorAlertModifier.swift"
      provides: "SwiftUI .alert() bridging for engine errors per D-17"
      min_lines: 20
  key_links:
    - from: "App/Views/ContentView.swift"
      to: "App/ViewModels/WatermarkViewModel.swift"
      via: "@State var viewModel: WatermarkViewModel"
      pattern: "@State.*WatermarkViewModel"
    - from: "App/ViewModels/WatermarkViewModel.swift"
      to: "Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift"
      via: "async call to WatermarkEngine.shared.process(sourceURL:config:)"
      pattern: "engine\\.process\\(sourceURL:"
    - from: "App/Views/Share/ShareSheetView.swift"
      to: "Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift"
      via: "completionWithItemsHandler calls TempFileManager.cleanup(url:)"
      pattern: "TempFileManager\\.cleanup"
    - from: "App/Views/ContentView.swift"
      to: "App/Views/Share/ShareSheetView.swift"
      via: ".sheet(isPresented: $viewModel.showShareSheet)"
      pattern: "\\.sheet\\(isPresented:.*showShareSheet"
---

<objective>
Deliver the vertical slice: import photo → configure text watermark → see real-time preview → share without saving. This is the thinnest complete flow proving the full MEDI-01 + WMRK-04 + SHAR-01 loop with text watermarks only. Logo/image watermark, white frame, pinch gesture, and layer list are deferred to Plan 02-02.

Purpose: Every subsequent plan and phase extends a working end-to-end app, not a skeleton.
Output: A buildable iOS 18 app that links WatermarkCore, shows PhotosPicker on launch, renders debounced text watermarks, and shares via UIActivityViewController with temp file cleanup.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-main-app-photo-watermark-share/02-CONTEXT.md
@.planning/phases/02-main-app-photo-watermark-share/02-RESEARCH.md
@.planning/phases/02-main-app-photo-watermark-share/02-UI-SPEC.md
@Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessResult.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/TextWatermarkInput.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create App Target + ViewModel + PhotosPicker Import + ContentView Layout</name>
  <files>App/WatermarkApp.swift, App/ViewModels/WatermarkViewModel.swift, App/Models/PhotoItem.swift, App/Views/ContentView.swift, App/Views/PreviewArea/PreviewView.swift, App/Views/Controls/ControlsView.swift, Watermark.xcodeproj/project.pbxproj</files>
  <read_first>
    - Packages/WatermarkCore/Sources/WatermarkCore/Engine/WatermarkEngine.swift — understand process(sourceURL:config:) API signature
    - Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkConfiguration.swift — understand WatermarkConfiguration, WatermarkLayer enum, WatermarkPosition enum
    - Packages/WatermarkCore/Sources/WatermarkCore/Models/TextWatermarkInput.swift — understand TextWatermarkInput init params
    - Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessResult.swift — understand ProcessResult (url, data, outputUTI)
    - .planning/phases/02-main-app-photo-watermark-share/02-RESEARCH.md (lines 206-280, 550-736) — project structure pattern and full ViewModel code example
    - .planning/phases/02-main-app-photo-watermark-share/02-UI-SPEC.md (lines 38-89, 93-154) — screen architecture, view hierarchy, layout spec, picker trigger spec
    - .planning/phases/02-main-app-photo-watermark-share/02-CONTEXT.md — all D-xx decisions, specifically D-01 (auto-open picker), D-02 (multi-select), D-03 (thumbnail-first), D-09 (60/40 split), D-10 (large "+" button), D-14 (config persistence)
  </read_first>
  <action>
    **1. Create Xcode project:**
    Create `Watermark.xcodeproj/project.pbxproj` for an iOS 18 app with SwiftUI App lifecycle. The app target must link WatermarkCore as a local Swift Package (path: `Packages/WatermarkCore`). Enable App Groups capability with `group.com.watermark.app`. Set deployment target to iOS 18.0, Swift 6 language mode.

    **2. Create WatermarkApp.swift:**
    Create `App/WatermarkApp.swift` — `@main` struct conforming to `App`. Create a `@State WatermarkViewModel()` at the `WindowGroup` level and inject it via `.environment()`. Do NOT call any engine methods in the App body — all work is driven by user interaction in ContentView.

    **3. Create PhotoItem.swift:**
    Create `App/Models/PhotoItem.swift` — `Identifiable, Hashable, Sendable` struct:
    - `let id: UUID`
    - `let thumbnail: UIImage?` — 200px max thumbnail for strip display
    - `let sourceURL: URL` — temp file URL for engine input (original photo copied to temp)

    **4. Create WatermarkViewModel.swift (@Observable, @MainActor):**
    Create `App/ViewModels/WatermarkViewModel.swift` — `@Observable @MainActor final class` holding ALL mutable state. Include these properties and methods:

    *Properties:*
    - `selectedItems: [PhotosPickerItem] = []`
    - `photos: [PhotoItem] = []`
    - `currentIndex: Int = 0`
    - `config: WatermarkConfiguration` — initialized with one text layer: `.text(TextWatermarkInput(text: "", fontSize: 48, color: CGColor(gray: 1, alpha: 1), opacity: 1.0), position: .bottomRight, scale: 0.15)`
    - `previewImage: UIImage?`
    - `isGeneratingPreview: Bool = false`
    - `renderingState: RenderingState = .idle` (see RenderingState enum below)
    - `fullResResult: ProcessResult?`
    - `showPicker: Bool = false`
    - `showShareSheet: Bool = false`
    - `showCancelAlert: Bool = false`
    - `errorMessage: String?`
    - `showError: Bool = false`
    - `engine: WatermarkEngine` — use `WatermarkEngine.shared`

    *Computed:*
    - `currentPhoto: PhotoItem?` — returns `photos[currentIndex]` if valid, nil otherwise
    - `hasMultiplePhotos: Bool` — `photos.count > 1`
    - `previewIdentifier: String` — `"\(currentIndex)-\(configTextHash)"` where `configTextHash` is derived from config.watermarks + config.whiteFrame?.isEnabled (use a custom hash function or compute a string key from the text watermark's text+position+scale)

    *Methods (stub bodies — Task 2 implements preview, Task 3 implements share):*
    - `handleSelection(_ items: [PhotosPickerItem])` — async, per D-02: iterate items, load Data via `item.loadTransferable(type: Data.self)`, create 200px thumbnail via `CGImageSourceCreateThumbnailAtIndex(maxPixelSize: 200)` with `kCGImageSourceCreateThumbnailWithTransform: true` for EXIF orientation, copy original data to temp file via `FileManager.default.temporaryDirectory` with UUID filename (preserve original extension), append `PhotoItem(id: UUID(), thumbnail: thumb, sourceURL: tempURL)`. Load all items concurrently via `TaskGroup` or sequential `for-in` (discretion per RESEARCH.md — whichever avoids memory pressure). Set `photos = loaded` then `currentIndex = 0`. Set `showPicker = false`.

    - `goToNext()`, `goToPrev()` — bounds-checked currentIndex mutations
    - `requestCancel()` — set `showCancelAlert = true`
    - `confirmCancel()` — reset photos, currentIndex, fullResResult, renderingState; set `showPicker = true`

    *RenderingState enum (in same file or PhotoItem.swift):*
    ```swift
    enum RenderingState: Equatable {
      case idle
      case rendering
      case done
      case error(Error)
      static func == (lhs: RenderingState, rhs: RenderingState) -> Bool { ... }
    }
    ```

    *Engine integration:* `private let engine = WatermarkEngine.shared`

    **5. Create ContentView.swift:**
    Create `App/Views/ContentView.swift` — root view. Use `GeometryReader` for 60/40 split (D-09):
    - Top 60%: `PreviewAreaView(viewModel:)` — shows `Image(uiImage: previewImage)` when available, or the center "+" picker button when empty. The center picker button is a large pill (200×56px, 28px corner radius) with SF Symbol `photo.on.rectangle.angled` + "Add Photos" label, with `.ultraThinMaterial` background + system blue gradient, positioned via `.overlay(alignment: .center)`. When `hasPhotos`: compact button (44×44 circle) with SF Symbol `plus.rectangle.on.rectangle` positioned top-trailing corner of preview. Tapping either button sets `showPicker = true`.
    - Divider: `Color(.separator)` 1px
    - Bottom 40%: `ControlsView(viewModel:)` — ScrollView stub for this task (empty placeholder or "Controls" text; content added in Task 2)
    - `.photosPicker(isPresented:selection:maxSelectionCount:matching:)` modifier on the VStack: `isPresented` bound to `viewModel.showPicker`, `selection` bound to `viewModel.selectedItems`, `maxSelectionCount: 20`, `matching: .images`
    - In `onChange(of: selectedItems)`: call `viewModel.handleSelection(newItems)` — handleSelection resets showPicker internally
    - On `.onAppear`: if `viewModel.photos.isEmpty`, set `viewModel.showPicker = true` (D-01: auto-open on launch)
    - `.alert("Rendering Error", isPresented: $viewModel.showError)` with OK dismiss — placeholder for Task 3 wiring
    - `.confirmationDialog` for cancel multi-photo (D-15) — bound to `$viewModel.showCancelAlert`, destructive "Discard" calls `confirmCancel()`, cancel "Keep Editing"
    - `.sheet(isPresented: $viewModel.showShareSheet)` — placeholder for Task 3 wiring

    **6. Create PreviewView.swift:**
    Create `App/Views/PreviewArea/PreviewView.swift` — display-only view:
    - If `viewModel.previewImage != nil`: `Image(uiImage: preview)` with `.resizable().aspectRatio(contentMode: .fit).drawingGroup()` (Metal-backed)
    - If `viewModel.isGeneratingPreview`: overlay `ProgressView()` centered, dim background to 0.6 opacity (per UI-SPEC §3.2)
    - If no photo and no preview: show the center picker button (same as ContentView empty state)
    - Respect edgesIgnoringSafeArea(.top) for edge-to-edge preview display

    **7. Create ControlsView.swift:**
    Create `App/Views/Controls/ControlsView.swift` — `ScrollView(.vertical)` with `.scrollIndicators(.hidden)`. For Task 1: this is a stub with placeholder text "Controls". It will be populated by Task 2. Use `.padding(.horizontal, 16)` and respect bottom safe area.

    **IMPORTANT:** All SwiftUI views reference the ViewModel via a single `@State` or `@Bindable` property. Use `@State var viewModel = WatermarkViewModel()` in ContentView and pass as `@Bindable` to child views (iOS 17+ @Observable binding pattern). The ViewModel is created once at ContentView level, not recreated in children.
  </action>
  <verify>
    <automated>xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `Watermark.xcodeproj/project.pbxproj` exists and references WatermarkCore as local package dependency
    - `App/WatermarkApp.swift` exists with `@main` struct conforming to `App`
    - `App/Models/PhotoItem.swift` exists with `Identifiable, Hashable, Sendable` conformance and `id: UUID, thumbnail: UIImage?, sourceURL: URL` properties
    - `App/ViewModels/WatermarkViewModel.swift` exists with `@Observable @MainActor` class containing `selectedItems`, `photos`, `currentIndex`, `config`, `previewImage`, `renderingState`, `showPicker`, `handleSelection()`, `goToNext()`, `goToPrev()`
    - `App/Views/ContentView.swift` exists with GeometryReader 60/40 split, `.photosPicker()` modifier, center "+" picker button per D-10, auto-open on `.onAppear()` per D-01
    - `App/Views/PreviewArea/PreviewView.swift` exists with `Image(uiImage:)` display and loading overlay
    - `App/Views/Controls/ControlsView.swift` exists as ScrollView stub
    - Project compiles: `swift build --target WatermarkApp` exits 0
  </acceptance_criteria>
  <done>Xcode project builds. PhotosPicker auto-opens on launch. User can select 1-20 photos. ContentView shows 60/40 split with preview area displaying low-res thumbnails. Controls area is empty but scrollable.</done>
</task>

<task type="auto">
  <name>Task 2: Text Watermark Controls + Debounced Preview Rendering</name>
  <files>App/Views/Controls/ControlsView.swift, App/Views/Controls/TextWatermarkInputView.swift, App/Views/Controls/PositionGridView.swift, App/Views/Controls/ScaleStepperView.swift, App/ViewModels/WatermarkViewModel.swift</files>
  <read_first>
    - App/ViewModels/WatermarkViewModel.swift — current state (from Task 1), existing config structure and previewImage property
    - App/Views/Controls/ControlsView.swift — current stub (from Task 1)
    - Packages/WatermarkCore/Sources/WatermarkCore/Models/WatermarkPosition.swift — 9 case values and .translation() API for position labels
    - Packages/WatermarkCore/Sources/WatermarkCore/Models/TextWatermarkInput.swift — TextWatermarkInput(text:fontSize:color:opacity:) init signature
    - .planning/phases/02-main-app-photo-watermark-share/02-UI-SPEC.md (lines 180-206, 215-218) — text input spec, scale stepper spec
    - .planning/phases/02-main-app-photo-watermark-share/02-RESEARCH.md (lines 208-243, 396-438) — Pattern 1 (.task(id:) debounce), Pattern 7 (AsyncButton), full ViewModel preview method
  </read_first>
  <action>
    **1. Update WatermarkViewModel.swift — add preview generation:**
    Add `generatePreview() async` method:
    - Guard `let sourceURL = currentPhoto?.sourceURL`
    - Set `isGeneratingPreview = true`; defer `isGeneratingPreview = false`
    - Debounce: `try? await Task.sleep(for: .milliseconds(350))` (D-05 midpoint; .task(id:) auto-cancels if config changes)
    - Check `Task.isCancelled` after sleep — return if cancelled
    - Call `try? await engine.process(sourceURL: sourceURL, config: config)`
    - Load `UIImage(data: Data(contentsOf: result.url))` from result URL
    - Set `previewImage = uiImage` on @MainActor
    - Wrap in `Task<Void, Never> { ... }` and await its value so errors don't propagate

    **2. Wire preview in ContentView.swift:**
    Add `.task(id: viewModel.previewIdentifier)` modifier to the VStack (or PreviewAreaView) that calls `await viewModel.generatePreview()`. The `previewIdentifier` is a computed String on the ViewModel. IMPORTANT: Only generate preview when `currentPhoto != nil`. The `.task(id:)` auto-cancels previous tasks when `previewIdentifier` changes.

    **3. Implement previewIdentifier in WatermarkViewModel:**
    Compute a string that changes when any watermark config property changes. Since `WatermarkConfiguration` and its nested types may not be `Hashable`, build a manual identifier: `"\(currentIndex)-t:\(textWatermarkText)-pos:\(currentPosition)-s:\(currentScale)-wf:\(whiteFrameEnabled)"` where `textWatermarkText` is the text of the first text watermark layer, `currentPosition` is its position rawValue, `currentScale` is its scale, `whiteFrameEnabled` is `config.whiteFrame?.isEnabled`.

    **4. Create TextWatermarkInputView.swift:**
    Create `App/Views/Controls/TextWatermarkInputView.swift`:
    - Section label: "Watermark Text" in `.title3` `.semibold`
    - `TextEditor` (not TextField) bound to the first text watermark's `text` property, multi-line per D-11
    - Placeholder "Watermark text" when empty (use `Text` overlay with `.foregroundColor(.secondaryLabel)` that hides when text is non-empty)
    - Min height 80px (3 lines at body size), max height 120px (scrolls internally)
    - Font: `.body` (17px Dynamic Type)
    - On change: update `viewModel.config.watermarks[0]` with new TextWatermarkInput (new text, keep other params)
    - Max text length validation: truncate to 500 characters (RESEARCH.md anti-pattern: "Overly long text input crashing engine")
    - Disabled state (grayed out, 0.4 opacity) when `viewModel.currentPhoto == nil`

    **5. Create PositionGridView.swift:**
    Create `App/Views/Controls/PositionGridView.swift`:
    - Section label: "Position" in `.title3` `.semibold`
    - `LazyVGrid(columns: [GridItem(.flexible()), GridItem(.flexible()), GridItem(.flexible())], spacing: 8)` — 3 columns × 3 rows
    - 9 buttons, one per `WatermarkPosition` case (topLeft, topCenter, topRight, middleLeft, center, middleRight, bottomLeft, bottomCenter, bottomRight)
    - Each button: 40×40px square, `.bordered` style
    - Label: abbreviated position names: TL, TC, TR, ML, C, MR, BL, BC, BR
    - Selected: filled accent color background, white SF Symbol `checkmark` overlay
    - Unselected: `.bordered` style, system gray border, label in `.secondary`
    - Accessibility: full names via `.accessibilityLabel()` — "Top Left", "Top Center", "Top Right", "Middle Left", "Center", "Middle Right", "Bottom Left", "Bottom Center", "Bottom Right"
    - Tapping sets `viewModel.config.watermarks[0]` position to the selected enum value
    - Disabled when `viewModel.currentPhoto == nil`

    **6. Create ScaleStepperView.swift:**
    Create `App/Views/Controls/ScaleStepperView.swift`:
    - Section label: "Scale" in `.title3` `.semibold`
    - Display: `"Scale: \(Int(currentScale * 100))%"` in `.body` `.secondary`
    - `Stepper` with increment/decrement of 0.05 (±5% per D-08/D-18)
    - Range clamped to 0.01–0.90 (matching engine validation, ENGINE-02)
    - "-" button disabled at 0.01, "+" button disabled at 0.90
    - On change: updates `viewModel.config.watermarks[0]` scale
    - Always visible (D-18 accessibility fallback)
    - Disabled when `viewModel.currentPhoto == nil`

    **7. Update ControlsView.swift — populate with controls:**
    Replace stub with `Section`-based layout containing `TextWatermarkInputView`, `PositionGridView`, and `ScaleStepperView` in that order. Use `VStack(alignment: .leading, spacing: 20)` inside `ScrollView`. Minimum vertical padding at bottom for safe area.

    **IMPORTANT (D-05):** The preview rendering MUST use the SAME `WatermarkEngine.process()` call as the final output. The engine handles full-res — the preview just displays the result UIImage at whatever dimensions the engine produces. For low-res preview speed, the engine processes at full source resolution (it's designed to; thumbnail-first loading per D-03 already provides initial low-res display while engine renders). If preview rendering is too slow at full resolution, accept the engine's output as-is — do NOT build a separate preview render path (RESEARCH.md: "anti-pattern" creating preview-specific renderers).
  </action>
  <verify>
    <automated>xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `App/Views/Controls/TextWatermarkInputView.swift` exists with multi-line `TextEditor` bound to `viewModel.config.watermarks[0]` text
    - `App/Views/Controls/PositionGridView.swift` exists with 3×3 `LazyVGrid` of 9 position buttons, each 40×40px with accessibility labels
    - `App/Views/Controls/ScaleStepperView.swift` exists with `Stepper` ±5% increment, range 0.01–0.90, label showing "Scale: N%"
    - `App/Views/Controls/ControlsView.swift` contains all three control sections in a ScrollView with 20px vertical spacing
    - `WatermarkViewModel.generatePreview()` exists and calls `engine.process(sourceURL:config:)` with 350ms debounce
    - `ContentView` has `.task(id: viewModel.previewIdentifier)` calling `generatePreview()`
    - Changing text, position, or scale in controls triggers a debounced preview update (executor verifies `.task(id:)` cancellation by observing only the latest render completes)
    - Project compiles: `swift build --target WatermarkApp` exits 0
  </acceptance_criteria>
  <done>User can type watermark text, tap a position button, and adjust scale via stepper. Preview image updates at 350ms debounce showing the watermark composited onto the photo. Controls are disabled when no photo is selected.</done>
</task>

<task type="auto">
  <name>Task 3: Share Flow + Thumbnail Navigation + Cancel + Error Handling</name>
  <files>App/Views/Share/ShareSheetView.swift, App/Views/Common/AsyncButton.swift, App/Views/Common/ErrorAlertModifier.swift, App/Views/Navigation/ThumbnailStripView.swift, App/ViewModels/WatermarkViewModel.swift, App/Views/ContentView.swift, App/Views/Controls/ControlsView.swift</files>
  <read_first>
    - App/ViewModels/WatermarkViewModel.swift — current state (from Tasks 1-2), existing properties for renderingState, fullResResult, showShareSheet
    - App/Views/ContentView.swift — current layout (from Tasks 1-2), existing .alert() and .sheet() modifiers
    - App/Views/Controls/ControlsView.swift — current controls layout (from Tasks 1-2)
    - Packages/WatermarkCore/Sources/WatermarkCore/Output/TempFileManager.swift — TempFileManager.cleanup(url:) signature
    - Packages/WatermarkCore/Sources/WatermarkCore/Models/ProcessResult.swift — ProcessResult structure
    - .planning/phases/02-main-app-photo-watermark-share/02-UI-SPEC.md (lines 259-276, 289-313, 161-177) — share button states/animation spec, share sheet bridge spec, thumbnail strip spec
    - .planning/phases/02-main-app-photo-watermark-share/02-RESEARCH.md (lines 359-439, 441-476) — Pattern 5 (UIActivityViewController), Pattern 6 (ThumbnailStrip), Pattern 7 (AsyncButton)
  </read_first>
  <action>
    **1. Create ShareSheetView.swift:**
    Create `App/Views/Share/ShareSheetView.swift` — `UIViewControllerRepresentable` wrapping `UIActivityViewController` per RESEARCH.md Pattern 5:
    - `let activityItems: [Any]` — pass `[tempFileURL]` from ProcessResult.url
    - `let excludedActivityTypes: [UIActivityViewController.ActivityType]?` — default `nil`
    - `let onDismiss: () -> Void` — cleanup closure
    - `makeUIViewController(context:)` — create `UIActivityViewController(activityItems:activityItems, applicationActivities:nil)`, set `completionWithItemsHandler = { _, completed, _, error in onDismiss() }` (fires on ALL dismiss paths: share, save, cancel)
    - `updateUIViewController` — no-op
    - Per SHAR-01: do NOT use `.saveToCameraRoll` exclusion; the share sheet itself doesn't save — the temp file is provided as an activity item and cleaned up on dismiss

    **2. Create AsyncButton.swift:**
    Create `App/Views/Common/AsyncButton.swift` — custom button with spinner per RESEARCH.md Pattern 7 and D-07:
    - Generic `Label: View`
    - `action: () async -> Void`
    - `@ViewBuilder label: () -> Label`
    - `@State private var isPerformingTask = false`
    - Button body: `HStack(spacing: 8)` with conditional: if `isPerformingTask` → `ProgressView().controlSize(.small)`, always show `label()`
    - Guard `guard !isPerformingTask else { return }` before starting task
    - `Task { isPerformingTask = true; await action(); isPerformingTask = false }`
    - `.disabled(isPerformingTask)`

    **3. Create ErrorAlertModifier.swift:**
    Create `App/Views/Common/ErrorAlertModifier.swift` — convenience ViewModifier bridging error state to `.alert()` (D-17):
    - Accept `errorMessage: Binding<String?>`, `isPresented: Binding<Bool>`
    - `.alert("Rendering Error", isPresented: isPresented) { Button("OK") { errorMessage.wrappedValue = nil } } message: { Text(errorMessage.wrappedValue ?? "Unknown error") }`
    - Apply as `.modifier(ErrorAlertModifier(...))` on ContentView

    **4. Create ThumbnailStripView.swift:**
    Create `App/Views/Navigation/ThumbnailStripView.swift` per D-13 and UI-SPEC §3.3:
    - Props: `photos: [PhotoItem]`, `@Binding currentIndex: Int`
    - Only render when `photos.count > 1` (D-13: shown only with 2+ photos)
    - `ScrollView(.horizontal, showsIndicators: false)` with `ScrollViewReader { proxy in ... }`
    - Inside: `LazyHStack(spacing: 8)` iterating `Array(photos.enumerated())`
    - Each thumbnail: 60×60px `Image(uiImage: thumb)` with `.aspectRatio(contentMode: .fill)`, `.clipShape(RoundedRectangle(cornerRadius: 8))`, `.overlay` of 3px accent border when `index == currentIndex`
    - Loading state: if `thumbnail == nil` → 60×60 placeholder with `ProgressView()` and `Color(.systemGray6)` fill
    - `.onTapGesture { currentIndex = index }`
    - `.onChange(of: currentIndex)` — `withAnimation { proxy.scrollTo(photos[newIndex].id, anchor: .center) }`
    - `.padding(.horizontal, 16)` for safe area
    - Height: 72px (60px thumbs + 12px padding)
    - Strip background: `.ultraThinMaterial`
    - Accessibility per photo: `.accessibilityLabel("Photo \(index + 1) of \(photos.count)")`, `.accessibilityHint("Double tap to select")`

    **5. Update WatermarkViewModel.swift — add render + share methods:**
    Add methods to ViewModel:

    `func renderAndPrepareShare() async`:
    - Guard `let sourceURL = currentPhoto?.sourceURL`
    - Set `renderingState = .rendering`
    - `do { let result = try await engine.process(sourceURL: sourceURL, config: config); fullResResult = result; renderingState = .done; /* update preview to full-res result */ previewImage = UIImage(data: try? Data(contentsOf: result.url ?? URL(fileURLWithPath: ""))) } catch { renderingState = .error(error); errorMessage = error.localizedDescription; showError = true }`

    `func presentShareSheet()`:
    - Guard `renderingState == .done` else return
    - Set `showShareSheet = true`

    `func cleanupTempFile()`:
    - If `let url = fullResResult?.url`, `try? TempFileManager.cleanup(url: url)`
    - Set `fullResResult = nil`, `renderingState = .idle`

    Add `currentPhoto` `didSet`-like behavior (via `.onChange` in ContentView): when currentIndex changes, discard fullResResult and reset renderingState to `.idle` (must re-render for new photo).

    **6. Update ControlsView.swift — add Share button:**
    Add `ShareButtonView` at the bottom of ControlsView.scrollContent. This uses `AsyncButton` with label `"Share"` and SF Symbol `square.and.arrow.up`. On tap: `await viewModel.renderAndPrepareShare()`. The button should show 3 visual states per D-06/D-07 and UI-SPEC §3.10:
    - State 1 (idle, `renderingState == .idle`): Blue accent pill, "Share" + arrow icon
    - State 2 (rendering, `renderingState == .rendering`): Same pill with `ProgressView()` + "Rendering…", disabled
    - State 3 (done, `renderingState == .done`): Green tint pill, `checkmark.circle.fill` icon + "Ready to Share", enabled — tapping calls `viewModel.presentShareSheet()`
    - Disabled entirely when `viewModel.currentPhoto == nil`

    **7. Update ContentView.swift — wire share sheet + cancel + error + thumbnail strip:**
    - Add thumbnail strip to PreviewArea: `if viewModel.hasMultiplePhotos { ThumbnailStripView(photos: viewModel.photos, currentIndex: $viewModel.currentIndex) }` positioned as overlay at bottom of PreviewAreaView
    - Wire share sheet `.sheet(isPresented: $viewModel.showShareSheet)` presenting `ShareSheetView(activityItems: [viewModel.fullResResult?.url].compactMap { $0 }, onDismiss: { viewModel.cleanupTempFile() })`
    - On `.onChange(of: viewModel.currentIndex)`: `viewModel.fullResResult = nil; viewModel.renderingState = .idle` (discard stale render when navigating)
    - Wire error alert per D-17: `.alert("Rendering Error", isPresented: $viewModel.showError) { Button("OK") { viewModel.errorMessage = nil } } message: { Text(viewModel.errorMessage ?? "Unknown error") }`
    - Wire `.confirmationDialog("Discard Changes?", isPresented: $viewModel.showCancelAlert) { Button("Discard", role: .destructive) { viewModel.confirmCancel() }; Button("Keep Editing", role: .cancel) {} } message: { Text("All unsaved watermark configurations for the remaining photos will be lost.") }` per D-15
    - Add a "Cancel" toolbar button (top toolbar, `.toolbar { ToolbarItem(placement: .navigationBarLeading) { Button("Cancel") { viewModel.requestCancel() } } }`) — only visible when `hasMultiplePhotos`
    - Update ContentView to use `.environment(viewModel)` rather than passing as @Bindable to simplify injection (or keep @Bindable — executor discretion, use whichever compiles cleanly with @Observable)

    **8. PhotosPicker auto-open refinement (D-01):**
    Verify that `.onAppear { if viewModel.photos.isEmpty { viewModel.showPicker = true } }` is present and working. The picker must NOT re-open on every view re-appearance — only on first launch when photos array is empty.

    **IMPORTANT:** The two-tap share flow (D-06): first tap triggers `renderAndPrepareShare()` which renders full-res and sets `renderingState = .done`. User sees the full-res result in preview. User taps Share again to confirm → share sheet opens. Do NOT auto-present share sheet after render — user must explicitly confirm per D-06.
  </action>
  <verify>
    <automated>xcodebuild -project Watermark.xcodeproj -scheme WatermarkApp -destination 'platform=iOS Simulator,name=iPhone 16 Pro' build 2>&1 | grep -q "BUILD SUCCEEDED"</automated>
  </verify>
  <acceptance_criteria>
    - `App/Views/Share/ShareSheetView.swift` exists as `UIViewControllerRepresentable` wrapping `UIActivityViewController` with `completionWithItemsHandler` calling `onDismiss()`
    - `App/Views/Common/AsyncButton.swift` exists with spinner state, `isPerformingTask` guard, and `.disabled()` during async work
    - `App/Views/Common/ErrorAlertModifier.swift` exists bridging `errorMessage` to SwiftUI `.alert()`
    - `App/Views/Navigation/ThumbnailStripView.swift` exists with `LazyHStack` of 60×60 thumbnails, scrollable, 3px accent border on current, `ScrollViewReader` auto-scroll
    - `WatermarkViewModel.renderAndPrepareShare()` sets `renderingState = .rendering`, calls `engine.process()`, sets `.done` on success or `.error` on failure
    - `WatermarkViewModel.cleanupTempFile()` calls `TempFileManager.cleanup(url:)` and resets state
    - Share button displays "Share" (idle) → "Rendering…" (spinning, D-07) → "Ready to Share" (green, D-06 confirm step)
    - `.sheet(isPresented: $viewModel.showShareSheet)` presents ShareSheetView with temp file URL
    - Thumbnail strip appears when `photos.count > 1`; tapping thumbnail changes current photo; auto-scroll to current
    - Cancel confirmation dialog exists with "Discard" destructive and "Keep Editing" cancel (D-15)
    - Error alert displays `error.localizedDescription` with "OK" dismiss (D-17)
    - Temp file cleanup fires on share sheet dismiss via `completionWithItemsHandler` (D-16)
    - Changing current photo discards stale `fullResResult` and resets `renderingState` to `.idle`
    - Project compiles: `swift build --target WatermarkApp` exits 0
  </acceptance_criteria>
  <done>User selects photos → configures text watermark → taps Share (spinner appears) → full-res renders → preview updates → user taps "Ready to Share" → iOS share sheet opens with temp file → user shares or cancels → temp file cleaned up. Multi-photo navigation works via thumbnail strip. Cancel discards all state and re-opens picker. Engine failures show error alert.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user input → engine | Text field input crosses into WatermarkCore engine; overly long text could cause memory pressure |
| share sheet → temp file system | Temp file URL exposed to system share sheet; cleanup must fire reliably |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Denial of Service | TextWatermarkInputView → WatermarkEngine | mitigate | Max 500-char text truncation in ViewModel before passing to engine; engine's own file size/pixel validation (Phase 1) provides additional defense |
| T-02-02 | Information Disclosure | ShareSheetView → UIActivityViewController | accept | Only temp file URL is shared — no metadata beyond what's rendered in the watermarked photo. Temp file is UUID-named (Phase 1). Risk: user shares watermarked photo to unintended recipient; this is user intent, not an app vulnerability |
| T-02-03 | Tampering | Temp file lifecycle | mitigate | UUID-based temp filenames (Phase 1 TempFileManager); cleanup fires on ALL UIActivityViewController dismiss paths via completionWithItemsHandler per D-16 |
| T-02-04 | Denial of Service | Thumbnail loading (PhotosPickerItem) | accept | Up to 20 photos × 200px thumbnails ≈ minimal memory. Full-res photos stored as URL references, not UIImage. Risk of crash from 20 concurrent Transferable loads mitigated by sequential processing |
</threat_model>

<verification>
Run `swift build --target WatermarkApp` to verify the app compiles cleanly. Manual verification: launch on iOS 18 Simulator, verify picker auto-opens, select 3 test photos, type watermark text, confirm preview updates, tap Share, confirm spinner → Ready, tap Share again, verify share sheet appears, cancel share sheet, verify no crash. Confirm thumbnail strip navigates between photos. Confirm Cancel → Discard flow works.
</verification>

<success_criteria>
- App launches with PhotosPicker auto-presenting per D-01
- User can select 1-20 photos with thumbnail-first loading per D-02/D-03
- Split layout shows preview (60%) and controls (40%) per D-09
- Text watermark input, 9-position grid, and ±5% scale stepper function correctly per D-11/D-08/D-18
- Preview updates at 350ms debounce via engine.process() per D-05
- Two-tap share: render → confirm → share sheet → cleanup per D-06/D-16
- Share button shows spinner during render per D-07
- Thumbnail strip enables multi-photo navigation per D-13
- Cancel clears state and re-opens picker per D-15
- Engine errors display as alert per D-17
- All output goes through TempFileManager — no camera roll save per SHAR-01
</success_criteria>

<output>
Create `.planning/phases/02-main-app-photo-watermark-share/02-01-SUMMARY.md` when done
</output>
