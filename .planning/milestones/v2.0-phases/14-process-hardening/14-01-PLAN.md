---
phase: 14-process-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "/Users/osama/.config/opencode/get-shit-done/templates/verif-plan-template.md"
  - "/Users/osama/.config/opencode/get-shit-done/templates/summary.md"
  - "/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md"
autonomous: true
requirements: [PHRO-01]

must_haves:
  truths:
    - "Executor writes a populated VERIFICATION.md after each task commit — no empty template left after plan completes"
    - "VERIFICATION.md contains UAT checkboxes, automated test counts, and manual QA steps per task"
    - "SUMMARY.md frontmatter carries tests_added and tests_modified fields auto-detected during execution"
    - "Plan completion gate blocks SUMMARY.md finalization when VERIFICATION.md is unpopulated"
  artifacts:
    - path: "/Users/osama/.config/opencode/get-shit-done/templates/verif-plan-template.md"
      provides: "Per-plan VERIFICATION.md template with frontmatter and table structure"
      min_lines: 40
    - path: "/Users/osama/.config/opencode/get-shit-done/templates/summary.md"
      provides: "SUMMARY.md template with tests_added/tests_modified frontmatter fields"
      contains: "tests_added"
    - path: "/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md"
      provides: "VERIFICATION.md write step + completion gate in executor contract"
      contains: "VERIFICATION.md"
  key_links:
    - from: "execute-plan.md create_summary step"
      to: "{phase}-{plan}-VERIFICATION.md"
      via: "gate check before SUMMARY.md finalization"
      pattern: "VERIFICATION.md"
    - from: "execute-plan.md execute step"
      to: "templates/verif-plan-template.md"
      via: "template copy during task execution"
      pattern: "verif-plan-template"
---

<objective>
Create the per-plan VERIFICATION.md infrastructure: a template that executors populate during task execution with UAT checkboxes, automated test counts, and manual QA steps. Add a completion gate to execute-plan.md that blocks plan finalization when VERIFICATION.md is empty. Extend SUMMARY.md frontmatter with `tests_added`/`tests_modified` fields for auto-detection.

Purpose: Per user decision D-01 (VERIFICATION.md Population Strategy), executors must populate VERIFICATION.md after each task commit so no plan ever completes with an empty verification template.
Output: One new template file, two modified workflow files — all under `get-shit-done/`.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/osama/Projects/Watermark/.planning/phases/14-process-hardening/14-CONTEXT.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
@/Users/osama/.config/opencode/get-shit-done/templates/verification-report.md
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create per-plan VERIFICATION.md template</name>
  <files>/Users/osama/.config/opencode/get-shit-done/templates/verif-plan-template.md</files>
  <read_first>
/Users/osama/.config/opencode/get-shit-done/templates/verification-report.md
  </read_first>
  <behavior>
    - The new file at templates/verif-plan-template.md is a Markdown template with a YAML frontmatter header
    - Frontmatter keys: `plan_id` (string), `status` (enum: empty | populated | verified), `uat_checks` (integer, default 0), `test_count` (integer, default 0), `verified_by` (string, default "executor")
    - Body contains a "UAT Checks" section with a markdown table: columns Task | UAT Criterion | Status | Evidence. One row per task, initially all `- [ ]` unchecked with `status: pending`
    - Body contains an "Automated Tests" section: a markdown table with columns Plan | tests_added | tests_modified | Source. `tests_added` and `tests_modified` read from SUMMARY.md frontmatter
    - Body contains a "Manual QA Steps" section: a markdown table with columns Task | Step | Expected Result. Populated from plan `<verify>` blocks
    - Body contains a "Verification Status" summary table with totals: UAT total/passed/failed, automated test count, manual QA steps count, overall status
    - Template is self-documenting — comments in the markdown explain what executors fill in
    - Template must NOT duplicate the existing verification-report.md (which is for verifier agents post-phase) — this is per-plan, populated during execution
  </behavior>
  <action>Create `/Users/osama/.config/opencode/get-shit-done/templates/verif-plan-template.md`.

Read the existing `templates/verification-report.md` first to understand conventions (frontmatter format, table styles, status values) — apply those conventions to the new template. The new template serves a different purpose (per-plan executor-populated vs per-phase verifier-populated) but should feel consistent with existing GSD template conventions.

The template must have:
1. YAML frontmatter with plan_id, status, uat_checks, test_count, verified_by
2. "## UAT Checks" — markdown table with Task | UAT Criterion | Status | Evidence columns, one placeholder row
3. "## Automated Tests" — markdown table Plan | tests_added | tests_modified | Source columns, reads from SUMMARY.md
4. "## Manual QA Steps" — markdown table with Task | Step | Expected Result columns
5. "## Verification Status" — summary table with totals and overall status (PASSED | FAILED | INCOMPLETE)
6. Inline guidance comments explaining what executors fill in at each section — these are Markdown comments (`<!-- ... -->`) visible during template editing but not rendered

Apply goal-backward verification: every section must have a clear "how to fill" comment so a new executor can populate it without reading external docs.

Per D-01: "UAT checkboxes per task" means one checkbox row per task. "automated test count (auto-detected)" means the template instructs the executor to read SUMMARY.md `tests_added`/`tests_modified` — not hardcode numbers. "manual QA steps column" means the Manual QA Steps table has a dedicated column for step-by-step instructions.</action>
  <verify>
    <automated>MISSING — Wave 0 must create test file first. Verify manually: file exists at `/Users/osama/.config/opencode/get-shit-done/templates/verif-plan-template.md`, has frontmatter with all five keys, four markdown tables present, inline guidance comments in each section.</automated>
  </verify>
  <done>verif-plan-template.md exists at the specified path with valid YAML frontmatter (5 keys), four markdown tables (UAT, Automated Tests, Manual QA, Verification Status), and inline guidance comments in each section explaining what executors populate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire VERIFICATION.md into execute-plan.md flow + extend SUMMARY.md schema</name>
  <files>
    /Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
    /Users/osama/.config/opencode/get-shit-done/templates/summary.md
  </files>
  <read_first>
/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
/Users/osama/.config/opencode/get-shit-done/templates/summary.md
/Users/osama/.config/opencode/get-shit-done/templates/verif-plan-template.md
  </read_first>
  <behavior>
    - SUMMARY.md template frontmatter gains two new fields: tests_added (integer, default 0) and tests_modified (integer, default 0)
    - execute-plan.md gains a step for VERIFICATION.md population after task commits
    - execute-plan.md create_summary step gains a gate: before finalizing SUMMARY.md, executor MUST check that {phase}-{plan}-VERIFICATION.md exists, has status != "empty", and has at least one UAT checkbox filled
    - If gate fails: executor halts, reports "VERIFICATION.md gate: plan completion blocked — VERIFICATION.md is empty or missing", does NOT write SUMMARY.md
    - The new step references templates/verif-plan-template.md as the source template
  </behavior>
  <action>
Three changes across two files:

### A. Extend SUMMARY.md template (`templates/summary.md`)

In the frontmatter schema (within the fenced code block showing the template), add two new fields after `requirements-completed`:

```yaml
tests_added: 0
tests_modified: 0
```

These go in the frontmatter block of the template (the markdown code fence showing what the executor writes). Also add them to the example SUMMARY at the bottom so executors see realistic sample values. Add a brief comment in the `<frontmatter_guidance>` section explaining these fields are auto-populated by the executor based on actual test file changes detected during the plan.

### B. Add VERIFICATION.md population step to execute-plan.md

In the `<step name="execute">` section, after step 3 (the per-task execution loop that includes task commit), insert a new instruction block. Place it after the task execution and acceptance criteria verification, but before step "3. Run `<verification>` checks".

The new block reads:

```
### VERIFICATION.md Population (per task)

After each task commits successfully, update `{phase}-{plan}-VERIFICATION.md` in the plan directory:

1. **First task:** copy `@/Users/osama/.config/opencode/get-shit-done/templates/verif-plan-template.md` to `.planning/phases/{phase_dir}/{phase}-{plan}-VERIFICATION.md`
2. **Populate UAT checkboxes:** For the current task, add a row to the UAT Checks table with the task name, a UAT criterion derived from the task's `<done>` block, mark status as `- [ ]` with `pending`
3. **Update test counts:** Count test files created/modified by this task (files matching `*.test.*`, `*.spec.*`, or under `tests/` that appear in the task's commit diff). Increment `uat_checks` frontmatter field. If tests were added or modified beyond what's already tracked, update the SUMMARY.md `tests_added`/`tests_modified` frontmatter fields.
4. **Populate manual QA steps:** For the current task, if `<verify>` contains `<human-check>` content, add a row to the Manual QA Steps table with the task name, the verification step, and expected result from `<done>`
5. **Update Verification Status table:** After each task, recalculate totals (UAT passed/failed, test count, QA steps count) and set overall status to `INCOMPLETE` until all tasks are done

Per D-01: "Automated test count detected by parsing SUMMARY.md frontmatter for tests_added / tests_modified fields". The executor auto-detects test files by inspecting `git diff --stat` for the task commit and extracting file paths matching test patterns.
```

### C. Add completion gate to create_summary step

In `<step name="create_summary">`, before "Create `{phase}-{plan}-SUMMARY.md`", insert a gate check:

```
### VERIFICATION.md Completion Gate

Before creating SUMMARY.md, verify the VERIFICATION.md is populated:

```bash
VERIF_PATH=".planning/phases/{phase_dir}/{phase}-{plan}-VERIFICATION.md"
if [ ! -f "$VERIF_PATH" ]; then
  echo "BLOCKED: VERIFICATION.md gate — {phase}-{plan}-VERIFICATION.md does not exist. Plan cannot be marked complete."
  exit 1
fi
VERIF_STATUS=$(grep "^status:" "$VERIF_PATH" | cut -d: -f2 | tr -d ' ')
if [ "$VERIF_STATUS" = "empty" ]; then
  echo "BLOCKED: VERIFICATION.md gate — VERIFICATION.md has status 'empty'. Populate UAT checkboxes and test counts before finalizing."
  exit 1
fi
UAT_TOTAL=$(grep "^uat_checks:" "$VERIF_PATH" | cut -d: -f2 | tr -d ' ')
if [ -z "$UAT_TOTAL" ] || [ "$UAT_TOTAL" -eq 0 ]; then
  echo "BLOCKED: VERIFICATION.md gate — uat_checks is 0 or missing. At least one UAT checkbox must be filled."
  exit 1
fi
echo "VERIFICATION.md gate: PASSED (status=$VERIF_STATUS, uat_checks=$UAT_TOTAL)"
```

Per D-01: "If executor fails to populate VERIFICATION.md: halt plan completion with a gate — SUMMARY.md must reference a populated VERIFICATION.md before plan is marked done". This bash gate enforces that before the Write tool creates SUMMARY.md.
```

Also add `- {phase}-{plan}-VERIFICATION.md` to the `git_commit_metadata` step file list so the VERIFICATION.md is committed alongside SUMMARY.md. In the worktree-mode commit, add it to the `--files` list; in non-worktree mode, same.

### Integration notes

- The `create_summary` step already has the "Critical ordering — write and commit SUMMARY.md as one atomic block" comment at the top. The VERIFICATION.md gate runs BEFORE the Write tool call — it does not break atomicity.
- The new execute-plan steps reference `verif-plan-template.md` — the file created in Task 1. Executors must have read access to `~/.config/opencode/get-shit-done/templates/`.
- The step numbering within `<step name="execute">` is preserved — the new VERIFICATION.md block is inserted as a sub-step, not a renumbering.
</action>
  <verify>
    <automated>MISSING — Wave 0 must create test file first. Verify manually:
    1. `grep "tests_added" /Users/osama/.config/opencode/get-shit-done/templates/summary.md` returns ≥1 match
    2. `grep "VERIFICATION.md" /Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md` returns ≥1 match
    3. `grep "verif-plan-template" /Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md` returns ≥1 match
    4. `grep "VERIFICATION.md Completion Gate" /Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md` returns ≥1 match
    5. The gate uses `exit 1` on each blocking condition
    </automated>
  </verify>
  <done>
  - SUMMARY.md template has `tests_added` and `tests_modified` frontmatter fields with default 0
  - execute-plan.md has a VERIFICATION.md population step (copy template, fill UAT checkboxes per task, update test counts, populate QA steps, update status table)
  - execute-plan.md create_summary step has a blocking gate that checks VERIFICATION.md exists, status != "empty", uat_checks > 0
  - git_commit_metadata step includes VERIFICATION.md in committed files
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| executor → VERIFICATION.md file | Executor writes to filesystem; no untrusted input — internal tooling artifact |
| executor → SUMMARY.md frontmatter | Executor populates tests_added/tests_modified based on git diff — git output is trusted in this context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | VERIFICATION.md on disk | accept | VERIFICATION.md is a local audit artifact, not a security boundary. Tampering would only affect traceability, not app security. |
| T-14-01B | Spoofing | tests_added/tests_modified counts | accept | Counts are derived from git diff output by the executor. If the executor is compromised, all bets are off — this is tooling, not a security boundary. |
| T-14-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages installed — all changes are to existing markdown workflow files and templates. slopcheck not required. |
</threat_model>

<verification>
- [ ] `verif-plan-template.md` exists at templates/ with valid frontmatter and 4 markdown tables
- [ ] `summary.md` template includes `tests_added` and `tests_modified` fields
- [ ] `execute-plan.md` includes VERIFICATION.md population instructions after task commits
- [ ] `execute-plan.md` create_summary step includes the blocking VERIFICATION.md gate
- [ ] `git_commit_metadata` step references VERIFICATION.md in committed files
</verification>

<success_criteria>
- verif-plan-template.md is a complete, self-documenting template that executors can populate without external docs
- SUMMARY.md frontmatter carries tests_added/tests_modified fields populated during execution
- execute-plan.md executors write VERIFICATION.md after each task commit
- Plan completion gates on VERIFICATION.md being populated (status != "empty", uat_checks > 0)
- VERIFICATION.md is committed alongside SUMMARY.md in the plan metadata commit
</success_criteria>

<output>
Create `.planning/phases/14-process-hardening/14-01-SUMMARY.md` when done
</output>
