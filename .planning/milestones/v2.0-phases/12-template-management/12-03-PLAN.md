---
phase: 12-template-management
plan: 03
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - App/Views/Templates/TemplateRowView.swift
  - App/Views/Templates/TemplatePreviewThumbnail.swift
  - App/Views/Templates/TemplateListView.swift
autonomous: true
requirements:
  - TMPL-02
  - TMPL-03
  - TMPL-05
  - TMPL-06

must_haves:
  truths:
    - "User can browse saved templates in a scrollable list with 48pt preview thumbnails showing the watermark applied to current media"
    - "User can tap a row to apply template config, swipe-to-delete with confirmation, and long-press for context menu"
    - "User can import a .watermarktemplate file via toolbar button"
  artifacts:
    - path: "App/Views/Templates/TemplateRowView.swift"
      contains: "struct TemplateRowView: View"
    - path: "App/Views/Templates/TemplatePreviewThumbnail.swift"
      contains: "struct TemplatePreviewThumbnail: View"
    - path: "App/Views/Templates/TemplateListView.swift"
      contains: "public struct TemplateListView"
  key_links:
    - from: "TemplateListView row tap"
      to: "WatermarkConfigurable.applyTemplate"
      via: "viewModel.applyTemplate(template)"
    - from: "TemplatePreviewThumbnail"
      to: "TemplateStore loadThumbnail/saveThumbnail"
      via: "cache read/write"
---

<objective>
Create the template list UI: TemplateRowView for individual rows, TemplatePreviewThumbnail for 48×48pt watermark previews, and TemplateListView as the full sheet with list, context menus, import, and empty state. Enables TMPL-02 (browse+apply), TMPL-03 (rename/duplicate/delete), TMPL-05 (import), TMPL-06 (preview thumbnails).

Purpose: These three views form the complete template library UI. TemplateListView is generic over WatermarkConfigurable & Observable matching the ControlsView pattern. It presents as a sheet from ContentView (wired in Plan 04). Rows show preview thumbnails rendered on-demand via .task, cached as PNG in caches directory. Context menu provides rename, duplicate, set default/remove default, export, delete. Toolbar provides import via .fileImporter.

Output: TemplateRowView.swift, TemplatePreviewThumbnail.swift, TemplateListView.swift.
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-template-management/12-PATTERNS.md
@.planning/phases/12-template-management/12-UI-SPEC.md
@.planning/phases/12-template-management/12-RESEARCH.md
@Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift
@App/Views/Navigation/ThumbnailStripView.swift
@Packages/WatermarkCore/Sources/WatermarkCore/Models/Template.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TemplateRowView — single template row with preview, name, default badge</name>
  <files>App/Views/Templates/TemplateRowView.swift</files>
  <read_first>
    App/Views/Navigation/ThumbnailStripView.swift
    .planning/phases/12-template-management/12-PATTERNS.md (TemplateRowView section lines 385-464)
  </read_first>
  <action>
    Create NEW file App/Views/Templates/TemplateRowView.swift.

    Declare struct TemplateRowView: View with properties: let template: Template, let sourceURL: URL?.

    Body: HStack(spacing: 12) containing:
    - TemplatePreviewThumbnail(template: template, sourceURL: sourceURL) — 48×48pt preview
    - VStack(alignment: .leading, spacing: 4) with Text(template.name).font(.body).lineLimit(1) and Text(template.createdAt, style: .date).font(.caption).foregroundStyle(.secondary)
    - Spacer()
    - If template.isDefault: Image(systemName: "star.fill").foregroundStyle(.yellow).font(.caption).accessibilityLabel("Default template")

    Padding: .padding(.horizontal, 12).padding(.vertical, 10). Row height minimum 44pt (per UI-SPEC line 50), preferred 60pt with thumbnail.

    Import SwiftUI and WatermarkCore.
  </action>
  <verify>
    <automated>grep -c "struct TemplateRowView: View" App/Views/Templates/TemplateRowView.swift</automated>
  </verify>
  <acceptance_criteria>
    - TemplateRowView.swift exists and compiles
    - Displays template name as .body font with .lineLimit(1)
    - Displays creation date as .caption .secondary using Text(date, style: .date)
    - Shows star.fill in .yellow when template.isDefault is true
    - Embeds TemplatePreviewThumbnail in leading position
    - Row uses HStack(spacing: 12) with .padding(.horizontal, 12) and .padding(.vertical, 10)
  </acceptance_criteria>
  <done>TemplateRowView renders a template row with thumbnail, name, date, and default badge</done>
</task>

<task type="auto">
  <name>Task 2: Create TemplatePreviewThumbnail — lazy 48×48pt watermark preview with caching</name>
  <files>App/Views/Templates/TemplatePreviewThumbnail.swift</files>
  <read_first>
    App/Views/Navigation/ThumbnailStripView.swift
    App/ViewModels/WatermarkViewModel.swift (createThumbnail function lines 703-714)
    .planning/phases/12-template-management/12-PATTERNS.md (TemplatePreviewThumbnail section lines 467-546)
  </read_first>
  <action>
    Create NEW file App/Views/Templates/TemplatePreviewThumbnail.swift.

    Declare struct TemplatePreviewThumbnail: View with: let template: Template, let sourceURL: URL?, @State private var thumbnail: UIImage?, @State private var isLoading = false. References: private let engine = WatermarkEngine.shared, private let store = TemplateStore.shared.

    Body uses Group with three states:
    1. If thumbnail exists: Image(uiImage: thumbnail).resizable().aspectRatio(.contentMode: .fill).frame(width: 48, height: 48).clipShape(RoundedRectangle(cornerRadius: 6))
    2. If isLoading: ProgressView().frame(width: 48, height: 48).background(Color(.systemGray6), in: RoundedRectangle(cornerRadius: 6))
    3. Else (placeholder): Image(systemName: "doc.text").font(.system(size: 20)).foregroundStyle(.secondary).frame(width: 48, height: 48).background(Color(.systemGray6), in: RoundedRectangle(cornerRadius: 6))

    Attach .task { await generateThumbnail() }.

    generateThumbnail() async:
    A) Check cache: if let cached = store.loadThumbnail(for: template.id), let image = UIImage(data: cached) → set thumbnail, return.
    B) Guard sourceURL != nil else { return }.
    C) Set isLoading = true, defer { isLoading = false }.
    D) Process with engine: engine.process(sourceURL: url, config: template.config).
    E) On result: load result URL data, create CGImageSource, call CGImageSourceCreateThumbnailAtIndex with kCGImageSourceThumbnailMaxPixelSize = 48 * UIScreen.main.scale.
    F) Convert to UIImage, set thumbnail, cache PNG via store.saveThumbnail(pngData, for: template.id).

    Replicate the createThumbnail pattern from WatermarkViewModel.swift lines 703-714 for the CGImageSource thumbnail options. Cache key: template.id — re-generate only when template config changes (Plan 01 TemplateStore already handles the file storage).
  </action>
  <verify>
    <automated>grep -c "struct TemplatePreviewThumbnail: View" App/Views/Templates/TemplatePreviewThumbnail.swift && grep -c "generateThumbnail" App/Views/Templates/TemplatePreviewThumbnail.swift && grep -c "CGImageSourceCreateThumbnailAtIndex" App/Views/Templates/TemplatePreviewThumbnail.swift</automated>
  </verify>
  <acceptance_criteria>
    - TemplatePreviewThumbnail.swift exists and compiles
    - Shows ProgressView while rendering, placeholder doc.text icon when idle with no cache
    - Uses .task modifier for lazy async generation
    - Checks TemplateStore.shared.loadThumbnail cache before rendering
    - Caches result via TemplateStore.shared.saveThumbnail after rendering
    - Uses CGImageSourceCreateThumbnailAtIndex with kCGImageSourceThumbnailMaxPixelSize = 48 * scale
    - Frame is exactly 48×48pt with RoundedRectangle clip shape cornerRadius 6
  </acceptance_criteria>
  <done>TemplatePreviewThumbnail lazily renders and caches 48×48pt watermark previews</done>
</task>

<task type="auto">
  <name>Task 3: Create TemplateListView — full template list sheet with context menus and import</name>
  <files>App/Views/Templates/TemplateListView.swift</files>
  <read_first>
    Packages/WatermarkCore/Sources/WatermarkCore/UI/ControlsView.swift
    Packages/WatermarkCore/Sources/WatermarkCore/UI/LayerListView.swift
    .planning/phases/12-template-management/12-PATTERNS.md (TemplateListView section lines 259-381)
    .planning/phases/12-template-management/12-UI-SPEC.md
  </read_first>
  <action>
    Create NEW file App/Views/Templates/TemplateListView.swift.

    Declare public struct TemplateListView<ViewModel: WatermarkConfigurable & Observable>: View with @State var viewModel: ViewModel. Local state: @State private var showRenameAlert = false, @State private var renameTemplateID: UUID?, @State private var renameText = "", @State private var showDeleteConfirmation = false, @State private var deleteTemplateID: UUID?, @State private var showFileImporter = false.

    Body: List containing ForEach over TemplateStore.shared.templates, each rendering TemplateRowView. Row interactions:
    - .onTapGesture: calls viewModel.applyTemplate(template), sheet dismisses
    - .swipeActions(edge: .trailing): Button(role: .destructive) with Label("Delete", systemImage: "trash") that sets deleteTemplateID and showDeleteConfirmation = true
    - .contextMenu with 6 items: "Apply" (Image: checkmark), "Rename" (pencil), "Duplicate" (doc.on.doc), "Set as Default" (star) or "Remove Default" (star.slash) based on template.isDefault, "Export" (square.and.arrow.up), "Delete" (trash, .red, role: .destructive)

    Context menu actions:
    - Apply: viewModel.applyTemplate(template)
    - Rename: set renameTemplateID, renameText = template.name, showRenameAlert = true
    - Duplicate: try? TemplateStore.shared.duplicate(template)
    - Set as Default: try? TemplateStore.shared.setDefault(id: template.id)
    - Remove Default: try? TemplateStore.shared.removeDefault(id: template.id)
    - Export: serialize template via TemplateStore.shared.exportData(for:), write to temp URL with .watermarktemplate extension, present ShareSheetView (same pattern as ContentView line 81)

    After List, add modifiers:
    - .alert("Rename Template", isPresented: $showRenameAlert) with TextField("Template name", text: $renameText), Button("Save") calling TemplateStore.shared.rename(id:to:), Button("Cancel", role: .cancel)
    - .alert("Delete Template?", isPresented: $showDeleteConfirmation) with message Text("Template \"{name}\" will be permanently deleted."), Button("Delete Template", role: .destructive) calling TemplateStore.shared.delete(id:), Button("Cancel", role: .cancel)
    - .overlay showing ContentUnavailableView when templates.isEmpty: heading "No Templates Saved", systemImage "doc.text.magnifyingglass", description per UI-SPEC line 122
    - .fileImporter(isPresented: $showFileImporter, allowedContentTypes: [UTType(filenameExtension: "watermarktemplate")!]) with handler calling TemplateStore.shared.import(from:), on failure showing error alert

    Toolbar: ToolbarItem(placement: .navigationBarTrailing) with Button showing Image(systemName: "square.and.arrow.down") + Text("Import") that sets showFileImporter = true.

    Navigation title "Templates" with .navigationBarTitleDisplayMode(.inline).

    Present via .sheet in ContentView — the NavigationStack wrapper goes in ContentView (Plan 04).
  </action>
  <verify>
    <automated>grep -c "public struct TemplateListView" App/Views/Templates/TemplateListView.swift && grep -c "ForEach(TemplateStore.shared.templates)" App/Views/Templates/TemplateListView.swift && grep -c ".fileImporter" App/Views/Templates/TemplateListView.swift && grep -c ".contextMenu" App/Views/Templates/TemplateListView.swift</automated>
  </verify>
  <acceptance_criteria>
    - TemplateListView.swift exists and compiles with generic ViewModel constraint matching ControlsView
    - List renders ForEach over TemplateStore.shared.templates
    - Row tap calls viewModel.applyTemplate(template)
    - Swipe actions provide red Delete button
    - Context menu has: Apply, Rename, Duplicate, Set/Remove Default, Export, Delete
    - Rename alert has TextField pre-filled with current name
    - Delete has confirmation alert with template name
    - Empty state shows ContentUnavailableView with icon, heading, and body per UI-SPEC
    - Toolbar has Import button opening .fileImporter filtered to .watermarktemplate
    - Uses "Templates" as navigation title
  </acceptance_criteria>
  <done>TemplateListView compiles with full CRUD context menus, import, and empty state</done>
</task>

</tasks>

<verification>
- TemplateRowView: grep for "struct TemplateRowView", "TemplatePreviewThumbnail", "star.fill"
- TemplatePreviewThumbnail: grep for "generateThumbnail", "CGImageSourceCreateThumbnailAtIndex", "store.loadThumbnail"
- TemplateListView: grep for "TemplateListView", "ForEach(TemplateStore", ".fileImporter", ".contextMenu"
</verification>

<success_criteria>
- [ ] TemplateRowView renders thumbnail, name, date, and default badge in correctly styled row
- [ ] TemplatePreviewThumbnail caches and displays 48×48pt watermark previews lazily
- [ ] TemplateListView shows scrollable list with tap-to-apply, swipe-to-delete, and context menu
- [ ] Import button opens .fileImporter and calls TemplateStore.import()
- [ ] Empty state shows ContentUnavailableView with correct copy
- [ ] All three views compile in the App target
</success_criteria>

<threat_model>
Plan 03 creates template list UI views. The .fileImporter import flow delegates to TemplateStore.import() which enforces all mitigations from Plan 01 (schema version validation, isDefault forced false, size limits). The export flow serializes from TemplateStore.exportData() which strips isDefault. Preview thumbnail generation reads from local caches directory — no network access. No new HIGH-severity threats introduced.
</threat_model>

<output>
Create `.planning/phases/12-template-management/12-03-SUMMARY.md` when done
</output>
