---
phase: 12-template-management
plan: 05
type: execute
wave: 3
depends_on: ["12-01", "12-02"]
files_modified:
  - ShareExtension/ShareExtensionViewModel.swift
  - PhotoEditExtension/PhotosExtensionViewModel.swift
  - App/Info.plist
  - ShareExtension/Info.plist
  - PhotoEditExtension/Info.plist
autonomous: true
requirements:
  - TMPL-04
  - TMPL-05

must_haves:
  truths:
    - "The default template watermark automatically appears when importing media via the share sheet or Photos edit extension"
    - "Template files can be opened from the Files app and Safari downloads"
  artifacts:
    - path: "ShareExtension/ShareExtensionViewModel.swift"
      contains: "var showSaveTemplateAlert: Bool = false"
      contains: "TemplateStore.shared.defaultTemplate"
    - path: "PhotoEditExtension/PhotosExtensionViewModel.swift"
      contains: "var showSaveTemplateAlert: Bool = false"
      contains: "TemplateStore.shared.defaultTemplate"
    - path: "App/Info.plist"
      contains: "UTExportedTypeDeclarations"
      contains: "com.watermark.app.template"
    - path: "ShareExtension/Info.plist"
      contains: "UTExportedTypeDeclarations"
    - path: "PhotoEditExtension/Info.plist"
      contains: "UTExportedTypeDeclarations"
  key_links:
    - from: "ShareExtensionViewModel loadPhotoFromProvider"
      to: "TemplateStore.defaultTemplate"
      via: "auto-apply on share extension import"
    - from: "PhotosExtensionViewModel startEditing"
      to: "TemplateStore.defaultTemplate"
      via: "auto-apply on fresh edit only (not re-edit)"
    - from: ".fileImporter UTI filter"
      to: "Info.plist UTExportedTypeDeclarations"
      via: "system UTI registry"
---

<objective>
Add template protocol conformance and default template auto-apply to both extension ViewModels, and register the `.watermarktemplate` UTI in all three Info.plist files. Enables TMPL-04 (default auto-apply in share extension and Photos edit extension) and TMPL-05 (export/import file recognition).

Purpose: ShareExtensionViewModel and PhotosExtensionViewModel need showSaveTemplateAlert and showTemplateList properties to satisfy the expanded WatermarkConfigurable protocol (even though extensions don't show template UI). Both must auto-apply the default template when they receive media. The PhotosExtensionViewModel additionally guards against overwriting re-edit config with the default template. The UTI registration makes `.watermarktemplate` files recognizable by the system for .fileImporter filtering, Files app "Open In", and Safari downloads.

Output: Modified ShareExtensionViewModel.swift (2 properties + 2 auto-apply points), modified PhotosExtensionViewModel.swift (2 properties + 1 conditional auto-apply), modified App/Info.plist (UTExportedTypeDeclarations + CFBundleDocumentTypes entry), modified ShareExtension/Info.plist (UTExportedTypeDeclarations), modified PhotoEditExtension/Info.plist (UTExportedTypeDeclarations).
</objective>

<execution_context>
@/Users/osama/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/osama/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/12-template-management/12-PATTERNS.md
@.planning/phases/12-template-management/12-RESEARCH.md
@ShareExtension/ShareExtensionViewModel.swift
@PhotoEditExtension/PhotosExtensionViewModel.swift
@App/Info.plist
@ShareExtension/Info.plist
@PhotoEditExtension/Info.plist
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add template protocol conformance and auto-apply to extension ViewModels</name>
  <files>ShareExtension/ShareExtensionViewModel.swift, PhotoEditExtension/PhotosExtensionViewModel.swift</files>
  <read_first>
    ShareExtension/ShareExtensionViewModel.swift
    PhotoEditExtension/PhotosExtensionViewModel.swift
  </read_first>
  <action>
    SHAREEXTENSION — Three changes per PATTERNS.md lines 864-906:

    A) Add two protocol conformance properties after the existing var declarations (find "var showError: Bool = false" or similar section end). Add:
    var showSaveTemplateAlert: Bool = false
    var showTemplateList: Bool = false
    With comment "// Phase 12: Template Management (protocol conformance — extension doesn't show UI)".

    B) In loadPhotoFromProvider, after "await generatePreview()" (line 261), insert:
    if let defaultTemplate = TemplateStore.shared.defaultTemplate { config = defaultTemplate.config }

    C) In loadVideoFromProvider, after the preview generation call (around line 306), insert same auto-apply.

    PHOTOSEXTENSION — Two changes per PATTERNS.md lines 909-959:

    A) Add the same two protocol conformance properties as ShareExtension.

    B) In startEditing, after the adjustmentData re-edit block (lines 169-173) and before "Task { await generatePreview() }" (line 176), insert CONDITIONAL auto-apply:
    // Phase 12: Auto-apply default template on fresh edit only (not re-edit)
    if input.adjustmentData == nil, let defaultTemplate = TemplateStore.shared.defaultTemplate {
        self.config = defaultTemplate.config
    }
    The condition input.adjustmentData == nil is CRITICAL: when the user re-edits a previously watermarked photo, the saved config from adjustmentData must be restored, NOT overwritten by the default template.
  </action>
  <verify>
    <automated>grep -c "var showSaveTemplateAlert: Bool = false" ShareExtension/ShareExtensionViewModel.swift && grep -c "var showTemplateList: Bool = false" ShareExtension/ShareExtensionViewModel.swift && grep -c "TemplateStore.shared.defaultTemplate" ShareExtension/ShareExtensionViewModel.swift && grep -c "var showSaveTemplateAlert: Bool = false" PhotoEditExtension/PhotosExtensionViewModel.swift && grep -c "TemplateStore.shared.defaultTemplate" PhotoEditExtension/PhotosExtensionViewModel.swift && grep -c "input.adjustmentData == nil" PhotoEditExtension/PhotosExtensionViewModel.swift</automated>
  </verify>
  <acceptance_criteria>
    - ShareExtensionViewModel has showSaveTemplateAlert: Bool = false and showTemplateList: Bool = false
    - ShareExtensionViewModel auto-applies default template in loadPhotoFromProvider (after generatePreview)
    - ShareExtensionViewModel auto-applies default template in loadVideoFromProvider (after generateVideoPreview)
    - PhotosExtensionViewModel has showSaveTemplateAlert: Bool = false and showTemplateList: Bool = false
    - PhotosExtensionViewModel auto-applies default template ONLY when input.adjustmentData == nil (fresh edit)
    - Re-edit scenario (adjustmentData != nil) does NOT apply default template — preserves saved config
    - Both files compile in their respective extension targets
  </acceptance_criteria>
  <done>Both extension ViewModels conform to expanded WatermarkConfigurable and auto-apply default template</done>
</task>

<task type="auto">
  <name>Task 2: Register .watermarktemplate UTI in all three Info.plist files</name>
  <files>App/Info.plist, ShareExtension/Info.plist, PhotoEditExtension/Info.plist</files>
  <read_first>
    App/Info.plist
    ShareExtension/Info.plist
    PhotoEditExtension/Info.plist
    .planning/phases/12-template-management/12-RESEARCH.md (UTI registration XML at lines 715-741)
    .planning/phases/12-template-management/12-PATTERNS.md (Info.plist section lines 963-1023)
  </read_first>
  <action>
    THREE Info.plist modifications per PATTERNS.md lines 963-1023:

    A) App/Info.plist — TWO insertions:

    A1) Add UTExportedTypeDeclarations as a NEW top-level key (after existing CFBundleDocumentTypes or UIApplicationShortcutItems blocks). The key value is an array containing one dict:
    - UTTypeIdentifier: com.watermark.app.template
    - UTTypeDescription: Watermark Template
    - UTTypeConformsTo: array with single string public.json
    - UTTypeTagSpecification: dict with public.filename-extension array ["watermarktemplate"] and public.mime-type array ["application/json"]

    A2) Inside the existing CFBundleDocumentTypes array (after the Video dict closing </dict>), add a new dict:
    - CFBundleTypeName: Watermark Template
    - LSHandlerRank: Alternate
    - LSItemContentTypes: array with single string com.watermark.app.template

    B) ShareExtension/Info.plist — Add UTExportedTypeDeclarations as a NEW top-level key (after CFBundleDisplayName). Same dict structure as App/Info.plist A1.

    C) PhotoEditExtension/Info.plist — Add UTExportedTypeDeclarations as a NEW top-level key (after CFBundleDisplayName). Same dict structure as App/Info.plist A1.

    The XML must be valid plist. Use the exact key names, string values, and nesting structure from RESEARCH.md lines 715-741. The UTI identifier "com.watermark.app.template" must be identical across all three files.
  </action>
  <verify>
    <automated>grep -c "UTExportedTypeDeclarations" App/Info.plist && grep -c "UTExportedTypeDeclarations" ShareExtension/Info.plist && grep -c "UTExportedTypeDeclarations" PhotoEditExtension/Info.plist && grep -c "com.watermark.app.template" App/Info.plist && grep -c "com.watermark.app.template" ShareExtension/Info.plist && grep -c "com.watermark.app.template" PhotoEditExtension/Info.plist</automated>
  </verify>
  <acceptance_criteria>
    - App/Info.plist contains UTExportedTypeDeclarations key with template UTI dict
    - App/Info.plist CFBundleDocumentTypes contains Watermark Template entry with LSItemContentTypes com.watermark.app.template
    - ShareExtension/Info.plist contains UTExportedTypeDeclarations key with template UTI dict
    - PhotoEditExtension/Info.plist contains UTExportedTypeDeclarations key with template UTI dict
    - All three files use UTI identifier "com.watermark.app.template"
    - All three files declare public.filename-extension "watermarktemplate"
    - All three files declare conformance to public.json
    - XML is valid plist (can be parsed by plutil)
  </acceptance_criteria>
  <done>.watermarktemplate UTI registered in all three targets, enabling .fileImporter filtering and system file recognition</done>
</task>

</tasks>

<verification>
- ShareExtensionViewModel: grep for "showSaveTemplateAlert", "showTemplateList", "TemplateStore.shared.defaultTemplate"
- PhotosExtensionViewModel: grep for same plus "input.adjustmentData == nil"
- Info.plist files: grep for "UTExportedTypeDeclarations" and "com.watermark.app.template" in all three files
- XML validity: plutil -lint each Info.plist
</verification>

<success_criteria>
- [ ] ShareExtensionViewModel has showSaveTemplateAlert and showTemplateList set to false
- [ ] ShareExtension auto-applies default template on photo AND video import
- [ ] PhotosExtensionViewModel auto-applies default template only on fresh edits (adjustmentData == nil)
- [ ] Re-edit scenario preserves saved config, does not apply default template
- [ ] All three Info.plist files contain UTExportedTypeDeclarations with com.watermark.app.template
- [ ] App/Info.plist additionally has CFBundleDocumentTypes entry for Watermark Template
- [ ] All target schemes build successfully (build gate passes)
</success_criteria>

<threat_model>
Plan 05 adds protocol conformance properties and auto-apply to extension ViewModels, plus UTI registration in Info.plist files. Auto-apply reads from TemplateStore.defaultTemplate with same mitigations as Plan 04. The PhotosExtensionViewModel guards against overwriting re-edit config by checking input.adjustmentData == nil. UTI registration is declarative only — no executable code. No new HIGH-severity threats introduced.
</threat_model>

<output>
Create `.planning/phases/12-template-management/12-05-SUMMARY.md` when done
</output>
