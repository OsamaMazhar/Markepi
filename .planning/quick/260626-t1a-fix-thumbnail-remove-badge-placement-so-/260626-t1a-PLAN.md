---
quick_id: 260626-t1a
title: Fix thumbnail remove badge placement
status: in_progress
created: 2026-06-26
---

# Quick Plan: Fix Thumbnail Remove Badge Placement

## Goal

Fix the batch thumbnail edit-mode red remove circles so each badge sits on the top-left corner of its own thumbnail instead of drifting above the strip.

## Tasks

1. Replace the scroll-offset badge overlay with a thumbnail-local badge overlay.
2. Keep the delete affordance compact, tappable, and visually anchored to the thumbnail corner.
3. Run lightweight verification and record any blocked build checks.

## Verification

- Run `git diff --check` on the touched SwiftUI file.
- Run an iOS build-for-testing if local Xcode permissions allow it.
