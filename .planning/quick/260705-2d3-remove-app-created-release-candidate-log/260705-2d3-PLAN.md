---
quick_id: 260705-2d3
description: Remove app-created release candidate logs without changing app logic
status: in_progress
---

# Quick Task 260705-2d3 Plan

## Goal

Silence app-created logging in Release builds so the release candidate console no longer includes Markepi/WatermarkCore diagnostics, without changing app behavior.

## Tasks

1. Gate app-created `Logger` and `os_log` calls behind `#if DEBUG` in app, share-extension, and WatermarkCore runtime source files.
2. Keep all non-logging control flow, error handling, user-visible state, metadata processing, and export behavior unchanged.
3. Verify no ungated app-created logging calls remain in runtime sources and run a Release build gate if practical.
