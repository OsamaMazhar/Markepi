---
quick_id: 260705-2d3
description: Remove app-created release candidate logs without changing app logic
status: complete
completed_at: 2026-07-04
commit: working tree
---

# Quick Task 260705-2d3 Summary

## Outcome

Release builds no longer compile app-created logging calls from the app, Share Extension, or WatermarkCore runtime sources.

## Changes

- Wrapped photo render diagnostics, image-load diagnostics, white-frame diagnostics, shared inbox logs, share handoff logs, notification scheduling logs, template/config/store logs, Live Photo logs, and video export/validation logs in `#if DEBUG`.
- Made `Logger(...)` instances DEBUG-only where they existed solely for those diagnostics.
- Preserved control flow, thrown errors, returned validation warnings, receipts, progress callbacks, metadata handling, and export behavior.

## Verification

- Static sweep: no ungated `os_log`, `Logger`, `print`, or `debugPrint` calls remain in `App`, `ShareExtension`, or `Packages/WatermarkCore/Sources`.
- Release build: `xcodebuild -project Watermark.xcodeproj -scheme "WatermarkApp (Release)" -configuration Release -destination 'generic/platform=iOS' CODE_SIGNING_ALLOWED=NO build` succeeded.
